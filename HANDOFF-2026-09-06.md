# DSH 汇报设计闭环交接（2026-09-06）

> 供下一位开发 Agent 使用。本文以当前实际仓库状态为准，不覆盖旧版 `HANDOFF.md`。

## 一、当前版本坐标

### Pre

- 仓库：`ArchitectureWorld/pre-design`
- 分支：`feat/pre-v2.0.0`
- 当前 HEAD：`c700027495a7809d2ae8181bad85bdc1ca7d8ced`
- 远程 HEAD：与本地一致
- 关键职责：前期成果、来源、事实/Gate 约束、视觉生成桥接、汇报设计任务路由

### Studio

- 仓库：`ArchitectureWorld/presentation-tools`
- 分支：`feat/report-studio-v0.2.0-layout`
- 当前 HEAD：`15d7c35443c81d2d0adb5c93f62edb507f8560d0`
- 远程 HEAD：与本地一致
- 工作区：仅有未跟踪 `.tmp/`，不要提交
- 关键职责：草案、页面素材库、布局候选、真实 PNG 预览、Proposal/CAS 保存和进度统计

## 二、本轮已完成

### Pre 已完成

- 汇报设计任务可独立于 `nextWorkflow = null` 的前期工作项继续执行。
- 视觉请求通过 Studio 设计能力和 Proposal 路径处理。
- 视觉候选采用前会校验项目、页面、来源哈希和授权范围。
- 取消、未派发和不确定视觉请求不会被错误标记为已完成。
- 视觉桥接保留候选身份和来源，不通过全项目强制同步挂图。

主要验证：

- `pnpm test`：102 files / 696 tests 通过
- `pnpm test:built`：2 files / 5 tests 通过
- `pnpm typecheck`：通过
- `git diff --check`：通过

### Studio 已完成

- 受控设计上下文和布局候选生命周期。
- 不可变候选、CAS 基线、来源映射和保护页校验。
- 编辑器与后台预览共用 renderer。
- 真实 Edge PNG 预览、素材完整性和布局自动检查。
- AI/通用非现场素材的页面披露检查已在隔离源码阶段补齐。
- 设计进度统计区分：前期工作项、汇报页数、相关视觉、候选、Agent 看图、实际保存。
- adopted-but-unlinked 视觉候选可在版本冲突后重读并重试挂接，不重复生成。

### Fixture / Harness

- 冻结的 current112 输入捕获：160 个来源文件，Studio Revision 112，Pre Revision 59。
- Fixture focused tests：87/87 通过，含真实 Pre schema oracle。
- 隔离 harness tests：24/24 通过。
- 中文公开 fixture 文件名已修正，字节哈希保持不变。
- Fixture 最新提交：`f490f2bfabd3306eaeb8d6fe1f814b3ca36dc38d`。

## 三、明确未完成

以下事项不能写成“已部署”或“功能已验收”：

1. 最终修补尚在独立源码 stage 中，尚未重新应用到共享 Studio Git 工作树。
2. 最终修补尚未重新打包成新的 Studio tgz。
3. 尚未完成使用最新包的真实隔离 DSH Host 启动和工具注册验收。
4. 尚未完成真实 DSH Agent 读取成果、看 PNG、返修并保存的端到端证据。
5. 尚未确认当前模型是否真正支持图像输入；不得切模型或修改正式认证配置绕过该门禁。
6. 正式 DSH 当前未作为本轮部署结果重新确认；不要直接操作正式 3080 端口。
7. 尚未重新部署正式环境。
8. 尚未把最终修补推送到远程；远程目前只包含已提交的 Pre/Studio HEAD。

## 四、重要并行变动

Studio 共享仓曾有另一任务提交“批注直接应用、不再保留 Proposal”的逻辑：

- `e86be96 feat: apply annotation tasks directly`
- `912273d test: update annotation UI acceptance`

这套语义与本轮批准的 Proposal 审批边界不同。下一位 Agent 不得默认把它和本轮设计闭环混合发布，必须先得到明确裁定并重新审查相关测试、vendor 和包内容。

## 五、下一位 Agent 的执行顺序

1. 先读取本文、`progress.md`、设计规格和设计计划。
2. 核对两个仓库的 HEAD、远程 HEAD、工作区状态；保留 `.tmp/` 和其他外部改动。
3. 读取独立源码 stage 的最终修补报告和 diff，不从共享工作树复制未审查修改。
4. 对最终修补执行一次 scoped review；若修改产品，重新运行受影响的测试。
5. 从审核后的精确源码构建新的 Studio 包，并记录源码 commit、包文件清单和 SHA-256。
6. 用最新 Pre/Studio 包重新物化冻结 current112 fixture；先验证原始来源、手工保护页和 57 条工作流保持。
7. 在新 home、新 workspace、新 Session 中启动隔离 DSH，禁止使用正式 3080 和正式数据。
8. 先做 text-only 工具链验收；只有当前模型明确支持图片输入时，才执行真实 PNG 看图和返修验收。
9. 若隔离验收通过，备份正式包和正式数据，再只重启确认的 DSH Node。
10. 最后分别将 Pre 和 Studio 的审核后提交正常快进推送到既有远程分支，并核对本地 HEAD、远程 HEAD、tgz hash 和安装文件清单。

## 六、禁止事项

- 不创建新分支，不 force push，不 reset，不覆盖用户工作区。
- 不重新运行已经完成的 57 个前期工作项。
- 不修改模型、认证、全局 HOME/USERPROFILE 或正式凭证。
- 不停止 Tailscale，不随意占用或重启 3080。
- 不把 Codex 生成的页面、坐标、截图或预制答案当作 DSH Agent 能力证据。
- 不把 `layoutRef`、候选生成或 57/57 作为汇报完成率。
- 不把 AI/通用图当作现场证据；页面必须保留相应披露。
- 不把 `pageAssets` 引用数当作已查看、已相关或已保存。
- 不提交 `.tmp/`、凭证、私有少潭河原始资料或完整未脱敏聊天记录。

## 七、关键文件

- 当前交接：`HANDOFF-2026-09-06.md`
- 旧交接（可能过时）：`HANDOFF.md`
- 设计规格：`docs/superpowers/specs/2026-09-06-dsh-report-design-loop-design.md`
- 设计计划：`docs/superpowers/plans/2026-09-06-dsh-report-design-loop.md`
- 实施进度：`.superpowers/sdd/2026-09-06-dsh-report-design-loop/progress.md`
- 隔离验收说明：`.superpowers/sdd/2026-09-06-dsh-report-design-loop/task-6b-report.md`
- 视觉/素材规则：`docs/presentation-material-registry.md`

## 八、当前结论

本轮已经完成大量代码、测试和隔离测试基础设施，但**尚未完成最终 Host 验收、正式部署或最终修补远程同步**。下一位 Agent 必须从本文的“未完成”部分继续，不得根据旧 `HANDOFF.md` 宣称已发布。
