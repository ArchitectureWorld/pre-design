# Development ledger — v2.1.0

Plan: docs/superpowers/plans/2026-09-24-pre-v2.1.0-foundation.md
Source: approved specification v1.2; branch base main@a1c1ad12b469d086037ec66e4bd89c7c0e4ac422.

- Remote branch created from the verified merged main commit.
- Bootstrap CI commit a001800b8806884fe023fbc9a8904d669ea68f95 provides immutable source archives and full regression.
- Local source tree verified equal to remote tree 6a35ea1e76cf1ed277449cdf061a7a50cd649ff4 before edits. Node 24.11.0 and pnpm 10.15.1 restored from that CI run; dependency archive checksums verified.
- Baseline typecheck passed; full baseline regression started before feature edits.
- Ruling: milestone 1 ships only the hash-locked identity/routing projection of the complete approved specification, plus a deterministic importer. Full research fields and execution are NOT replaced with reduced generic schemas. This keeps the existing 57-workflow runtime usable while introducing the new 62-module foundation.
- Pre-flight: index -> projection, index -> candidate manifest, index -> legacy references and projection -> DSH read-only command share the same immutable catalogue. No separate UI copy of the catalogue.
- RED foundation: 3 expected assertion failures (missing engine, missing DSH command, package still 2.0.2).
- RED engine: behaviour tests added before production code; explicit missing-implementation assertion fails, not a missing-import collection error.
- Bootstrap GitHub CI run 35954358715 passed typecheck, build/full regression and built-package regression.

- Source-fidelity check: all 176 edges match approved edge records by edgeId; output ports match all62 items. Array order differs only because the late-added failure-mode edge is grouped by its target in the compiled index. Fingerprints sort edges by edgeId explicitly; no edge content was changed.
- Local baseline regression: 2053 passed / 3 failed / 6 skipped. All three failures came from Chromium refusing root without --no-sandbox in the container, before feature edits. Remote bootstrap CI passed the complete baseline. An external wrapper was tried but could not override the golden fixture's browser selection; production browser security defaults remain unchanged. Remote CI is authoritative for those browser tests.
- Added display preflight for screen pixels and A3 physical PPI, analytical crop blocking and retained-photo area. This is geometry/density checking only, not image review or publication approval.

- Final local focused suite: 45 new tests passed. Command and actual Host regressions add 19 passing tests (64 total in the combined run). Built-package/Host targeted suite passed 6 tests. Typecheck and source importer --check passed.
- Two existing command-list expectations were updated to include the new registered command, after the complete suite exposed them; both targeted suites then passed. The remaining three full-suite container failures are the same root/Chromium baseline limitation.
- Integration publication uses a hash-verified one-time patch, scoped exclusively to v2.1.0, following the repository's existing materialize-workspace workflow pattern. It commits only after focused tests, typecheck, full regression and built-package regression pass. It never forces a ref or touches main. Workflow logs and artifacts record the exact tested final tree/commit; successful transfer does not itself mean tests passed.

## Milestone 2 — source-bound regional OD
Plan: `docs/superpowers/plans/2026-09-24-pre-v210-regional-od.md`.
Start: `v2.1.0@c2d7f7e6e4854a4393c6c8dad65cbd1714bcfeae`, restored tree verified equal to `9e95e3916e77fa45cf9a6bc739950a3f1500e30d`.

- Packaged the complete approved v1.2 source losslessly using built-in Brotli. The decompressed SHA remains `1ad0d7b0a2b52a84515cd97ab313c5596c0da02bea6175eaf4390d39d99d2a08`; identity/routing index and original professional descriptions were not rewritten.
- Ruling: original field descriptions are available for all62, but exact executable field schemas and a production command exist only for the regional OD slice in this milestone. No menu count is reported as completed research.
- RED: observed missing-spec, OD validation/calculation, bundle/replay and persistence/command failures before implementing their corresponding features. Added adversarial tests for rehashed result/publication edits, timeout/abort, historical scope and injected-transport identity.
- Host regression found the real repository throws `session-not-bound` rather than returning an empty context. Added a failing real RepositoryError test and mapped that exact error to the safe `RESEARCH_PROJECT_REQUIRED` response; all other unexpected errors remain redacted.
- Final focused local result: **88 passed / 8 files**, including45 M1 tests and43 new M2 tests. Typecheck, source lock/full-spec validation, and built-package/real-Host tests **5 passed / 2 files**.
- Synthetic rehearsal: injected-response and offline runs both saved append-only bundles and passed no-network replay; these do not certify actual coordinates, road accessibility or live provider results. A native SVG/HTML result was visually checked at desktop and mobile widths.
- The sandbox baseline's three root/Chromium golden-PDF failures are not bypassed by production changes. Full remote Ubuntu regression is the final complete-suite authority.
- Publication protocol: a one-time SHA-verified transfer restores exactly the approved source and reviewed text files on `v2.1.0`, runs focused/type/full/built regression and an offline audit rehearsal, and commits only the tested tree. It never modifies main, force-pushes, publishes npm packages, or includes customer/project documents. Final commit, tree, commands and logs are retained as a CI verification artifact.
- Scope remaining: automatic source/candidate discovery; licensed geographic basemap; all62 field adapters and persistent scheduler; full image-review/page/PPT/PDF integration; professional factual validation and user-device deployment.
