# 汇报文案迭代验证

## 修正依据

阅读并提炼用户提供的石桥水库125页汇报稿，见 [方法与语言契约](reference-shiqiao-planning-method.md)。该稿只提供方法参考，其事实、数字、经营与审批结论不移植为少潭河事实。

## 已改动的行为

- 完成专业策划后，独立的文本类 DSH 子会话编写汇报内容稿；自动链路先写文案，再逐页配图和导出。
- 来源、章节和论点页有稳定身份；同版本内容复用，失效稿与过程记录保留。
- Presentation、HTML、打印版及可编辑 PPTX 共用正文；完整产品和比较表不再被裁成一句三点或移到讲稿。
- 导出前必须存在匹配来源的文案；同步失败不得显示成功；取消信号传递到导出；手动模式不会因缺自动授权而被误拦截。
- 新稿不使用重复正文生成的SVG卡片充当分析图。概念图提示按具体产品/体验/空间逐页生成。

## 首轮真实运行发现的问题

首轮4个任务实际使用 `antigravity / gemini-3.8-flash-high`。场地章节以 `max-tokens` 结束，输入与缓存合计193231 tokens，未得到有效输出；机会章虽完成，审读仍发现管控术语重复、后台评分与旧版修正措辞，未采纳为新成果。

据此进行代码修正：章节来源从工作流前缀选取改为业务字段选择，完整依据用字典消重。少潭河7章输入从158343–318803字符缩减至34562–62437字符，选中的完整语句、限定条件与来源身份不截断。写作规则区分各章职责，正文拒绝“已剔除旧版”、内部评分、内部编号等工作记录。内容策略升为 `planning-manuscript-2026-09-18.2`，首轮候选留在原检查点，不视为完成稿。

章节输出上限调整为32768，并记录模型停止原因和配置上限。单章失败只停止新任务派发，在途章节继续完成并保存；两次内容纠错仅计算有校验反馈的修改，执行失败恢复仍计入正常模型任务额度。打印与PPTX按实际文案长度续页，保留完整段落、产品描述和表格行，不作二次摘要。

## 代码验证

首版全套154文件/1041项通过；接入取消与同步状态修正后的针对性71项通过，类型检查与构建包5项检查通过。后续真实运行修正另行记录针对性验证，不以这些测试证明文案已经可用。

2026-09-18 00:48，新策略、恢复逻辑及分页的11文件87项针对性测试通过，TypeScript检查通过。覆盖章节派发与恢复、完整来源及依据消重、取消/同步、各格式正文投影与现有Edge打印布局。该批测试发生在上述修正之后；154文件全套结果是修正前基线，不冒充修正后全套验证。

## 内容验收

七章真实完整稿已逐页审读并修订，正文与资料依据分别保存。审读修正了茶类工序混用、游客受益被写成付费、未调查被写成实测为零、把茶厂鉴定绑定整个首期开园、阶段触发顺序倒置、销售额与购销利润重复计入，以及无依据工程数量和绝对保证。审读记录与原稿备份保留在项目 `.pre-design/`。页数、配图率或Schema通过不作为专业阅读的替代。

## 第二轮真实初稿与继续修正

策略2已完成机会、场地、定位、空间、启动5章；产品章仍以max-tokens结束（配置32768、可见输出1280 tokens），运营章未派发。在途章节均正常保存。不能从可见usage精确判断上游的隐藏思考量或限制原因；后续请求上限改为65536，并继续保留明确停止诊断。

独立审读发现机会章仍有“多维协同”“造血载体”“商用容量保持为0”等不适于委托方汇报的表达，且将宏观车程、工程数量、政策适用写得过于确定。已拒绝将这批初稿直接发布。

新增生产必经的统一成稿编辑：仍使用配置中的文本类真实子会话，通读七章后重写文字；独立任务计入现有模型额度。编辑保留章节、页面、产品和来源身份，保存初稿及编辑检查点。编辑未完成或失败时，不生成正式文案；已完成章节复用，不重跑专业工作流。成稿正文与详细来源/图文说明分别保存，防止技术说明混入用户拿到的文案。

部署使用内容哈希命名的独立包路径；已验证同路径替换包可能命中旧安装缓存，并通过安装后代码哈希校验拦截。后续部署必须使用独立路径且逐文件核对，不能只凭安装命令成功。

## 用户指定的模型切换

2026-09-18 用户明确要求使用 workbuddy 的 Deepseek-V4.1-Flash，Gemini 仅用于生图。经 DSH 实时模型目录确认，全局 text/web 两类设置为 `workdubbyAI / global:deepseek-v4.1-flash`；image 保持 `antigravity / gemini-3.1-flash-image`。配置通过原生插件接口保存并回读，不直接篡改存储文件。旧配置另行备份。继续复用已完成的5章检查点，仅恢复缺少的产品与运营章，并由 DeepSeek 统一编辑全部章节。

最终编辑与版式修订的13文件100项针对性测试、类型检查、构建包5项检查已通过。实际 Edge 压力样例包含长标题、多段正文、长产品字段、超长表格行和两张配图，11个正文页边界检查通过；PPTX只验证了文本及XML几何，未作Office截图验收。

本次部署包SHA-256：`06d78d4a5c2eabe28c60c569358285a36f92810c548224676875fda5628bff94`。安装后代码哈希与本地一致，配置、凭据、项目状态及Report Studio运行时校验未变；备份目录 `C:\Users\2899\.dsh\backups\manuscript-deploy-20260918-010929`。

## DeepSeek运行与成稿拆分

初次切换后的两章均因本地7863端口的WorkBuddy2API网关未启动而TRANSPORT失败；恢复既有网关后，健康检查200、无鉴权401、鉴权模型目录200。实时目录的Deepseek-V4.1-Flash声明最大输出128000。随后两个DSH真实子会话完成产品章及运营章，记录分别为输入39812/输出21844、输入40945/输出18605 tokens。主会话与text/web类别均已切到用户指定的DeepSeek，Gemini保留生图用途。

用户已明确补授最多10次DeepSeek任务，新授权`authorization-76961c1f-018b-4340-b0e6-cc91e299df53`；旧120次记录保留、按需生图设置保留。整稿编辑第一次实际消耗输出65537 tokens并以max-tokens结束，未采纳不完整输出。该问题不属于Gemini独有限制。

编辑实现改为按章并发：每章接收同一套成稿语言规则及全稿主题关联，只返回自身页面；默认3并发，独立检查点，失败时保存其他在途章节，所有章节完成后才采用全文。编辑纠错总共最多2次，所有新请求继续受既有全局任务额度约束。旧的整稿失败记录保留，不掩盖或标记为成功。

## WorkBuddy 容量限制与恢复

分章编辑第一次三并发时，机会、场地两章完成，定位章返回 `503 no_healthy_account`。网关安全配置及源码确认当前仅一个 global 账户，`max_in_flight_global=2`。六次失败请求均未分配账号、耗时为0；成功的两条请求持续约31秒、37秒。DSH在两槽仍被占用的16秒内用完重试，属于容量不足，未进入上游，不是正文或DeepSeek本身出错。

文案起稿和编辑现按每次运行时的全局文本路由确定并发：当前 `workdubbyAI` 为2，其他路由上限5。不会切回Gemini文字模型，不调整网关的账号限制。变更只影响文案调度；已经完成的章节和既有专业工作项不重跑。后续增加账户或换用其他网关时，应重新核实该路由的实际容量。

容量回归先确认旧代码失败，修正后55项文案及宿主测试、TypeScript、构建包5项检查通过。部署包SHA-256：`e56ecaae8f615c4949e9ce53b164d1944794099817c804a84ad9ab3fc4ea7f3d`；安装文件哈希一致，配置、凭据、业务存储、Report Studio运行时校验未变。备份 `C:\Users\2899\.dsh\backups\manuscript-deploy-20260918-015107`。

真实机会、场地章审读仍发现具体问题：茶类工序未分清、游客受益写成付费行为、法律约束写得过于绝对，以及资料不足说明挤占方案正文。结构通过不代表内容验收通过，须在发布成果前逐项修订并保留审读记录。

## 完整稿恢复与本轮额度

补授10次文字模型任务已全部使用，记录为6条完成、4条失败。运营章的一次输出实际包含完整9页，但非产品页带有全部为空的可选product对象，导致旧校验拒绝。新校验只忽略非产品页的全空product；真实产品和部分填写的对象仍须通过完整校验。

编辑服务可以重新校验有完整returnedContent、stopReason为completed的旧候选。运营章由原attempt0无调用恢复，恢复身份记入检查点；原失败执行记录及额度消耗保持原样。没有追加文字模型调用，也没有重跑已完成的57项专业工作流。

## 手动导出和图解缺口修正

真实导出发现手动命令遗漏prepareVisuals，已修正为文案准备、配图准备、导出依次执行；配图失败立即阻止成功结果。分析页过去只有subject/purpose/caption，不能证明已经有图。新增可选diagram节点与连线结构，运行时校验身份、网格坐标、数量和边引用，经SVG资产链绑定到同一正文页；不将正文卡片冒充分析图。DSH结构化输出Schema保持兼容其支持的子集。

原始影像通过sourceMaterialKey精确匹配已扫描登记，不接受任意文件路径、不随机取图。当前稿手工审定25项关系图及1项库区影像绑定。图注说明拟议关系或资料性质，不绘制未经核验的地理边界。关系图和原始图绑定不调用文字或生图模型。

封面采用正文总体定位，审定图注替代后台素材名称，来源和条件说明保留。真实PDF预览发现关系图字号偏小及表格固定页底留白，已增加图解宽度、节点字号，按较窄正文栏分页，并让无图表格紧接正文。

本地预览的完整正文、标题、主张、产品字段及表格单元格均可从PDF回读，未见越过页面边界的文本。场景图仍须以原生DSH实际生成、采用和导出的最终包进行验收；预览不代表最终配图完成。

## Office 实际排版与最新回归范围

本机 PowerPoint 16 实际打开预览 PPTX 时发现，百分比行距在 Office 中的实际高度大于分页估算，9页正文触及页脚。渲染器已改为精确行距：正文18pt、主张23.04pt、标题32.4pt，并指定中文语言。随后96页预览的 Office 文本框边界检查未发现溢出候选，且已放大复核此前异常页。此结论对应当时只有25项关系图和1幅原始库区影像的预览；带全部场景图的最终文件另验。

02:42开始的全套验证通过158文件、1126项。其后精确行距及中文语言修订通过2文件11项针对性检查。03:03新增的网页资料分离修正通过3文件18项回归和TypeScript检查；03:05重新构建后的5项包检查通过。全套结果属于该批后续小修之前，不冒充后续全套结果。验证日志保存于 `work/manuscript-full.log`、`work/manuscript-office-layout.log`、`work/manuscript-source-separation-green.log` 和 `work/manuscript-built.log`。

## 网页成果与内部工作资料分离

复核导出链路又发现，`renderConditionalHtml` 无条件把全部冻结策划条目及依据附到网页成果末尾。新增有正式文案的回归样例，先确认旧代码失败，再改为：汇报正文网页只呈现采用的文案和图表，完整工作条目单独保存为 `planning-sources.html`。依据内容、HTML转义及原始记录保留。未带正式文案的兼容路径维持旧行为。

仅成果排版版本升为 `planning-manuscript-2026-09-18.3`，用于使旧导出包失效并重新生成文件；文案策略和成稿编辑版本仍为 `.2`，不会因此发起新的文字任务。图像生成完成前不部署、不重启DSH。

## 生图实际内容审看

逐图审看发现卫生服务点场景含有错字和不成句的像素文字；展销场景上下留白较大；材料图将尚未确定的螺旋钢桩基础画成了具体做法。仅格式、尺寸和文件哈希通过不能代表图像内容可用。旧图保留记录，03:27已通过原生拒收命令撤下三图，只补这三项。材料页的视觉任务已与正文保持相同确定性，改为可见构件和材料意向，不绘制地下基础或地层。正文段落、来源身份、七章结构和25项关系图保持原样，修订与旧稿留存。

视觉角色中“明确标注AI概念表现”的措辞已改为由汇报版面图注负责，不再把图注文本作为画面任务传入，并明确画面铺满、无说明区或像素文字。提示修正后的25项相关测试及5项重新构建包检查通过；图片质量仍由实际新图审看确认。

第一轮13项真实图像任务均由 `antigravity / gemini-3.1-flash-image` 完成；一项经历DSH自身的TRANSPORT重试后完成。测试客户端在30分钟截止时结束等待，但后端保留并完成13项图片；没有把客户端等待截止当作整轮生成失败重做。客户端后续等待上限调整为60分钟，逐页已有资产检查继续生效。

03:25安装包SHA-256：`274d47006887cf57f177dd3da38729b7d02634fedb19d0ca45a4e91a9aee9000`。安装代码哈希匹配，配置、凭据、业务存储与Report Studio运行时保护项未变，既有DSH服务恢复并重新附着原会话。备份：`C:\Users\2899\.dsh\backups\manuscript-deploy-20260918-032448`。回执：`work/manuscript-deployment-receipt.json`。

## 最终 Office 图注修正与部署

带图的102页 PPTX 在 PowerPoint 16 中实际打开，发现一处长图注因固定0.28英寸高度而出现孤立的闭括号。新增真实图片导出回归先确认旧代码失败，再按图注换行数预留高度，使用中文语言及12pt精确行距，图片相应让出图注空间。修正仅影响正文页的 PowerPoint 图注排版。成果排版版本升为 `planning-manuscript-2026-09-18.4`；文案与成稿策略版本不变。

修正后21项相关回归、TypeScript、构建及5项构建包检查通过；没有重复宣称已跑后续全套。03:45部署包 SHA-256：`457012ff3b13901f7929f3e1edd4f22a972236dfa3c30dcb79501a6f39d08bbf`。保护项与安装代码哈希核对通过，备份 `C:\Users\2899\.dsh\backups\manuscript-deploy-20260918-034456`。DSH恢复后，原会话执行 `/preplan-export`，于03:47返回成功。

## 本轮最终成果与验收边界

最终原生包为 `conditional-0372a928-8fe6-4694-990d-6761a0f83e3f`，sourceRevision为103。七章56个文案内容页，网页含封面及条件说明共58段；PDF和PPTX按正文与表格延续分页，各102页。图片共39个唯一资产：13张AI场景、25张关系图、1张原始库区影像。HTML有40处图片引用（封面复用1张）；PPTX有107处图像使用，续页复用同页图。

- PDF逐页渲染、全部联系表审看以及关键场景/表格放大复核完成；全部标题、主张、正文、产品字段及表格单元格回读无遗漏，无页面边界越界。
- PPTX直接XML检查无正文或媒体遗漏。本机PowerPoint16实际打开并渲染102页；图注修正后纵向溢出为0，放大查看3、23、43、74、101页。仍有27处Office文本宽度超出文本框的候选，按实际文本宽度与幻灯片及图片框核对，页面越界0、图片相交0，属于栏间留白内的中文行末排版，不将候选数伪报为0。
- PowerPoint另存PDF的字形映射导致部分中文文本抽取不完整。该辅助文件只用于视觉检查，不作为交付PDF；完整性由PPTX原生XML及正式PDF回读确认。不得据此重写正文。
- 侧边浏览器实际打开最终HTML，40处图片全部完成加载，茶园漫游与运营投入页导航和实际画面检查通过，没有横向滚动溢出。全部图片为本地资源。汇报正文不含内部工作条目附录。
- HTML与PPTX采用相同39项媒体。三张拒收旧图的文件SHA-256均未出现在最终媒体中。没有把拒收记录删除或改成成功。

分页保留了全部正文，因此部分表格续页较空、图解在续页复用；这次验收确认内容完整、图像实际呈现及可读性，不宣称全套已完成人工精修排版。

## 交付与后续监督检查点

交付目录：`D:\shaotanhe\成果汇报\2026-09-18-r103`。主文件为 `少潭河前期策划汇报文案.md`、`少潭河前期策划汇报.pdf`、`少潭河前期策划汇报.pptx`，完整离线网页在 `网页汇报/index.html`；来源与工作条目单独存放于 `资料依据`。45个交付文件逐项复制哈希一致；复制后的网页再检没有缺图或外部图片依赖。原生包及旧交付目录保留。

文案JSON SHA-256：`8dab750be4caaad992bdd45bd5eaae06ca7e2720de237b59dbeffee24ed7750c`。

- 正文Markdown：`5472f5f97685cf5c3d5e09528d6a6abe492e0cd9ce6989c29c8f6fe2db415329`
- PDF：`448932b3138a781fe23533049016c2661243b02f11133a3415bf803406eec413`
- PPTX：`55d11f823fba47ab04c3799f8534f3e767c5c9666f5e0824f2ae658da32bb4be`
- HTML：`cb65415877c85ebd84de2b343501016f4962929553dafb7a9f158cc6395e6c13`

完整回执在 `work/manuscript-delivery-receipt.json`、`work/manuscript-final-package.json`，验收证据在 `work/manuscript-delivery-qa`。最终导出期间没有新增子Agent执行记录，结束时没有活动子Agent；补授10次文字任务已经用完，不能自动续授。后续监督以该包与检查点为基准，只处理新增问题，不把旧拒收图、已恢复的失败任务或Office辅助PDF文本抽取差异当作新故障重跑。

## 04:28持续监督

DSH由预期web进程监听127.0.0.1:3080。检查少潭河292份主/子会话记录，交付后没有文件更新；子Agent执行记录仍为273条，没有活动执行或工作流。宿主stderr为空。45个交付文件及审定文案哈希未变，HTML、PDF、PPTX经原生下载路由实际读取均为HTTP200，下载字节SHA-256与交付回执一致。

本轮没有新的运行故障需要修正，未修改或重新部署业务代码，未调用模型或重跑工作流。新增只读巡检脚本 `work/monitor-manuscript-delivery.mjs`，对比会话更新时间、执行计数、成果哈希及真实下载路由；本次真实运行通过。证据：`work/manuscript-monitor/2026-09-17T20-28-26-547Z.json`。出现变化时再读取相应会话定位，不反复解压未更新的125MB主会话。
