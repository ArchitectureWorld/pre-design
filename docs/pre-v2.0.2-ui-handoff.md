# Pre 2.0.2 · R3.1 · 当前 UI/UX 交接

开发支线 `pre-V2.0.2`，main 基准 `801afcc794b34fa734ba624303ed9552b152407c`。本轮接续 `7588ec0`，保留 `6815bf7` 的并行 R2 修订。产品仍为 **2.0.2**，R3.1 仅为候选修订标识；候选包必须通过 COMMIT.txt 和 SHA256SUMS.txt 识别，不可只比较 package version。

## 已实现

- A/B/C/D 液态玻璃；C/A 与 D/B 分别保持相同面板几何、折射、磨砂和悬浮，仅色系不同。外观本地保存，不修改模型设置或启动任务。
- Klein 与必填配套 LLM 同行，主项和每个备用项一致。普通图像 API 无配套选择。添加仅 `+`；排序/删除/刷新/文件夹/外观使用图标，完整可访问名称保留。辅助说明按需展开；错误仍明确显示。
- 保留显式配套、备用提升时的配对、目录工具身份过滤、执行记录中的启动/实际模型及冻结链。地址和工作流仍在原 DSH ComfyUI 插件中管理；不宣称目录可见就是已验证能力。
- 修复每 5 秒自动读取短暂 disabled 导致原生下拉菜单自动关闭。选择期间不更新选项，选择结束再恢复；慢读取不能阻断保存或覆盖新结果。
- 保留工作区零输入启动、原始资料等待、打开文件夹、已有项目不重复启动、未保存草稿、revision 冲突和会话清理。

## 验证与下一步

本地 UI 49/49、类型检查、构建与构建包测试 5/5；浏览器原生菜单 28 项、布局交互 46 项、切换标签返回后重新选择检查通过。客户端 87,533 bytes。

本地全量回归为 2026 通过、3 失败、6 跳过，失败来自 root Chromium 启动 PDF 测试的沙箱限制，详情见下方修复记录。远程是否全部通过，必须检查本提交对应 Actions，不使用父提交结果。交付包包含本轮完整日志和浏览器证据。

DSH SDK 仍为 0.1.5-rc.1，Node >=24.11.0，Project Format / Presentation Contract 0.1.0，research/v2.0.1。没有后端、调度、依赖或契约变更。

HTML 使用实际 React 组件、模拟接口，非实际 DSH 安装；宿主 WebView/CSP、真实模型目录、保存与真实 ComfyUI 出图仍待环境验收。不得自动安装、重启、合并、发布。

## 详细记录

- [R3.1 下拉稳定性、竞争条件与验证](pre-v2.0.2-dropdown-fix.md)
- [R3 主/备用同行配置](pre-v2.0.2-r3-inline.md)
- [R2 简约比例与完整生图要求](pre-v2.0.2-r2-review.md)

```sh
pnpm install --frozen-lockfile
pnpm typecheck
pnpm test
pnpm test:built
node scripts/build-ui-review-v202.mjs
```

预览输出为 `work/ui-review-r3-1/pre-V2.0.2-R3.1-preview.html`，直接用浏览器打开。真实插件包应取同一次通过验证的 Actions 产物，核对提交与哈希后，在用户授权的测试宿主安装；目前未安装，也未合并 main。
