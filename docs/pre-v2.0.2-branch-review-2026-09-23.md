# Pre 2.0.2 支线 Review · 2026-09-23

> 本文保留 2026-09-23 的支线状态与部署证据。Pre 2.0.2 于 2026-09-24 按用户要求合入 `main`；当前合并与发布状态见 [版本权威](version-matrix.json)。

## 当前版本与软件说明

| 对象 | 当前权威 |
|---|---|
| Pre 产品、插件、npm 包 | `2.0.2`；开发支线 `pre-V2.0.2`，尚未合并或正式发布 |
| 上游主线基线 | Pre 2.0.1，`main@801afcc794b34fa734ba624303ed9552b152407c` |
| DSH Host | `0.1.5-rc.1` |
| Presentation 标准 / 依赖包 | 格式 `0.1.0`；`@architectureworld/presentation-contracts@0.1.1` |
| Research 合同 | `research/v2.0.1`，随 2.0.1 合入 main，继续由 2.0.2 使用 |

Pre 是 DSH 中的前期策划 Skill / Workspace 插件；DSH Agent 执行任务。一个 DSH Workspace 对应一个 Pre 项目，项目文件写入 Workspace 标准目录。浏览器中的主题和背景偏好不作为项目、模型或执行状态的权威。

## 已同步的用户可读入口

- [README](../README.md)：统一产品版本、当前分支、素材登记规则、Presentation 依赖和一屏布局说明。
- [版本权威](VERSIONING.md)与[机器矩阵](version-matrix.json)：将 Research 的状态改为已合入主线，将本地宿主验收状态更新为已验证，并修正 Presentation 包坐标。
- [部署说明](deployment-dsh-v0.1.5-rc.1.md)：使用当前支线、CI 工作流与部署边界。
- [同步开发看板](../research/v2.0.1/source-audit.html)：保留现有路径，标题统一为“同步开发看板”。按 8 章 57 项显示要做的步骤、资料与网站来源、来源核查状态、汇总与分析方法、最低证据要求及预期输出；可按工作流、来源或方法搜索并展开详情。来源核查时间取自 `source-audit-status.json`，不能当作当前项目的运行证据。
- [R4 历史交接](pre-v2.0.2-ui-handoff.md)：保留当时的原始验收记录，标明当前状态以本 Review 为准。历史设计计划与旧版本合同未改写。

## 本地部署与界面验收

本地 Web profile 的插件升级前，已检查没有运行中的类任务或工作流，并备份 Web profile、storages 与候选包；最终候选的备份目录为 `C:\Users\2899\.dsh\backups\compact-home-20260923-20260923-224317`，包 SHA-256 为 `eca397cf09e5d689d8d837efd731ce804b46c6f85a4e4b9cd6b1fb0b347d17e7`。部署时保护了设置、凭据、项目和会话存储的哈希。安装器保留了同版本包的旧 README 和看板文件，因此已单独备份旧文件、同步候选中的两份文件，随后核对候选包全部文件与安装目录一致。端口 `127.0.0.1:3080` 已由重启后的 DSH 进程监听，已登录页面能读出 `shaotanhe` 项目与四张子 Agent 配置卡。

在 1645×918 视口下，四张卡片为两列，主页滚动区 `scrollHeight=clientHeight=918`，无横向溢出；项目卡、全部模型控件、备用模型及执行记录按钮均在首屏。780 与 480 宽度下恢复单列，均无横向溢出。

验证：`pnpm test` 通过 211 个测试文件、2060 项测试（2 项按原规则跳过）；`pnpm test:built` 通过 2 个测试文件、5 项测试。版本矩阵、TypeScript 类型、Research 看板结构与 `git diff --check` 均通过。

## 核查边界

“Research 链已映射”表示设计合同可以从工作流追溯到步骤、数据项、来源和分析方法；不能据此断言某个项目的 57 项已运行成功。看板展示的是 2026-09-13 的 25 个来源核查记录，当前网址能否访问需要实际运行时重新判断。浏览器安全策略禁止直接访问本地 `file://` 看板；已核对生成结果、结构与研究测试，未将该文件的浏览器视觉验收写成已通过。

当前没有 `v2.0.2` Tag、正式 Release 或合并 main。历史 `feat/pre-v2.0.1`、V2.0.0 设计文档、旧合同编号保留为历史追溯，不作为当前活动版本。
