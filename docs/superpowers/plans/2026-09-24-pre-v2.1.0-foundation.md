# Pre 2.1.0 implementation — milestone 1

Goal: make the approved v1.2 research catalogue executable as a safe, read-only planning foundation inside DSH, without falsely switching the legacy 57-workflow executor to 62 completed workflows.

Authority: user-approved `pre-design_62项研究与成果统一规格_v1.2.html` and `unified-data.json` from its verification package. The exact source JSON SHA-256 is `1ad0d7b0a2b52a84515cd97ab313c5596c0da02bea6175eaf4390d39d99d2a08`. Runtime baseline: main@a1c1ad12b469d086037ec66e4bd89c7c0e4ac422; development branch: v2.1.0.

Architecture: a compiled planning index keeps module, port, conditional-edge, candidate-artifact and legacy-reference identities from the approved source. The existing canonical JSON utility provides hashes. A read-only DSH command consumes the same engine used by tests. Full professional field schemas, research execution, geographic acquisition and final layout adapters remain explicit later milestones. No additional harness, model or package dependency is introduced.

## Global constraints
- Keep DSH 0.1.5-rc.1, Node >=24.11.0, pnpm 10.15.1 and Presentation Standard Project 0.1.0 unchanged.
- No edits to main, no merge, no npm publish; all commits target v2.1.0.
- No changes to legacy contracts or stored project identities. Candidate references never inherit confirmed/approval/quality.
- Unknown conditions are not false. Require typed alternative ports, exact snapshots and current lifecycle.
- Candidate figures are not generated files; formatting renditions are not new analytical objects.
- No dependency on user-local absolute paths in shipped runtime.

## Tasks
1. Capture merged baseline and run its typecheck/build/full tests; retain commands and results.
2. RED: add tests for all 16 boolean projections, unknown paths, malformed indexes, legacy references, dynamic figure counts and DSH command registration. Run tests before implementation.
3. GREEN: implement typed index compilation, validation, three-valued conditions, DAG projection and input readiness checks; retain source projection hashes and reproducible importer.
4. Implement candidate deliverable planning and read-only legacy reference migration; test immutable inputs and forged IDs.
5. Integrate `/preplan-research-plan` in actual DSH command registration with explicit condition inputs, no model/network/state writes. Update product/version matrix to 2.1.0 development; retain old UI and rendering rules.
6. Run focused tests, typecheck, build, full regression and built-package tests. Review diff, record limitations and handoff, commit to v2.1.0 and verify remote CI.

## Acceptance boundaries
This milestone plans research; it does not execute all 62 professional analyses. Do not market the 62 planning nodes as 62 fully implemented schemas or providers. The attached complete v1.2 specification remains the source for later field compilation. The shipped compact index is explicitly labelled an identity/routing projection, not a replacement for the full research specification.
