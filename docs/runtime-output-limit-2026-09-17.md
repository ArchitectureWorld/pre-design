# 第八轮：只修运行阻断

本轮按用户要求只处理运行错误。数值、事实语义、引用支持程度及内容精修留待下一阶段，不新增相关门槛，不重开来源对象。

## 已确认的故障与边界

- 原会话 07.02 与 07.05 在 `max-tokens` 终止，未返回完整结构化结果；进度 42/57、Revision 88。
- 宿主、认证 RPC、模型连接正常；这两次失败没有 HTTP/工具/超时错误。
- Gemini 路由模型条目没有配置输出额度。安装的 pi-ai 适配器默认 32,768 tokens，SDK 无网络复现向代理发送 `max_completion_tokens:32768`。请求头缺少此值并不表示没有输出上限。
- 本地代理模型目录只返回身份字段，不能据此证明更高额度或远端如何映射该参数。保留模型、思考设置与输出上限。
- 07.05 成功响应 arguments 原本已是紧凑 JSON（40,947 字符），输出去缩进本身没有收益。

## 最小代码修订

`src/runtime/subagent-workflow-analyzer.ts`：对 Schema、示例、Research、纠正候选和审查反馈统一无损紧凑序列化；要求说明与 rationale 简洁，保持全部字段、完成条件、证据与限制。保留 `max-tokens` 失败判定和现有候选校验。

原失败输入离线等价转换：07.02 从 199,122 降至 176,907 字符；07.05 纠正轮从 171,489 降至 136,944 字符（未计新增提示措辞）。所有 JSON 段往返一致。这不是实际 token 降幅或修复验收。

回归覆盖上游完整性、Schema/示例/纠正上下文完整性、拒绝截断、既有有界纠正、超时与模型路由。相关 17 项通过，类型检查通过。完整套件、部署及原会话运行结果须以本轮后续验收记录为准。

只读监督证据：`work/cycle8-adapter-audit.json`、`work/cycle8-review.json`。此次诊断未调用额外模型生成接口；恢复运行继续使用已有 120 次授权额度，不重置历史记录。

## 部署及实际验证

147 个测试文件、961 项测试全部通过。16:28 部署包 `ad159d2a8fd54eca7b98dc5f05f974a2772dd776765d20306c5ebe90acb6c8c4`；宿主 63644，配置/凭据/业务存储/ReportStudio 保护哈希全不变。回执见 `work/supervision-cycle8-runtime-deployment-receipt.json`。

原会话恢复后，07.05 和 07.02 各经过一次已有的来源结构校验纠正，四个真实子会话均完整返回且没有传输/超时/截断错误，分别提交 Revision 89、90。随后 07.04、07.03 也已提交。16:38 快照为 46/57、Revision 92、零阻断、07.06 运行，额度 74/120，派发已恢复。报告尚未生成，08.01 仍需实际观察；这不是全流程完成声明。

补充参数证据：官方 Antigravity-Manager v4.7.2 源码只接收 `max_tokens`，而 SDK 当前发送 `max_completion_tokens`。未收到字段时，代理还使用动态/静态模型规格及思考配置，故不能把本地 32,768 当作当前 Gemini 实际上限。已验证候选 `max_tokens:65536` 的本地 SDK 序列化，但未修改生产设置，候选不是待部署项。健康运行时不因这个诊断额外中断或盲目提高限额。

## 第九轮更新：参数修正已正式应用

08.01 纠正任务在17:00再次max-tokens后，17:07对当前Gemini模型精确应用 `maxTokens:65536` 与 `compat.maxTokensField:max_tokens`。上文“未应用”为第八轮历史状态。本次保留路由/思考等其他配置，备份与保护哈希见 `work/cycle9-model-cap-receipt.json`；宿主33136。SDK无网络断言与真实请求头均确认新参数。

原会话三次子调用均完整返回，无传输/工具/超时/截断；08.01通过两次既有结构纠正后于17:13提交Revision96。17:16快照50/57、0阻断，08.02/08.04并发，额度89/120。实际证据 `work/cycle9-live-audit.json`。本轮是经证实的集成配置修正，生产源码包和其961项通过测试沿用第八轮；报告生成仍待修复和验收。
