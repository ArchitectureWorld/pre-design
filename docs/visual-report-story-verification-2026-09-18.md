# 通用图文汇报管线验收

## 最新交付状态（2026-09-19 03:00）

本轮通用代码已部署，正式 HTML 已完成验收并保存至 `D:\shaotanhe\成果汇报\2026-09-18-r103-v3`，包 ID 为 `conditional-5e2d72b8-ea3c-42cf-a9f8-aa54c3fef102`。155 页，4 案例各 4 页，纯文字及表格页 0，143 阶段全有图，43 图阵贴边；291 图片引用加载通过，55 个交付图片文件 hash 与原包一致。逐页几何/翻页与10个代表页目视验收通过。通用竖图裁剪修复、完整验证和最终边界见 [竖图排版与实际交付验收](portrait-photo-layout-verification-2026-09-19.md)。下方带时间的运行记录为历史过程，不再是当前阻塞。

当前版本 `planning-manuscript-2026-09-19.1` / `regular-2026-09-19.1`，部署包 SHA-256 `eedd420c524c6a4f6ea1db7c70a5f73b8567af927a0599baaf37ff84e71345d8`。最新全量 175 文件1374测试、排版专项5文件57测试、类型检查通过。正式重新排版期间没有新模型任务，r103、57工作流与原稿保持不变。

## 范围

开发库 `E:\前期策划开发`，既有 `feat/pre-v2.0.1`，HEAD `6d77306a6229c44419cf3bcb5d8a7a976e24711b`。本轮按用户已确认方案实施；保留既有修改，不提交推送。少潭河 r103 是验收输入，程序不存在针对其项目 ID、目录或页 ID 的版式分支。

- 案例管线支持每案 3–4 个独立分析页，分别关联已核验真实照片及来源。当前四案例各四页、共十六张不同来源照片。
- 写作、统一编辑、呈现投影共用图文规则。完整文稿和证据保留在独立资料依据；精确重复句只展示一次。数字、小数、否定、前提、容量和客群信息不会被模糊相似度删除。
- 正文与表格按实际文字高度共排；长内容带相关图片续排。模板选择依据可用空间，不因固定字数估算产生稀疏尾页。
- 场景需求从页面及图解节点提取，按语义关联已验收图片。没有可信同主题匹配时单独生成，不挪用其他主题配图；真实案例、真实现状照片不能由 AI 替代。
- 流程节点逐一绑定照片，使用横向或纵向阵列，至少一边抵达画布边缘。正交连线避开图片并分离平行边，支持回边、自环和标签避让；空间不足明确失败。
- 导出以实际物理页检查纯文字及纯表格占比 ≤15%。小装饰图和仅文字框图不算实质配图；阶段照片缺失或图阵没有贴边同样阻止交付。
- 默认只导出 HTML；缓存、清单、状态回放与前端链接均以实际格式为准。继续 16:9 单页浏览。

案例选择要求每案至少两项有来源的共同点、每次三至五例。当前受信案例资料库覆盖茶旅、滨水、慢行和旧建筑活化，没有通用在线案例检索；其他主题需要扩充核验资料，不能硬套已有案例。这是资料覆盖边界，不是少潭河专用判断。

## 代码验证

- 最新 `pnpm test`：175 文件、1374 项通过（包含构建、契约和安装包测试），日志 `work/visual-story-full.log`，2026-09-19 02:48 完成。
- 最新 `pnpm typecheck`：通过；排版专项5文件57项通过。此前配图、恢复及版式验证记录保留于原归档。
- 跨主题测试包含工业遗产、滨海环境以及不相关主题拒绝；正文测试包含雨天与正常天气容量差异、小数、否定及条件保留。
- 照片图解路由专项 13 项通过；实际稿件 25 张图解、143 节点和 112 条边全部可路由。
- `work/visual-story-geometry-fixture` 仅用于补图前几何测试。其占位图不是真实补图成果，禁止渲染为交付，不作实际配图验收证据。

## 部署与实际验收

首次候选 SHA-256：`60741f72212a2be8d7fe00026e10b50a7072395ee198a489f6a9f6041a84d51e`，composition `planning-manuscript-2026-09-18.6`。

部署前项目 r103，57 项专业工作流已完成，无活动子任务。项目范围内执行记录 267 条，其中文字 237、生图 30；与历史所有项目合计 273 条的统计口径不同。原始文稿 SHA-256 仍为 `8dab750be4caaad992bdd45bd5eaae06ca7e2720de237b59dbeffee24ed7750c`。

已备份到 C:\Users\2899\.dsh\backups\visual-story-deploy-20260918-152517 并部署原生 DSH；182 项代码 hash 一致，7 项保护数据未改变。服务已重新监听 127.0.0.1:3080。原生 /preplan-export 于 2026-09-18 15:28 启动真实补图。真实补图、HTML 下载、浏览器和交付目录验收尚待完成；不能把代码测试或几何夹具称为正式交付。

### 真实补图首次失败与恢复修复

首个生图子会话发生上游 `TRANSPORT / Stream ended without finish_reason`，600 秒后结束但未产生图片。网关原请求超时为 120 秒；在独立备份后仅调整为 600 秒并重启网关，其余设置逐项确认未改。该调整是否解决真实生图仍需新的成功图像验证。

发现 pre-design 只查询内存会话，已卸载子会话的持久化终结记录未被识别，导致任务停在 `recovery_required`。通用修复通过 DSH 公开 `sessionPersistence` 只读句柄读取事件，并在同一快照内检查图片与终结事件；始终关闭只读句柄。已终结无图则收尾旧 execution 并允许受控重试；晚到图片恢复原 execution；日志缺失、读取失败及未终结继续阻止新的模型调用。

后续候选 composition 为 `planning-manuscript-2026-09-18.7`，包含上述恢复修复、密集图阵最小可见照片空间及按 SHA 去重导出的图片文件。已通过本页最新全量验证，真实新版 HTML 尚未交付。

### 17:35 部署与晚到图自动恢复

当前宿主包 SHA-256 为 `b36e34a37eacd483a684adbee06088b6f3cbdefd15538580799bc30a8dedf68d`，composition 仍为 `.7`（本补丁不改文案及排版缓存），备份 `C:\Users\2899\.dsh\backups\visual-story-deploy-20260918-173502`；182 代码文件核验一致、7项保护数据未变。

真实运行中第三张图在超时状态写入后约2毫秒才进入最终消息，但1024×1024 JPEG完整。已通过原生命令恢复原图及原execution，没有重复生成。为避免此类时序再次要求人工续跑，自动场景流程新增一次明确 `recoveryOnly` 回收，使用父任务信号和15秒期限；仅允许恢复已存在的原图、原attempt、原execution。恢复前后重核来源版本，取消/来源变化阻止采用。无图、未知、读取失败及恢复超时继续失败，不能派发新任务。新增14项专项回归均包含于本页1301项全量结果。

Flash 模型后续因上游 `HTTP429 / QUOTA_EXHAUSTED` 终结，服务声明北京时间20:28:11恢复；它不是用户授予的任务额度。实际共成功采用5张新增图，33场景顺序处理到第10项，当前稿件仍有配图缺口。

为检查同一Gemini图像系列的可用通道，17:39在备份后仅向DSH既有antigravity模型目录加入网关当时提供的 `gemini-3-pro-image-16x9`；凭证及无关设置未改变。17:41通过原生全局子Agent配置CAS（revision8→9）仅切换image，text/web保持 `workdubbyAI / global:deepseek-v4.1-flash`。正在原生 `/preplan-export` 验证Pro模型，未将模型列表可见等同于真实可用。

证据：`work/visual-story-deployment-receipt.json`、`work/visual-story-image-retry-observation.json`、`work/visual-story-third-image-failure.json`、`work/visual-story-dsh-image-model-change.json`、`work/visual-story-image-route-change.json`。完整HTML和浏览器验收仍待完成。

### 当前外部阻塞（17:44）

Pro真实子会话也失败：`HTTP503 / SERVER`，明确返回 `No accounts available with quota for model: gemini-3-pro-image`，无图片、无恢复时刻。已通过原生CAS将image恢复为此前实际成功的Flash配置（revision10），text/web保持不变；没有继续尝试同一模型的其他别名。相关证据为 `work/visual-story-pro-image-observation.json`、`work/visual-story-image-route-restored.json`。

当前诊断使用真实已采用图片：33场景已覆盖11项、仍缺22项；143个临时物理页中19页无照片（13.29%），但仍缺66个流程节点图片。因此阶段覆盖门禁仍然阻止新版HTML交付；本诊断不是正式成品验收，不使用占位图补齐。原稿哈希未变，新增8项image执行，其中5项成功、3项失败；新增text为0、无活动请求。证据 `work/visual-story-upstream-block-receipt.json`。

既有30分钟监督在不早于北京时间20:28:11时再验证Flash额度。服务提示的恢复时刻不是成功保证；若再次限额，遵循新的恢复信息退避，不重复空转。恢复后继续原生导出，完整保存旧执行记录，剩余验收脚本和交付目录仍按本轮计划执行。

### 20:20 Hy4 验证与通用执行修复

用户指定的 `workdubbyAI / global:hy4-preview` 实测未产出图片。首轮脚本提示词缺失已明确判为无效；完整提示词试验仍仅返回文字和工具委派，下级转调Gemini也没有图片，不能计为Hy4生图成功。image配置已恢复Flash（revision12），text/web保持原DeepSeek。详见 `docs/hy4-image-model-verification-2026-09-18.md`。

本轮已部署两项通用修复：图像等待器每秒复查原会话、避免漏接事件后空等；视觉子会话工具执行前统一拦截，堵住自身注册的subagent工具绕过继承白名单的问题，禁止继续委派或换模。最终包SHA-256为 `f0acfdee247030e3fd42d6dffe01006b99887488325df36f1643ac5171ab6889`，174文件1311全量测试、5文件42专项及类型检查通过，182项安装代码匹配。当前composition与layout版本未改变。

两个自建失败试验已在备份及离线精确核对后移出必需出图项，历史完整保留。相对原基线新增10个image类执行、0个text类执行；下级Hy4工具链中的直接API尝试另记，不包含在该执行计数内。当前无活动execution，r103、57工作流、原稿SHA-256不变；仍缺22项场景图，新版HTML未交付。

上游最新记录的Flash恢复时刻为北京时间20:35:25，取代上文旧的20:28:11。监督任务不得提前发起生图或轮询其他Gemini别名，也不得自动重试Hy4作为原生生图模型。

### 22:26 失败流中的图片回收

20:57的真实导出新增2项image执行：第一项采用了超时前最终消息中的图像；第二项在失败attempt中已收到完整JPEG，但旧收集器忽略，造成丢图并阻断导出。当前覆盖12/33、仍缺21项；文字任务与原稿未变。

该缺陷已从通用收集/执行层修复：严格验证完整像素、CRC/文件结束、限制分片与解码规模，忽略无效最终图并回收原attempt；活动重试由原父Agent停止，观察终结并重核turn后再采用。未知、取消或来源变化不误报成功、不启动替代请求。详见 `docs/stream-image-recovery-2026-09-18.md`。

部署包SHA256 `250fe7517d3d96d67a5f4cf8dafa08dd4d0b142907f80c608d54424ab1714693`，183项代码、7项保护文件核验通过。认证访问HTTP 200、部署前无活动execution；已启动一次原生导出验证回收并继续补图。正式HTML验收尚待完成。

22:28验收：旧失败图已通过原生流程采用，原execution、child与attempts=1保持不变，原图哈希一致；覆盖13/33，仍缺20项。新请求只有reasoning而无图片，已正确判为图像任务失败，未报告服务限额或恢复时间。没有活动请求、新增文字任务为0、原稿及57项工作流未变。真实HTML尚未交付，详见 `work/stream-image-recovery-final-receipt.json`。

### 23:13 页面级配图检查与在线恢复验收

继续原生导出的RPC为 `c94acbf3-7b79-4e93-ae0b-7179c882db60`，22:47启动，截至23:12已新增采用五张真实图片，场景覆盖18/33，第六项仍在执行；完整HTML未生成。旧命令回执在本轮结算前仍可能指向22:28失败请求，监督必须核对RPC，而不能将旧回执当成本轮结果。

第三张在failed `assistant/attempt` seq13中收到完整JPEG，随后出现 `TRANSPORT / Stream ended without finish_reason`；241毫秒后原child由父Agent停止，未触发新的 `llm/retry-started`，原图采用成功。原attempt与正式asset `4bc553d1-cebc-4233-bd56-7fb34cdc674d` 的SHA256均为 `26a6d38fac4c162f70288f6e8e446092f512b95310a38893cdb308c9904405fd`，999349字节、1024×1024严格像素解码通过。这次实测完成了“原图已到但流失败，停止后续重试再采用”的在线分支验收。证据 `work/stream-image-probe/resume-2246-third-result.json`。

同时复现并修复通用门禁缺陷：同章节不同页面复用nodeId时，旧检查可能把其他页面的阶段图片算入覆盖。现在图片必须属于对应源页及其物理续页，无法确定源页的图片不计入；新增四项回归先失败再通过。composition升为 `planning-manuscript-2026-09-18.8`，避免沿用旧检查版本的缓存，未改原稿或专业工作流。

最终源码通过本页最新验证；新候选SHA256 `bbd10567305d3370c02a838f08f7aad1ca7de78c418cdd8046c35ce679d4fdd6`，183项代码核验完备。当前仍运行22:26部署的 `.7` 包；必须等原生导出结算且无活动请求后才部署本候选。最新运行证据 `work/visual-story-runtime-resume-2308.json`：r103、57项工作流、原稿SHA及237条文字执行保持不变。

### 23:25 部署与23:30上游额度阻塞

第一轮续跑最终采用十张图；第十一项 `tea:water-management` 只返回文字而未出图，23:22:59明确结算失败、0活动，未报告上游额度信息。随后部署上述 `.8` 包，备份 `C:\Users\2899\.dsh\backups\visual-story-deploy-20260918-232449`；183项安装代码与7项保护文件均通过核验，认证访问HTTP200。浏览器仅记录重启窗口的连接重试提示，23:25:28之后没有新的警告或错误。

23:27:16从原任务续跑，RPC `8f5f9763-d020-4ec4-aa8a-b312b841471d`，`tea:water-management` 于23:29:47真实出图并采用；下一项 `water:arrival` 遇到明确HTTP429、QUOTA、RESOURCE_EXHAUSTED，无原图。23:30:28命令失败结算、0活动。上游声明恢复时间为 `2026-09-18T17:35:25Z`，即北京时间9月19日01:35:25，监督已改为该时刻之后再验证，不提前试跑或轮换别名。证据 `work/stream-image-probe/resume-2326-second-failure.json`。

本次“继续”共新增采用十一张图，治理层覆盖24/33；文字执行保持237条（相对原始基线新增0），图像执行56条。r103、57个工作流与原稿保持不变。当前在审查两个已采用图片在工作区素材装配后未计入的问题，因此23:32诊断预览为22/33，152物理页中9页纯文字/表格，29个流程节点仍无图。本诊断不是正式HTML成果，不能重复生成已采用图片来掩盖绑定问题。完整HTML和浏览器验收仍待完成。

该统计差异已查明并修正：`prepareClientVisuals` 在节点配图齐全时移除多余的整页图片展示绑定，原图不丢失。生产 `PageVisualFillService.planScenes` 已使用裁剪前的 `preparePresentationMaterials`，生成决策正确；只有诊断脚本误用了裁剪后的资产重算。已让 `work/preview-visual-story.ts` 与生产生成规划使用同一层输入，保留原本正确的页面排版裁剪，没有新增业务接口或模型调用。最新诊断 `work/visual-story-diagnostic-2340.log` 确认为24/33、仅缺9个滨水场景；纯文字/表格9/152（5.92%），流程节点缺图29项。尚未通过完整导出验收，不能把这些诊断页称为正式成果。
