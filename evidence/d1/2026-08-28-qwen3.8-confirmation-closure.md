# D1 Qwen3.8 确认闭环证据

日期：2026-08-28（Asia/Shanghai）

## 边界

- 使用 DSH 页面实际选择的 `Qwen3.8 27B`（用户指定 `qwen3.8:27b`）。
- 只读取已安装插件的 v0.6 合同与模型受控上下文。
- 未读取、记录或代填模型凭据；未读取或手改原始 Storage。
- 未删除旧 Session；另建独立验收项目避免污染修复前现场。

## 真实闭环

1. `/preplan-new` 创建并绑定项目 `preplan-18e99bc1-4b40-4088-9649-972ba6c020ae`。
2. Qwen 调用 `preplanning_get_context`，读取 revision 0 与允许动作。
3. Qwen 把完整 ProposalEnvelope 作为嵌套 JSON 对象提交；合同校验一次通过。
4. 返回 `proposalId=prop-PS01-preplan-18e99bc1-01-01`、`expectedRevision=0`、`status=pending_review`；页面仍为 revision 0、待确认 1、开放问题 1，模型未越权确认。
5. 用户命令 `/preplan-confirm prop-PS01-preplan-18e99bc1-01-01` 返回 revision 1、待确认 0、开放问题 0。
6. Qwen 只调用一次 `preplanning_get_context` 复核，得到：
   - PS01 `revision=1`、`status=confirmed`、`data.status=confirmed`；
   - `approval.status=approved`，批准角色为 `decision_owner`，批准时间与对象更新时间一致；
   - `confirmedProposals` 含该 proposalId，`committedRevision=1`；
   - `auditEvents` 含 `prop-PS01-preplan-18e99bc1-01-01:confirmed`，事件类型为 `proposal.confirmed`、revision 1、对象为 PS01；
   - `pendingProposals=[]`、`openQuestions=[]`。
7. 停止并重启 DSH，重新打开同一 Session；`/preplan-status` 仍返回 revision 1、待确认 0、开放问题 0。
8. 重新打包最终 README 后，再次用官方 CLI remove/add 安装最终 tarball；第二次重启后的 `/preplan-status` 结果不变。

## 自动门禁

- Vitest：13 个文件，28 项测试，全部通过。
- TypeScript：类型检查通过。
- Build：通过。
- 构建产物：2/2 通过。
- v0.6 合同：949/949 通过。

最终 tarball：1,560,119 bytes；SHA-256 `F9A13DD002F6CD0E5E71CF0005D473BE01BDE152CEC74FE886E4A37A33D713CC`。
