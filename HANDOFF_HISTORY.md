# 前期策划 DSH 插件：甲方产品级成果改版总交接

> 文档性质：唯一权威 HANDOFF（项目状态 + 成果标准 + 视觉规范 + 工程边界 + 验收合同）
> 初始交接：2026-08-28（Asia/Shanghai）
> 汇总更新：2026-09-02（Asia/Shanghai）
> 适用范围：DSH 前期策划插件、自动化报告生成、甲方版 HTML/PPTX/PDF、概念视觉及产品级质量验收
> 当前基线：包版本仍为 v0.7.0；有效开发线已归并到唯一 `main`，功能与测试收束点为 `730b1e1`，本地/远端均只保留 `main`，只剩一个主工作树且 `git status` 干净。最终 HANDOFF 作为独立文档提交追加，当前远端 SHA 应以 `git rev-parse main` 与 `git rev-parse origin/main` 的相等结果为准。2026-09-02 合并后新鲜验证为 56/56 个测试文件、466/466 项测试通过，`pnpm typecheck` 与构建产物测试 2/2 通过；详细 Git 收束与恢复信息见 0.13。R15 仅保留为历史 HTML-first 过程候选，正式项目仍缺真实总平/红线输入、正确的 R16 全页视觉 QA、真实 DSH 生图—采纳—发布—下载—重启恢复 E2E 与甲方验收，因此客户成果状态仍为 `PARTIAL / publishable:false`，不得因工程测试通过而宣称正式交付完成
> 最高原则：最终交付必须是甲方可直接汇报、传阅和决策的正式产品；内部运行证据不能冒充客户成果。

---

## 0. 接棒者先读：这次到底要做什么

一句话任务：

> 把现有“像内部会议纪要、流程台账和自动化测试报告”的前期策划成果，重构为具有苹果产品发布质感的建筑策划产品册，同时保留 DSH 底层治理、版本追踪、自动化生成、证据和审计能力。

这里的“苹果产品美学”不是复制 Apple 官网、字体、品牌图形或产品页面，而是借鉴其产品表达原则：

- 单一焦点：一屏只讲一个核心价值。
- 产品优先：先让客户看见最终价值和体验，再解释技术与过程。
- 巨幅视觉：关键产品、空间和场景具有足够尺度与沉浸感。
- 强烈层级：大文字、短句、主图、数据和留白形成明确阅读顺序。
- 极致克制：少颜色、少装饰、少噪声，但不是少内容。
- 材质真实：建筑、场地、人物、光线、图表和细节都可信。
- 连续叙事：像一次产品发布，而不是若干独立模板页的拼接。
- 精密完成：所有对齐、裁切、字号、动效、跨媒介输出都经过验证。

接棒后不得重新讨论“要不要产品手册化”：用户已经明确同意。下一步应围绕具体项目受众、篇幅、媒介和实现方案进入规格与实施，而不是退回会议纪要或传统规划文本路线。

### 0.1 2026-08-29 开发更新：工程 Golden r3

- 当前 worktree：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\work\dsh-preplanning-agent\.worktrees\r070`
- 当前分支：`feat/client-report-v0.8.0`
- v0.8 客户投影、同源 HTML/PPTX/PDF、内容政策、页面计划、原子发布与工程 Golden 已完成第一轮实现。
- 本轮新增 `visual-evidence` 页面语义：地图、空间分析图和图表不再作为普通证据配图，而是在 HTML、PPTX、PDF 中成为独立主视觉页。
- 工程 Golden r3：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.8.0\engineering-golden-r3`
- 新鲜工程回归：44 个测试文件、111 项测试、0 失败；类型检查与构建通过。
- 三格式检查：身份一致、禁词 0、缺失采用资产 0；HTML/PPTX/PDF 各含 3 页专业主视觉证据。
- 全页 QA：Microsoft PowerPoint 导出 36/36 页；PDF 渲染 48/48 页；无明显黑页、空白页、拉伸、越界或裁切。
- 当前评分：内容产品性 78/100，美学 80/100；均未达到 85/88 门槛。
- 未通过原因：工程夹具仍是泛化滨水项目，`map/diagram/chart` 的底层图片仍为概念图，不是真实地图、总平面或数据图表；核心产品只有一个，真实商业与空间机制不足。
- 下一步固定为：制作“明塘＋洋澜湖”真实 showcase，建立项目专属内容、真实专业图件和视觉母题；之后再做真实 DSH `模式选择 → 任务执行 → 生图 → 采纳 → 发布 → 下载 → 重启恢复` E2E。
- 包版本继续保持 `0.7.0`，不得宣称 v0.8 完成。

### 0.2 2026-08-30 历史验收记录（已由 0.3 撤销）：真实 DSH r21

#### 当时结论（仅作追溯，不再有效）

- 历史客户成果状态：`PASS`；该结论已撤销，当前有效状态见 0.3。
- 历史内容产品性评分：`94/100`；不能作为当前合格证明。
- 历史苹果式美学评分：`92/100`；不能作为当前合格证明。
- 当时记录的一票否决项：`0`；复审后已确认存在图件完整性一票否决项。
- 工程状态：类型检查、构建、44 个测试文件、139/139 测试通过。
- 真实宿主状态：DSH Web Profile 已安装 r21 候选包，PID `44896`，`http://127.0.0.1:3080/` 返回 HTTP 200。
- 发布边界：本轮未升版本、未提交、未推送、未合并、未创建 Release；worktree 中全部未提交改动均保留。

#### r18—r21 闭环修复

1. 裸数值且没有单位的冻结字段不再进入客户事实，删除“财务净现值：1280”这类不完整表达。
2. 决策收束页不再重复同一投资证据；三条依据分别对应项目定位、首期分期和运营平衡。
3. 无内容块的决策收束页优先显示“项目定位、首期实施边界、建设与运营协同机制”三项行动，不再被“总投资约 1.05 亿元”覆盖。
4. 所有页面的证据 ID 均去重，收束页增加防御性去重。
5. 人工复核 r20 时发现 PDF 第 39—50 页连续使用同一白底证据模板，触发“一票否决”。r21 将证据附录改为浅色、项目绿、深色三种视觉处理循环，同时改变文本宽度与落位，消除连续模板机械重复。

#### TDD 与完整工程证据

- r20 第一轮 RED：3 项内容回归按预期失败；GREEN：2 个测试文件 16/16 通过。
- r20 第二轮 RED：决策收束页缺少三项行动，按预期失败；GREEN：3 个测试文件 21/21 通过。
- r21 RED：新增“连续三个附录页必须具有不同视觉处理”测试，按预期得到 `expected 1 to be 3`；当时 6 项中 1 项失败。
- r21 GREEN：`tests/report-pdf.spec.ts` 6/6 通过。
- 新鲜类型检查：`pnpm typecheck` 通过。
- 新鲜完整回归：`pnpm test` 完成构建，44 个测试文件、139/139 通过、0 失败。

#### r21 候选包、备份与安装

- 候选包：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.8.0\dsh-candidate-r21\architectureworld-dsh-preplanning-agent-0.7.0.tgz`
- 包大小：`1,830,567 bytes`
- 包 SHA-256：`86B2D237680E3D2476DBBD8821956454BBA7DCFE6BF093C2DBB2D31B242A84F4`
- 安装前备份：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.8.0\dsh-candidate-r21\dsh-web-profile-before-r21-20260830-034528`
- 备份与源 Web Profile：`840 / 840` 个文件。
- 已安装 `lib/index.js` 与本地构建 SHA-256 一致：`86E0126EAE83D3D2F2E51B1DA34D9513650794AE6460117A19977655AF34355C`。
- DSH 标准错误日志：0 bytes。

#### 真实 DSH r21 三格式成果

真实会话中输入 `/preplan-export`，由 DSH 原子发布；不是用本地构建脚本替代宿主验收。

- packageId：`report-74de6498-232e-4ccc-a615-ffcaff28d406`
- 独立验收目录：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.8.0\real-dsh-e2e-r21-74de6498`
- HTML：35,723 bytes；SHA-256 `1ADA4C4DD1F5D37DA100037BED3868327F7988E95112EF600301FC9A8602E77D`
- PPTX：4,728,110 bytes；SHA-256 `A010C23C191EC85F3DD25876B0FD54CE7F240ACBE1DE523F3AAEB6954CFAEA49`
- PDF：4,737,542 bytes；SHA-256 `D77220E782863104F4A9E2413C58056FFAEB744A7C203B71F8DE4E08EB8DF289`
- 三格式均 HTTP 200，下载字节数与 SHA-256 均和 artifact manifest 完全一致。

#### HTML、PPTX、PDF 视觉与结构 QA

- HTML：38 页；5 张图片全部加载；缺图 0；11 条内链无断链；横向溢出 0；控制台错误 0；客户层禁词 0。
- HTML 第 28 页：标题为“洋澜湖西岸滨水剖面”，内容明确为“3 米亲水木栈道＋4 米骑行绿道＋草坡驳岸”，并绑定对应概念场景图。
- PPTX：Microsoft PowerPoint 真实渲染 38/38 页；对象越界 0。
- PowerPoint 文本边界检查提示 3 项，位于第 21—23 页；逐页实图核对后确认文字完整可见，没有裁切、遮挡或出界。
- PDF：PyMuPDF 渲染 51/51 页；第 39—50 页已形成浅色、项目绿、深色节奏，不再连续三页机械重复。
- PDF 第 51 页：“财务净现值”与裸数值 `1280` 均为 0 次；“1.05 亿元”在本页为 0 次；三项行动及三类不同依据各出现 1 次。
- 关键页测试图：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.8.0\real-dsh-e2e-r21-74de6498\qa\关键页测试图-r21.png`
- 全册联络表：同目录 `qa\pptx-contact-sheet.png` 与 `qa\pdf-contact-sheet.png`。

#### 双量表评分说明

内容产品性 `94/100`：甲方成品定位 15/15，叙事结构 19/20，判断与证据 18/20，产品与场景 18/20，运营投资与实施 14/15，交付完整性 10/10。

苹果式美学 `92/100`：视觉概念与项目专属性 14/15，图像选择与空间表现 17/20，网格构图与留白 19/20，字体尺度与信息层级 15/15，色彩材质与视觉识别 10/10，图表地图与专业表达 8/10，全册节奏 5/5，输出细节与视觉 QA 4/5。

扣分边界已经保留：现有场景图为 1024×1024 图像，不等于完整的专业分析成果；专业地图、可编辑矢量总平面和专项图表仍可在下一阶段继续深化，因此不打虚高满分。

### 0.3 2026-08-30 图件完整性复审与强制标准更新

0.2 节保留为历史验收记录，但不再代表当前有效结论。复审 r21 得到：HTML 共 38 页，仅 5 页包含图像，33 页为纯文字页，视觉图件覆盖率为 13.2%；`visual-evidence`、正式区位图、场地边界图、数据图表和场地分析图均为 0。该成果不能满足建筑前期策划产品册的图件完整性要求。

当前有效结论：r21 客户成果状态改为 `FAIL / 待重做图件体系`；历史工程测试通过只能证明导出链路可运行，不能证明甲方成果通过视觉与专业验收。

本轮新增三项不可降低的硬合同：

1. 32—48 页主册至少包含 6 张有效数据分析图表，并覆盖至少 4 类业务主题。
2. 除封面、三页开场、章节幕和最终决策大字页外，至少 50% 的内容页由图像、地图、图表或分析图担任第一视觉主角；纯文字内容页不得连续超过 2 页。
3. 场地分析必须形成完整序列，至少包含城市区位、项目边界、现状用地/建筑、对外交通与可达性、内部流线、资源与限制 6 项强制图件；景观生态、机会结构和分期落位按项目条件补充。

图像制作方式不作为客户层分类或评分依据。所有图像统一按清晰度、构图、裁切、项目适配度和信息有效性验收；是否注明“概念示意”只取决于成果阶段与表达内容，不取决于制作工具。

### 0.4 2026-08-30 工程实现更新：图件完整性门禁已落地

当前 worktree 与边界：

- worktree：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\work\dsh-preplanning-agent\.worktrees\r070`
- 分支：`feat/client-report-v0.8.0`
- HEAD：`4847bdd`
- 包版本仍为 `0.7.0`；本轮没有升版本、提交、推送、合并或创建 Release。

本轮已把 0.3 的标准从文档要求变成报告生成前的强制工程合同：

1. `architectural-v1` 视觉合同要求至少 6 张数据分析图表，并覆盖至少 4 类业务主题；不足时分别返回 `DATA_CHART_COUNT_LOW` 与 `DATA_CHART_TOPIC_COVERAGE_LOW`。
2. 城市区位、项目边界、现状用地/建筑、交通可达性、内部流线、资源与限制 6 类场地分析缺一不可；不完整时返回 `SITE_ANALYSIS_SERIES_INCOMPLETE`。
3. 内容页视觉覆盖率不得低于 50%，纯文字内容页不得连续超过 2 页；不满足时分别返回 `VISUAL_PAGE_COVERAGE_LOW` 与 `TEXT_ONLY_RUN_TOO_LONG`。
4. HTML、PPTX、打印 HTML 三种页面计划都必须先通过同一套客户页面合同；不合格报告会在渲染器运行前失败，不能再以“能导出文件”绕过质量门禁。
5. 图件语义通过 `analysisKind` 与 `chartTopic` 进入客户投影和 Golden 配置；客户成果不显示图像制作工具或生成方式，也不再要求单独的图像制作方式说明。

新鲜工程验证（2026-08-30）：

- `pnpm typecheck`：退出码 0。
- `pnpm test`：完成完整构建，44 个测试文件、142/142 项测试通过、0 失败。
- 正向 Golden 可在 HTML、PPTX、PDF 中发布满足声明合同的图件配置；缺图、缺场地分析、图表主题不足、视觉覆盖率不足和纯文字连续页超限均有自动回归保护。

必须保留的真实性边界：

- 当前完成的是“标准合同＋工程门禁”，不是新版真实甲方产品册。
- 工程 Golden 中既有图片通过配置声明了 `analysisKind` 与 `chartTopic`，用于证明完整配置能够通过流程；这些底层图片本身不等于真实区位图、场地边界图、现状分析图、交通/流线图或数据驱动图表。
- 当前检查能够验证资产数量、声明类型、主题覆盖和页面分布，尚不能自动判断图像像素内容是否真的与其专业角色一致；真实项目数据源、制图过程和人工视觉 QA 仍不可省略。
- 因此 r21 仍保持 `FAIL / 待重做图件体系`。只有完成真实 6 类场地分析、至少 6 张数据图表、重新生成 DSH 三格式成果并逐页验收后，才允许重新评分和改变客户成果状态。

### 0.5 2026-08-30 工程实现更新：正式场地边界必须由人提供并确认

自然语言只能描述项目名称或大致区位，不能精确表达法定红线、项目边界或研究范围。本轮已经把这一真实性边界落实为工程门禁：

1. 遇到具体场地位置、项目红线或正式场地分析，Agent 必须主动向人索取并确认至少一种资料：已采用的总平图、已采用的红线图、带 CRS 的闭合红线坐标，或兼容 GeoJSON 的闭合几何；不得从地名、自然语言描述、普通地图截图、图上画线或模型推断法定边界。
2. 文件类输入必须属于当前项目、已被采用且具有真实文件 SHA；坐标类输入必须为有限数值、闭合、至少三个有效点且面积非零。派生边界图保留自己的文件 SHA，同时通过独立 geometry lineage 绑定已确认坐标，二者不得混为同一个摘要。
3. 边界登记与确认是独立的人类命令/事件。缺少完整确认人、确认时间、确认 revision，或只有晚于目标 revision 的记录时，正式发布必须在 profile、分页和渲染之前 fail-closed，并提示“请提供总平图、红线图或闭合红线坐标并确认”。
4. 测试、演示和概念研究可以使用自定义范围，但必须在图面和三种媒介中完整显示：`研究范围（待核） · 非法定红线 · 非测绘成果`。该范围不得被称为正式红线，也不得作为真实项目正式发布的依据。
5. 地图圈地属于后续交互能力，本轮没有开发。未来可用地图 UI 收集人选范围并导出坐标/GeoJSON；在人完成确认且系统保存 CRS、闭合几何和来源之前，它仍是待核研究范围，不能自动升级为法定红线。

新鲜验证（2026-08-30）：正式文件边界与闭合坐标派生图制品链 `2/2` 通过；9 个场地边界定向测试文件 `98/98` 通过；制品 validator `2/2` 通过；`pnpm typecheck` 与 `git diff --check` 退出码均为 0。独立 Fix 2 复审结论为 `Approved`，新 Critical/Important 为 `0/0`。

必须保留的真实性边界：当前证明的是本地工程门禁和制品身份闭环，不等于已经取得真实项目总平/红线资料，不等于真实 DSH 用户身份验收，也不等于新版 r5 甲方成果已经完成。

#### 修改后的正式甲方汇报文本

> 明塘＋洋澜湖不是一次单点改造，而是一套面向城市更新、公共生活与复合运营的完整城市产品。
>
> 项目以文化体验串联公共空间与日常生活，把明塘历史街区与洋澜湖滨水资源转化为可使用、可运营、可传播的城市目的地。
>
> 推荐路径兼顾文化原真性与现代滨水活力，投资强度可控，能够同步提升公共价值、城市体验与长期经营能力。
>
> 三类核心产品共同构成日常、周末与节庆场景：明塘＋洋澜湖文化街区，以在地文化内容和日常消费重新激活存量空间；滨水生活客厅，把连续开放空间转化为全天候城市生活目的地；落日剧场与城市活动场，以高识别度活动建立持续传播能力。
>
> 明塘核心步行街采用 8 米人行街巷＋3 米外摆区，并控制在 18 米建筑限高与 1.2 容积率以内；洋澜湖西岸采用 3 米亲水木栈道＋4 米骑行绿道＋草坡驳岸，滨水构筑物限高 12 米。
>
> 基准经营情景下，运营第 3 年进入稳定期，年综合收入约 1,670 万元，IRR 为 6.8%，DSCR 为 1.35。
>
> 项目全口径工程总建设投资约 1.05 亿元，建议按首期示范、产品成型、长期提升三阶段推进。
>
> 现阶段需要锁定三项决策：项目定位、首期实施边界、建设与运营协同机制。

### 0.6 2026-08-30 工程测试候选更新：明塘＋洋澜湖 r5 三格式实物

本轮已生成独立的明塘＋洋澜湖 r5 工程测试候选，没有覆盖 v0.7.0 Golden，也没有升版、提交、推送、合并或发布：

- 目录：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.8.0\showcase-mingtang-yanglanhu-r5`
- HTML：48 页，56,946 bytes，SHA-256 `6401144758dc5db59f9fce9a091b5b6a9bf26c0e0632ae2a919741fd571ba3d7`。
- PPTX：48 页，23,077,427 bytes，SHA-256 `4c5b3aec202be7b9e94d84139cb73f72d3083e97c3ff6cc0013162e91aff1e22`。
- PDF：68 页，其中第 48 页明确主报告结束，第 49 页起为 20 页核验证据索引；26,190,640 bytes，SHA-256 `4cf76292884a6e4659059dad30f9f9e6f37c26d2b472a536167aa945dccd48d7`。
- 同一客户叙事模型包含 28 个采用资产、6 张强制场地分析图、9 张数据分析图表、5 类图表主题、32 个正文块和 3 个核心产品；HTML、PPTX、打印源各有 21 个专业主视觉页。
- 测试边界在三媒介中固定显示 `研究范围（待核） · 非法定红线 · 非测绘成果`，没有被升级为正式红线或测绘成果。

实际视觉闭环已发生：第一轮 PowerPoint 渲染发现第 7、9、16 页内嵌 SVG 在 Office 中变为空框，修复为已复核的同尺寸 PNG 后全页重导；独立复审又发现第 29 页标题被主图压住、PDF-only 证据索引缺少跨媒介边界。Fix 1 将第 29 页标题移入主图上方安全区，并在 HTML、PPTX、PDF 第 48 页统一加入 `证据附录（仅 PDF 供核验，不属于主报告）`。独立定向复审判定两项均 `ADDRESSED`，新 Critical/Important 为 `0/0`，Gate Verdict 为 `APPROVED`。

主控新鲜验证：5 个定向测试文件 `69/69` 通过；`pnpm typecheck`、artifact inspection、artifact hash/identity validator 与 `git diff --check` 退出码均为 0；PowerPoint/PDF 渲染目录分别为 `48/48` 与 `68/68`。三端 identity 一致，缺失采用资产 0，客户可见禁词 0。

真实性边界：该 r5 是使用自定义待核研究范围的工程测试候选，不等于已取得真实项目总平图、红线图或带 CRS 的闭合坐标，不等于真实 DSH 已发布该候选，也不等于甲方现场验收或整体 v0.8 完成。下一步仍必须执行真实 DSH `模式选择 → 任务执行 → 生图 → 采纳 → 发布 → 下载 → 重启恢复` E2E，再运行全量工程与最终全分支复审。

### 0.7 2026-08-30 真实 DSH r5 候选安装与正式边界门禁实测

本轮已把 r5 代码候选真正安装进 DSH Web Profile，并执行了一次新的宿主内边界门禁验收；这不是复用历史截图或只检查 HTTP 200：

- 安装包：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.8.0\dsh-candidate-r5-host-20260830-163218\architectureworld-dsh-preplanning-agent-0.7.0.tgz`，1,840,589 bytes，SHA-256 `3637678136b4eec6b16f677eafd82055a0ad0831da09f1d270bfb8caa9a49db8`。包版本仍为 `0.7.0`，本轮没有升版或发布。
- 安装前备份：同目录 `dsh-web-profile-before-install`；源与备份均为 840 个文件，逐文件 SHA-256 比对 `missing=0 / extra=0 / mismatch=0`。
- 已核对旧监听进程 PID `44896` 的完整命令行为 DSH `--profile web --no-open`，仅停止该进程；插件只通过官方 `dsh plugin --profile web remove/add` 替换，没有手改 Profile、Session、Storage、凭据或历史成果。
- 新受控实例 PID `51452`，URL `http://127.0.0.1:3080/`，启动日志目录为 `...\dsh-candidate-r5-host-20260830-163218\host-run-20260830-164028`；根页面 HTTP 200，stderr 为 0 bytes。
- 新会话的命令菜单真实出现 `preplan-boundary-asset`、`preplan-boundary-coordinates`、`preplan-boundary-confirm` 三个边界命令；新建测试项目 `边界研究范围门禁-r5宿主-20260830-1645`（`preplan-5c4c3153-d04e-443b-ae10-664ebd712396`）成功，并明确切换为人工确认模式。

为排除“项目尚未完成”造成的假阻断，本轮另行打开 DSH 持久化的完整项目 `明塘＋洋澜湖-v0.8候选验收-20260829`（`preplan-de6e6498-64ef-44ab-964e-f6fc09f8f9fd`）：Revision `57`、全流程 `57/57`、Gate `8/8 approved`、视觉已采用 `5`。在该状态下重新执行本轮 `/preplan-export`，宿主返回：

> `SITE_BOUNDARY_CONFIRMATION_REQUIRED: 请提供总平图、红线图或闭合红线坐标并确认。`

该结果证明正式发布没有把地名、旧效果图、普通地图或自定义研究范围误认成人工确认红线。失败后 `report-packages` 中 `.staging-*` 为 `0`，没有产生新的报告包；原有历史 HTML/PPTX/PDF 下载路由仍分别返回 HTTP 200，升级没有破坏既有成果读取。

本轮还在上述新测试项目中真实提交了 `/preplan-visual visual-r5-host-smoke 01 01-01 ...`。任务提示明确要求“16:9、简洁大气、不绘制红线、不暗示法定边界、作为普通图像使用”，宿主确实创建固定子会话 `preplanning_visual_task:preplan-5c4c3153-d04e-443b-ae10-664ebd712396:visual-r5-host-smoke:1`，Provider/Model 为 `antigravity / gemini-3.1-flash-image`，没有替换模型。该请求最终没有返回图像：轨迹记录为 `Failed`、`Stream ended without finish_reason`、`Tool calls=0`、`Retry scheduled 2 of 5`，总时长 `792.3 s`（TTFT `207.5 s`、generation `584.8 s`）。任务已超过生产代码声明的 600 秒上限后仍未自行收束，因此只停止了这个测试子会话；父会话随后明确返回 `视觉任务 'visual-r5-host-smoke' 生成失败`。项目状态为 `视觉 0/0/1`，候选 0、已采用 0、视觉阻断 1；对应项目视觉目录不存在，没有产生图片文件或伪造候选。本轮不得把“已提交/已重试”写成生图或采纳通过。

新鲜回归曾发现一处与边界逻辑无关的过期断言：统一增加“证据附录（仅 PDF 供核验，不属于主报告）”说明页后，通用工程 Golden 已从 PPTX/PDF `36/48` 变为 `37/49`，但 `tests/full-flow-golden.spec.ts` 仍保留旧页数。定向测试稳定复现后，仅把该断言更新为 `37/49`；随后 9 个边界与报告定向测试文件 `102/102` 通过，`pnpm typecheck` 与 `git diff --check` 退出码均为 0。

当前结论必须保持为 **PARTIAL**：r5 自定义待核研究范围的三格式工程候选已存在；真实 DSH 插件安装、项目创建、模式切换、持久化项目打开和正式边界 fail-closed 已验证；但本轮真实生图因上游流未给出 `finish_reason` 而失败，r5 仍未通过真实 DSH 生成、采纳、发布、下载与重启恢复。继续闭环除了恢复固定图像模型路由外，还必须在边界路径上二选一：由人提供真实总平图、红线图或带 CRS 的闭合坐标并确认；或另行批准开发一个只产出“非正式研究候选”的受控测试导入/发布能力。后一能力必须与 `/preplan-export` 正式发布路径隔离，并在三种媒介持续显示 `研究范围（待核） · 非法定红线 · 非测绘成果`，不得借测试模式伪造正式红线。

### 0.8 2026-08-30 视觉截止中断修复与 fresh 宿主复测

上一轮真实视觉任务暴露的根因已经定位并最小修复：父侧 `AbortSignal.timeout(600_000)` 原先只约束 `startContinuable()` 接受 prompt 之前，固定视觉 child 接受任务后独立运行，父侧超时没有调用 `subagents.interrupt()`。因此历史任务可在父侧 600 秒阻断后继续运行到 792.3 秒。当前候选已在 child 创建成功后，把 caller abort 映射为一次且只针对该 `childId` 的 ancestor interrupt；同时覆盖已 abort 竞态、隔离同步中断异常并在 `finally` 清理监听。模型路由与 600 秒常量没有改变。

工程验证与复审：新增 deadline interrupt 回归经历 RED→GREEN；全量测试 `46 files / 224 tests` 通过，`pnpm typecheck`、`pnpm test:built` 和 `git diff --check` 通过；两项独立复审均为 `Approved`，Critical/Important 为 `0/0`。2026-08-30 17:52 又针对场地边界、正式制品身份、命令、报告包、客户报告策略、报告源、Golden 全流程和视觉中断执行新鲜回归，结果为 `8 files / 90 tests` 通过，类型检查退出码为 0。

用于真实宿主复测的未升版候选包为：

- `C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.8.0\dsh-candidate-r5-timeoutfix-20260830-173248\architectureworld-dsh-preplanning-agent-0.7.0.tgz`
- 1,840,740 bytes；SHA-256 `49E52163EE5F6A76CE1EC1894D2D65851230651D2B6AE2A51C1C193CB981645C`；包版本仍为 `0.7.0`。
- 通过官方 `dsh plugin --profile web remove/add` 安装；当前 DSH Web 宿主 PID `45648`，`http://127.0.0.1:3080/` 返回 HTTP 200，stderr 为 0 bytes。
- Profile 内安装后的 `lib/index.js` 与本地构建 SHA-256 均为 `6C1FB0DF205BD9F02C1FAD245AD4F754BBB171C60FA87ADA3EC89BBB4DAAB3EB`。

fresh 真实任务：项目 `视觉截止中断验收-r5-fix-20260830-1740`（`preplan-bfaa5173-177f-4c4a-8257-4c82861d51a2`），任务 `visual-r5-fix-smoke-20260830-1740`，固定 child `preplanning-visual-aba77be0e631072e74927e62`，Provider/Model 仍为 `antigravity / gemini-3.1-flash-image`。本轮没有替换模型、复用历史图片或人工注入候选。

该 fresh 请求仍未生成图片，但失败形态已经查清：上游返回 `429 RESOURCE_EXHAUSTED / All accounts exhausted`，界面给出的 quota reset timestamp 为 `2026-09-04T10:49:45Z`；child 在 `529.428 s` 后结算，治理任务于 17:49:32 进入 `blocked`，原因为 `completed without an assistant image`。Session cache 显示 `openStep = null`、`pendingCalls = 0`，该项目候选资产 0、已采用资产 0、图片目录不存在，因此没有孤儿运行 child，也没有伪造候选。由于上游在 600 秒之前已失败，本次真实宿主复测没有覆盖新的 600 秒 interrupt 分支；该分支目前只有自动化 RED→GREEN 与静态复审证据。

因此总体状态继续保持 **PARTIAL**：正式场地边界 fail-closed、人工资料/闭合坐标登记确认、三格式工程候选、插件安装和中断回归已验证；真实 DSH 生图、采纳、正式发布、下载与重启恢复仍未通过。固定图像模型恢复可用前不得反复提交同一路由，也不得切换模型或拿历史资产冒充通过。

用户在本轮再次确认的场地输入原则已经进入系统提示、治理记录和正式导出门禁：具体场地位置和法定边界不能靠自然语言表达或模型推断；正式项目必须由人提供并确认已采用总平图、已采用红线图、带 CRS 的闭合坐标或兼容 GeoJSON。工程测试可使用自定义待核研究范围，但必须持续显示固定免责声明并被正式导出门禁阻断。地图圈地仅作为后续人类输入入口，必须输出 CRS、闭合几何和来源并再次由人确认；本轮不把地图 UI 擅自扩入开发范围。

必须同时保留一个尚未闭环的产品差距：当前 `/preplan-boundary-asset` 只能引用治理库中已经存在、已采用且属于本项目的 evidence/deterministic asset，插件尚未提供“用户附件或项目文件 → 安全导入 → 计算文件 SHA → 登记/采用为边界证据”的生产入口；`/preplan-boundary-coordinates` 能保存带 CRS 的闭合坐标，但尚未在生产路径中自动生成带 `boundaryGeometrySha256` 的确定性边界分析图；`geojson` 目前只有合同枚举，没有可调用的导入命令。现有 1×1 文件与空 SVG 测试只证明 hash lineage 与跨媒介 identity，不证明真实图纸读取、坐标制图或几何真实性。因此当前可以宣称“错误正式发布已 fail-closed”，不能宣称“人类场地资料输入 E2E 已完成”。真实项目启用前，必须另行批准并完成至少一条可用输入链；推荐先做文件/附件与闭合坐标 MVP，地图圈地 UI 作为后续独立能力。

### 0.9 2026-08-31 R5 历史分析图闭环、全量回归与视觉门禁

本节记录 R5 当时的工程与视觉基线；R5 随后又发现 PPTX 孤行和地图图例—北向标留白问题，曾由 0.10 的 R7 取代，当前有效基线已进一步更新为 0.12 的 R15。R5 保留为修复历史，不再作为最终展示目录。

#### 当时的 R5 目录与制品身份

- 最终目录：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.7.0\site-boundary-mvp-final-20260831-033330\research-preview-fixed-r5`。
- HTML：35 页，63,731 bytes，SHA-256 `b27cc264f82f92884d8f3263980260f4640204da0547d0884b9f05bce65d0d2e`。
- PPTX：35 页，1,684,651 bytes，SHA-256 `36b54f7a237aa39e71eff760bd14081dd6ff584120b0cd65a73c74b7a986dc00`。
- PDF：48 页，3,715,428 bytes，SHA-256 `d9bf08c2f21c5e964412c6056f6cc4cb2ccba6dc2b796650a8d7cbbedcbb2649`。
- `qa\research-preview-evidence.json` 仍明确记录 `kind: research_preview_evidence`、`publishable:false`；三种制品的实际字节数与 SHA-256 已逐项重算并与该证据文件一致。
- fresh artifact inspection：三媒介 identity 一致；核心价值在 HTML/PPTX/PDF 各出现一次；缺失采用资产 0；客户可见禁词 0；三媒介各有 12 页专业主视觉内容。

#### 本轮修复内容

1. PDF P27 由三组水平线改为 `三项内容层 → 唯一共同结果 → 三个责任节点` 的真实纵向连接；每条箭头均落入源节点和目标节点。
2. PDF P33 把三项决策及各自产出合并为三个输入节点，三条边共同汇聚到唯一 `triad-common-unlock`；不再生成三组互不相干的平行产出。
3. PDF P35 的共同解锁节点改为承接三列箭头的全宽节点，左右箭头不再落在节点外。
4. P28 的时段—客群矩阵由共享 `values` 数据驱动，HTML/PDF/PPTX 统一输出九格 `高/中/低`，并使用可辨识的三级色阶；不再把九格全部写成“需求重叠”。
5. P25 的“生态休闲区”标签上移，橙色空间轴线不再穿过文字。
6. 图像继续作为正常客户视觉使用；客户层不按制作工具分类，也不强调 AI 图像身份。

#### 新鲜验证证据

- RED：原始定向回归准确报出 P27/P33/P35 的非纵向或节点外连接、P28 九格值错误和 P25 轴线压字；补充的 HTML 拓扑测试也准确报出 `triad-common-unlock` 缺失。
- GREEN：HTML、页面计划、PDF 与真实 Edge 打印布局 4 个测试文件 `53/53` 通过；PPTX、报告打包与制品验证 3 个相关测试文件 `50/50` 通过。
- 完整回归：`pnpm test` 完成构建并通过 `56 files / 430 tests`；`pnpm typecheck` 退出码 0；`pnpm test:built` 为 `2/2`；`git diff --check` 退出码 0。
- PowerPoint 等价溢出检查：`slides_test.py` 返回 `Test passed. No overflow detected.`。
- 真实渲染：Microsoft PowerPoint 16.0 导出 `35/35`；Poppler 渲染 PDF `48/48`；两组渲染页的完全重复 SHA-256 组均为 0。
- 独立只读视觉 QA：Critical `0`、Important `0`、Minor `0`。P25/P27/P28/P33/P35 的上一轮问题全部关闭；两张全册联系表未见黑页、乱码、裁切、溢出、异常空白或明显低质页。
- 最终 QA 索引：`qa\pptx-contact-sheet-r5.png`、`qa\pdf-contact-sheet-r5.png`、`qa\pptx-key-pages-r5.png`、`qa\pdf-fix-pages-r5.png`。

#### 当前有效结论与真实性边界

R5 research-preview 的本轮工程门禁与视觉门禁已通过，可以作为“修改后的测试成果”展示；但整体产品状态必须继续写为 **PARTIAL**，不能写成正式发布或甲方验收完成。原因如下：

- 当前场地边界仍是自定义待核研究范围，三媒介继续显示 `研究范围（待核） · 非法定红线 · 非测绘成果`；它不是实际项目红线或测绘成果。
- 正式项目仍必须由人提供并确认总平图、红线图、带 CRS 的闭合坐标或兼容 GeoJSON；地图圈地 UI 仍是后续能力。
- 真实 DSH 生图、采纳、正式发布、下载与重启恢复 E2E 尚未通过，甲方现场验收亦未发生。
- 包版本继续保持 `0.7.0`；本轮不允许 stage、commit、push、merge、升版或创建 Release。

### 0.10 2026-08-31 R7 历史测试成果：孤行与地图留白闭环

本节记录 R7 当时的工程、制品与视觉基线；当前唯一有效基线见 0.12 的 R15。

#### 当时的 R7 目录与制品身份

- 当前目录：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.7.0\site-boundary-mvp-final-20260831-033330\research-preview-fixed-r7`。
- HTML：35 个客户主册页面，63,731 bytes，SHA-256 `b27cc264f82f92884d8f3263980260f4640204da0547d0884b9f05bce65d0d2e`。
- PPTX：35 页，1,684,724 bytes，SHA-256 `9dacbf5f2e9d18e07595195fbecbb89f5ed69d0d617426c7be3038bd9df27261`。
- PDF：48 页，2,002,461 bytes，SHA-256 `ca8f795c218b04b9364bcbd8187086acdf441692492d5a536bcd6214b2f46c06`。
- `qa\research-preview-evidence.json` 与上述三制品字节数、SHA、项目 identity、revision 和 24 个采用资产集合一致；仍明确记录 `publishable:false`。

#### 从 R5/R6 到 R7 关闭的问题

1. PPTX P3 的“3 个同步行动层”在物理排版中曾出现“面”“动层”等孤行；R7 将开场判断文本的预换行预算由 18 调整为 12，并把该短语主动排成 `3 个同步 / 行动层`。
2. PPTX/PDF P7、P9 的最后一项图例与北向标 `N` 曾仅相隔约 17px；R7 将 `map-boundary.svg`、`map-existing.svg` 的北向标纵坐标由 462 调整为 486，形成至少 32px 的基线间距。
3. 新增 PPTX 物理行宽回归，以及地图图例—北向标最小间距回归；RED 阶段均准确失败，修复后定向测试 `35/35` 通过。
4. 两张地图已同步到 fixture、manifest、client profile、provenance、HTML 与打印资产；`map-boundary.svg` SHA-256 为 `319cbb51bab59ad4ef24700bcee5184303d3f4830c890de83d9e862ed64065e6`，`map-existing.svg` SHA-256 为 `3970972c86da2d2ff6008bb61ba6137fd729610c0adde24e2cded91c92a37e04`。

#### 新鲜工程与制品验证

- 完整回归：`pnpm test` 通过 `56/56 files、432/432 tests`；`pnpm typecheck` 通过；`pnpm test:built` 为 `2/2`；`git diff --check` 退出码 0，仅有既存换行符提示。
- PowerPoint 等价溢出检查：`slides_test.py` 返回 `Test passed. No overflow detected.`。
- 真实渲染：Microsoft PowerPoint 16.0 导出 `35/35`；Poppler 渲染 PDF `48/48`；两组渲染页重复 SHA 组均为 0。
- 新鲜 artifact inspection：三媒介 identity 一致；核心价值在 HTML/PPTX/PDF 各出现一次；缺失采用资产 0；客户可见禁词 0；三媒介各有 12 页专业主视觉内容。
- 独立只读三格式完整性审计为 `PASS`：HTML 35 页、打印版/PDF 48 页、PPTX 35 页；PPTX 35/35 备注均且仅含一次 `[Publishable]false`；P24/P25 在三媒介均显示固定待核声明及总平图、红线图、CRS、GeoJSON 和人工确认要求。
- 主控对 `qa\pptx-fix-pages-r7.png`、`qa\pptx-contact-sheet-r7.png`、`qa\pdf-contact-sheet-r7.png` 复核后，P3 无孤字/孤词，P7/P9 图例与北向标留白已明显拉开，全册未见黑页、乱码、裁切、缺图或重复页。
- 独立全册视觉复审最终判定为：渲染与交付完整性 `PASS`，苹果式产品精修 `PARTIAL`，Critical `0`、Important `0`、Minor `3`。三项非阻断细节为：PPTX/PDF P4 副标题第二行以“的城市生活目的地”开头；P13“易涝关注区”对比度偏低；PDF P28 内容矩阵偏左、右侧留白偏大。PPTX 35/35 与 PDF 48/48 已逐页覆盖，P37—P48 的 E01—E12 证据页连续完整。

#### PDF 体积变化不是降质

R5 PDF 为 3,715,428 bytes，R7 为 2,002,461 bytes。约 99.29% 的缩减来自移除 28 个冗余 Type3 字体及 128 个重复 CharProc 字形；48 页数量、页面尺寸、有效文本、主字体、图像载荷和矢量绘图内容均保留。封面 JPEG 的尺寸、字节数与载荷 SHA 未变化，42/48 页逐像素一致，其余 6 页差异仅位于被主字体替换的地图标签区域。因此不得把体积下降误写为图片压缩或内容删减。

#### 当前有效结论与真实性边界

R7 在当时可作为“修改后测试成果”展示，但现已由 0.12 的 R15 取代；其整体状态始终为 **PARTIAL**，从未等同于正式发布或甲方验收完成。

---

### 0.11 2026-08-31 R14 历史测试成果：HTML-first 与大图完整显示闭环

本节曾取代 0.10 的 R7，现已由 0.12 的 R15 取代。R7、R12、R13、R14 均只保留为历史 QA，不得继续作为当前交付路径。

#### 当前能力形态与媒介边界

- 当前代码包 `@architectureworld/dsh-preplanning-agent` 是运行在 DSH 中、真正执行前期策划工作并生成成果的 npm 功能插件；它不是 Codex Marketplace 插件，也不是一个独立 Skill。
- Skill 是 Agent 的专业作业规程，负责规定何时索取资料、如何分析、怎样组织文案与图件、何时阻断发布；Codex 插件则可以在后续把 Skill、工具和连接能力统一打包安装。
- 本轮不新建 Skill 或 Codex 插件，不替换现有架构；先把 DSH 执行插件的客户成果质量做稳。后续若产品化为 Codex 插件，应由 Skill 作为专业入口并调用现有 DSH 能力，而不是重写这套执行逻辑。
- Golden / research-preview 过程候选默认只生成 HTML；CLI 仅在显式传入 `--formats html,pptx,pdf` 时生成三格式。正式 `/preplan-export` 与 `ReportPackageService` 继续保留 HTML/PPTX/PDF 三格式能力。

#### 当时的 R14 目录与制品身份

- 当前目录：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.7.0\site-boundary-mvp-final-20260831-033330\research-preview-html-first-r14`。
- HTML：35 个客户主册页面，74,344 bytes，SHA-256 `482e9b21d094e313d9257a7c513b568853ccf4456716e09564f3cac5522dfad5`。
- 顶层只包含 `html/` 与 `qa/`；`qa\research-preview-evidence.json` 仅记录 `html`，并明确为 `publishable:false`。
- 本候选不存在 `report.pptx`、`report.pdf` 或 `print/`；不得把历史 PPTX/PDF 混入本轮 HTML-first 交付。
- 35 页全册总览图：`qa\html-contact-sheet-r14.png`；35 张真实 Edge 原页截图：`qa\html-pages\page-01.png` 至 `page-35.png`；增强布局审计：`qa\html-layout-audit-r14.json`。

#### 视觉规则与 R12/R13 历史问题

1. 摄影、概念场景和章节幕继续使用 `.page-backdrop` 满版背景，并保持 `object-fit:cover`；封面、开场页和章节幕不退回小卡片。
2. 地图、场地分析图、数据图表等专业图件统一在 `.visual-evidence-media` 大媒体容器中使用 `object-fit:contain`，优先保证标题、坐标轴、图例、数据点和边界完整可见。
3. 左右通栏采用 5:7 网格，媒体宽度实测 58.3%；上下通栏采用 44:56 网格，媒体高度实测 55.9%；满版专业图页的媒体容器覆盖 99.9% 以上页面。
4. 图注位于图片内部底部浮层，不再额外撑高页面；纯文字页实测仅 1 页。P16/P19 的 editorial 文本区改为从顶部排布，并通过减小 padding、标题字号与卡片间距消除越界，没有隐藏或裁切文字。
5. R12 曾因图注额外撑高视觉页而产生约 32px 的页面高度偏差；该版仅作失败 QA。R13 修复图注后，增强实图检查又发现 P15/P16/P19 使用 `cover` 裁掉图表内容，且 P16/P19 标题向页面上方越界；R13 同样不得交付。

#### 新鲜工程、浏览器与人工验收

- `pnpm test`：`56/56 files、445/445 tests` 通过；`pnpm typecheck` 退出码 0；`pnpm test:built` 为 `2/2`；`git diff --check` 退出码 0，仅有既存换行符提示。
- 独立只读代码复核：规格符合性通过，代码质量 `Approved`，未发现 Critical、Important 或 Minor。
- 真实 Edge 视口 `1600 × 900`：35 页；纯文字页 1；缺图 0；横向溢出 0；运行时错误 0；布局违规 0；文字出界 0；对比度违规 0。
- 专业图件实际计算样式均为 `contain`；摄影背景实际计算样式仍为 `cover`；P16/P19 当前/目标卡片对比度为 `16.83:1`。
- 已人工放大复核 P1、P6、P15、P16、P19、P24、P30、P35：P15 的图表标题、坐标、分类与数值完整；P16/P19 标题和正文全部位于页面内；场地总平研究图、实施路径和最终决策页无明显裁切、缺图或越界。

#### 当前有效结论与真实性边界

R14 在当时可直接展示为 HTML-first“修改后测试成果”，但现已由 0.12 的 R15 取代；其整体状态始终为 **PARTIAL / publishable:false**，从未等同于正式发布或甲方验收完成。

---

### 0.12 2026-08-31 R15 当前测试成果：多图版式与单屏图注闭环

本节取代 0.11 的 R14，作为当前唯一有效的过程候选、工程验证与视觉验收基线。R7、R12、R13、R14 仅保留为历史 QA，不得继续作为当前交付路径。

#### 页面合同与视觉规则

1. HTML 页面计划具有显式图组合同：1 图为 `single`；2 图为 `pair-horizontal / pair-vertical`；3 图为 `hero-plus-two`；4 图为 `grid-2x2`；5—6 图为 `gallery-3x2`；超过 6 图时每 6 张自动拆为一页。
2. 单图页在左右或上下大媒体结构中交替：P9 为上图下文，P15 为左文右图，P18 为左图右文，P21 再回到左文右图。地图、分析图与数据图表使用 `contain`，摄影、场景与材质图使用 `cover`。
3. 三图页采用一主两辅：首图承担专业判断并标记为 `primary`，两张辅助图上下排列并标记为 `supporting`；多图页只保留短标题、资料来源和每张完整图注，不回退为文字清单或等权邮票墙。
4. Golden / research-preview 过程候选默认只生成 HTML；正式 `/preplan-export` 与 `ReportPackageService` 继续保留 HTML/PPTX/PDF 三格式能力。本轮没有把 HTML 拆页规则强行写入正式 PPTX/PDF 路径。

#### 唯一当前 R15 目录与制品身份

- 当前目录：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.7.0\site-boundary-mvp-final-20260831-033330\research-preview-html-first-r15`。
- HTML：35 个客户主册页面，83,357 bytes，SHA-256 `634737ba06792e89491df0cb2e35d376148b2d0ad4c97da2f13fffa0dcee0153`；实算哈希与 `qa\research-preview-evidence.json` 一致。
- 顶层只包含 `html/` 与 `qa/`；证据清单仅记录 `html`，并明确为 `publishable:false`。不存在 `report.pptx`、`report.pdf` 或 `print/`。
- 35 页全册总览图：`qa\html-contact-sheet-r15.png`；35 张真实 Edge 原页截图：`qa\html-pages\page-01.png` 至 `page-35.png`；增强布局审计：`qa\html-layout-audit-r15.json`。

#### TDD 修正与新鲜验证证据

- 第一轮 RED 准确报出 gallery 主图 `min-width / min-height` 未归零；补齐后定向测试转绿。真实 Edge 随后进一步发现 P7/P10/P13/P16/P19/P22 被内容撑到 1293px，证明仅约束图片不足以保证单屏。
- 第二轮 RED 要求 `.visual-evidence-gallery` 明确固定为 `height:100vh; min-height:0`；桌面端转绿，同时移动端明确回落为 `height:auto`。最终六个 gallery 页均回到单屏，主图和底部图注完整可见。
- 定向回归：`tests/report-html.spec.ts` 与 `tests/report-page-plan.spec.ts` 为 `57/57`；完整回归：`pnpm test` 为 `56/56 files、457/457 tests`；`pnpm typecheck` 退出码 0；`pnpm test:built` 为 `2/2`；`git diff --check` 退出码 0，仅有既存换行符提示。
- 真实 Edge 视口 `1600 × 900`：35 页、35 张截图；纯文字页 1；缺图 0；横向溢出 0；布局违规 0；运行时错误 0；禁词命中 0；页面级图注裁切 0；页面级文字出界 0。
- 独立代码复核与最终制品视觉复核均为 `Approved`，未发现 Critical、Important 或 Minor。人工放大复核 P7、P10、P16、P18：主图图注完整；地图边界、图例、北向与比例尺完整；一主两辅层级成立；P18 左图右文比例明确。全册联系表未见黑页、缺图、异常重复或连续邮票墙。

#### 当前有效结论与真实性边界

R15 是当前可直接展示的 HTML-first“修改后测试成果”，但整体状态必须继续保持 **PARTIAL / publishable:false**。当前场地仍是自定义待核研究范围；正式项目必须由人提供并确认已采用的总平图、红线图、带 CRS 的闭合坐标或兼容 GeoJSON，地图圈地交互仍是后续能力。真实 DSH 生图、采纳、正式发布、下载、重启恢复 E2E 与甲方现场验收仍未完成。图像按正常客户视觉使用，不按制作工具分类，也不强调生成方式。本轮没有 stage、commit、push、merge、升版或创建 Release。

---

### 0.13 2026-09-02 工程交接：视觉优先报告、正式场地边界、DSH 弹窗与 Git 收束

本节是下一位开发 Agent 的当前执行入口，取代 0.12 中关于源码、测试、工作树和 Git 状态的描述；0.12 及更早章节继续作为需求沿革、视觉标准和历史 QA 证据，不得冒充当前正式验收。

#### A. 已完成的工程能力

1. **报告默认路径改为 HTML-first。** Golden 与研究预览默认只生成 HTML，过程阶段不再强制生成 PPT/PDF；正式 `/preplan-export` 仍保留 HTML/PPTX/PDF 同源输出能力，避免把过程文件成本转嫁给用户。
2. **多图版式改为数量、比例、主次和页面节奏驱动。** 页面计划与 HTML 渲染已覆盖非对称双图、叠合双图、满版三联、一主两辅、L 形、中心锚点、错落四图、瀑布五图、编辑拼贴、周边马赛克等版式。地图、摄影、图表等语义类型不决定版式；专业图件是否使用 `contain` 或场景图是否使用 `cover` 只影响裁切策略。
3. **专业图件成为第一视觉主角。** 客户合同要求至少 6 张有效数据分析图表并覆盖至少 4 类业务主题；城市区位、项目边界、现状用地/建筑、交通可达性、内部流线、资源与限制 6 类场地分析缺一不可；内容页视觉覆盖率不得低于 50%，纯文字页不得连续超过 2 页。
4. **正式场地边界改为受控人工输入。** 新增正式边界 Schema、几何校验、原子资产存储、状态服务、SVG/场地图渲染、命令流和重启恢复。正式项目只能采用用户确认的总平图、红线图、带 CRS 的闭合坐标或兼容 GeoJSON；自然语言、普通地图截图和未确认圈画只能进入“研究范围（待核）”，不得升级为法定红线。
5. **报告排版与字体约束已工程化。** 新增客户字体尺度、默认专业档案、分析型 HTML 与场地图渲染；打印版通过真实 Edge 检查标题断词、短尾行、满版图件重叠、文本出界和矩阵居中。
6. **DSH“前期策划”弹窗已修复。** 启动器通过 React Portal 挂到 `document.body`，表单使用视口固定定位、最大高度和内部滚动，并改用宿主实际存在的 `--dsw-*` 主题变量。这样解决了侧栏祖先 `overflow:hidden` 导致的裁切，也修复了白底近白字。`react-dom` 被设为外部依赖，客户端包保持约 36 KB。

#### B. 为什么这样实现

- 客户要的是可以直接汇报、传阅和决策的建筑产品手册，而不是会议纪要、工作日志或治理台账；因此客户投影层与底层治理状态必须隔离。
- 建筑前期策划的判断必须由区位、边界、现状、交通、流线、限制与数据图表支撑；单纯堆文字或概念效果图无法构成专业证据链。
- 苹果式质感依赖巨幅视觉、单页单焦点、明确层级和克制排版；不能退化为等权小卡片、邮票墙或连续纯文字页。
- 法定红线属于高风险事实，模型不能凭语言描述猜测；必须把“人提供、人确认、可追溯来源”变成工程门禁，而不是提示词建议。
- 弹窗问题来自 DOM 挂载层级与宿主裁切，不是 z-index 不够；Portal 是针对根因的修复。

#### C. 2026-09-02 新鲜验证证据

- 功能提交：`a1faf2c feat: finalize visual-first preplanning workflow`，包含 104 个文件的源码、合同、夹具与回归测试变更。
- `pnpm test`：退出码 0；56/56 个测试文件、466/466 项测试通过。
- `pnpm typecheck`：退出码 0。
- `pnpm test:built`：退出码 0；2/2 项构建产物测试通过。
- `git diff --check`：退出码 0；仅出现仓库既有的 LF/CRLF 转换提示，没有空白错误。
- 构建产物：Host ESM 包约 1.73 MB；Client CJS 包 36.17 KB，source map 62.11 KB。
- 冻结目录 `handoff/FINAL_v2.0/` 与 `contracts/v0.6/` 没有被本轮功能提交修改。
- 实机热修验收：1286×1080 下弹窗 `position:fixed`、父节点为 `BODY`、四边位于视口内；800×480 下仍位于视口内并可内部滚动。热修只同步了当前 DSH Profile 的客户端构建，不等于正式重新安装新候选包。

#### D. 当前未完成与已知风险

1. **客户成果仍未正式通过。** R16 的旧 `qa/html-pages/page-*.png` 大部分错误地重复封面，旧 contact sheet 不能作为视觉验收证据；必须按每个 `.report-page` 的真实 CDP 坐标重新截图、生成布局审计 JSON、重做 contact sheet 并人工逐页检查。
2. **真实项目边界资料尚未提供。** 当前只能证明研究模式与合成边界闭环，不能声明任何真实项目法定红线成立。
3. **真实 DSH E2E 尚未闭环。** 仍需验证项目创建、人工确认、生图、采纳、正式发布、下载和重启恢复；端口 200、自动化测试或本地构建不能替代该验收。
4. **运行错误尚未诊断。** 曾出现 `ProposalEnvelope validation failed: /validation_intent must be equal to one of the allowed values` 和 `fetch failed / TRANSPORT`。下一位 Agent 必须先读取实际 Schema enum、捕获模型生成值和系统提示，再做最小修复；禁止直接放宽 Schema 或猜测允许值。
5. **安装状态不是正式发布状态。** DSH Web Profile 仍引用旧 v0.7.0 正式候选 tgz，当前仅做了可回滚的客户端热修。源码包未升版、未发布 npm、未创建 GitHub Release。

#### E. 下一位 Agent 的固定执行顺序

1. 先执行 `git status --short --branch`、`git log -5 --oneline --decorate`、`git branch -a -vv`，确认最终只剩干净的 `main`，不要复活已清理支线。
2. 复现并诊断 `validation_intent`，用真实 Schema enum 与失败载荷建立 RED 测试，再做最小修复并跑相关回归。
3. 使用 Edge CDP 对 R16 的 35 个 `.report-page` 逐页按真实坐标截图；生成 `qa/html-layout-audit-r16.json` 和新的 contact sheet，人工检查大图尺度、裁切、图注、文字边界、重复模板与全册节奏。
4. 重新运行 `pnpm test`、`pnpm typecheck`、`pnpm test:built`、`git diff --check`。
5. 从当前 `main` 制作新候选包；先备份 `C:\Users\2899\.dsh\profiles\web`，再通过 DSH 官方 CLI 安装，不能直接把热修文件当正式安装。
6. 在真实 DSH 页面完成弹窗、项目创建、人工边界输入、人工确认、生图、采纳、HTML 过程成果、正式发布、下载与重启恢复 E2E。
7. 只有真实业务 E2E、正确全页视觉 QA 和甲方验收同时通过后，才允许升版、发布 npm 或创建 Release。

#### F. 禁止操作

- 不得修改或重新保存 `handoff/FINAL_v2.0/` 与 `contracts/v0.6/` 冻结基线。
- 不得用 R15/R16 历史数字、端口在线或自动化测试替代当前真实 DSH 与客户视觉验收。
- 不得把自然语言、地图截图或未经确认的圈画升级为正式红线。
- 不得把日志、JSON、manifest、本地路径、Gate/Revision/Workflow ID 放进客户主册。
- 不得在未调查 `validation_intent` 根因前放宽 ProposalEnvelope 验证。
- 不得覆盖用户 Profile、Session、Storage、模型设置或凭据；安装前必须备份，且不能泄露认证内容。
- 不得升版、发布或创建 Release，除非用户另行明确授权。

#### G. 2026-09-02 Git 收束最终状态

- 权威仓库：`https://github.com/ArchitectureWorld/pre-design.git`。
- 唯一分支：本地 `main` 与远端 `origin/main`；功能与测试收束点为 `730b1e12b8adff5d7c5ac8e05f6b94f4be47f7ee`，随后只追加本 HANDOFF 文档提交。最终 SHA 不在文档内自引用，接棒时用 `git rev-parse main` 与 `git rev-parse origin/main` 核对，二者必须相等。
- 主线归并提交：`2db4504 merge: consolidate visual-first preplanning into main`。
- Windows 跨工作树稳定性修复：`f6ea74d fix: pin svg fixtures to lf bytes`；全新 `core.autocrlf=true` clone 中 12 个 SVG 均为 `w/lf`，关键 SHA 与 manifest 一致。
- 测试隔离修复：`730b1e1 fix: isolate vitest from sibling worktrees`；完整测试不再扫描 `.worktrees/r070/tests`。
- 已删除远端 `bootstrap/baseline-v2.0`、`release/dsh-preplanning-0.7.0`；已删除本地 `feat/client-report-v0.8.0`、`release/dsh-preplanning-0.7.0`、`feat/d1-vertical-slice`。
- `feat/d1-vertical-slice` 没有直接合并：它来自不连续历史，直接合并会删除冻结基线；其有效能力已由 v0.7 发布线进入主线。删除前已把全部 refs 做成完整 Git bundle，避免不可恢复丢失。
- 归档 bundle：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.8.0\git-consolidation-20260902-1327\pre-design-before-main-consolidation.bundle`；15,469,780 bytes；SHA-256 `7690a6f42dfc18149239183d9551bd2cd63c99d3315187156d96c3df6fc06af9`；`git bundle verify` 已通过并确认记录完整历史。
- 已移除 r070 Git worktree 注册及其残留目录；当前 `git worktree list` 只显示主仓库。旧 `work/` 内的运行日志与 Profile 备份没有删除，已通过 `.gitignore` 排除，不影响仓库干净状态。
- 合并后新鲜验证：`pnpm test` 为 56/56 files、466/466 tests；`pnpm typecheck` 退出码 0；`pnpm test:built` 为 2/2；`git diff --check` 退出码 0。

---

## 1. 不可误解的成果定位

### 1.1 主成果是什么

准确定位：

> 面向甲方决策者、可直接正式汇报和会后传播的“策划型建筑产品册 / 项目价值提案册”。

它必须同时具备四种能力：

1. 像咨询报告一样有明确判断。
2. 像建筑方案册一样有空间、图纸和场景。
3. 像产品发布一样聚焦价值、体验与记忆点。
4. 像决策简报一样回答投资、实施和下一步。

### 1.2 主成果不是什么

以下内容不能作为最终主成果：

- 会议纪要
- 工作过程说明书
- 调研资料堆积
- 政策摘抄汇编
- 功能清单或表格台账
- 方案截图集合
- 测试日志、JSON、manifest
- DSH 状态页和调试页面
- Gate、Revision、Workflow、工作项、完成度或审批记录
- “我们做了什么”的内部总结

判断方法：

> 把制作人员叫走后，甲方仍能独立回答“为什么做、做什么、价值在哪里、如何落地”，成果才算完成。

### 1.3 成果必须分为三层

#### A. 甲方主册

只保留核心判断、机会价值、定位愿景、产品场景、空间方案、运营投资、实施路径和决策事项。

#### B. 专业附录

承载完整政策依据、详细现状数据、指标表、导则、案例研究、测算假设和技术清单。

#### C. 内部治理包

承载会议纪要、版本记录、审批状态、工作项、完成度、测试日志、JSON、调试截图、运行证明和文件路径。

技术深度不是删除，而是分层；客户层与内部治理层必须隔离。

---

## 2. 当前项目与运行基线

### 2.1 目标代码仓库

- 基线仓库：`https://github.com/ArchitectureWorld/pre-design`；实现仓库为本地 `dsh-preplanning-agent`。
- 当前实现 worktree：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\work\dsh-preplanning-agent\.worktrees\r070`。
- 当前分支：`feat/client-report-v0.8.0`；HEAD：`4847bdd`（`feat: promote professional visuals to hero pages`）。
- `package.json` 版本仍为 `0.7.0`；worktree 中保留本轮有意的未提交改动和新增文件，不能再写成“干净状态”。
- 唯一当前 HTML-first research-preview R15：`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.7.0\site-boundary-mvp-final-20260831-033330\research-preview-html-first-r15`；R7、R12、R13、R14 仅作历史 QA。
- 唯一 HANDOFF：`C:\Users\2899\Nutstore\1\开发\12_前期策划\Reference\HANDOFF-前期策划甲方产品手册改版-2026-08-28.md`。
- 历史 v0.7.0 Release：`https://github.com/ArchitectureWorld/pre-design/releases/tag/v0.7.0`；它不是本轮未升版、未发布候选的实时证明。

本轮严格保持未 stage、commit、push、merge、升版或创建 Release。任何后续集成都必须先重新核对完整 diff、真实性边界与用户授权。

### 2.2 DSH 交接时实测状态

以下为 2026-08-28 的实测快照，不等于 2026-08-29 之后的当前状态：

- 地址：`http://127.0.0.1:3080/`
- HTTP：200
- 页面标题：`DeepSeek Harness`
- 监听地址：`127.0.0.1:3080`
- 当时进程 PID：`49644`
- 进程：`C:\Program Files\nodejs\node.exe`
- 启动命令：`@deepseek-ai/dsh\lib\bin.js --profile web --no-open`
- 插件包：`@architectureworld/dsh-preplanning-agent@0.7.0`

这只证明当时 DSH 服务在线，不能替代插件界面、任务执行、生图、报告发布和下载链路的真实验收。

### 2.3 v0.7.0 Golden 历史基线

前序记录：

- Revision 57
- 57/57 工作项
- 8/8 Gate
- 12 张概念图
- 17 张图表
- PPTX 42 页
- PDF 64 页
- 40/40 测试文件
- 97/97 测试
- built package 2/2
- `contracts/v0.6` 949/949
- 类型检查与构建通过

这些数字是改版前历史基线，不是改版完成后的验收结果。代码变化后必须重新运行。

Golden 根目录：

`C:\Users\2899\Documents\Codex\2026-08-27\yue-du\outputs\dsh-preplanning-0.7.0\golden-project`

主要文件：

- `html\index.html`
- `report.pptx`
- `report.pdf`
- `artifact-manifest.json`
- `qa\ppt-montage.png`
- `qa\pdf-montage.png`
- `qa\ppt-render\`
- `qa\pdf-render\`

旧 Golden 必须保留为“改版前”对比证据，不得覆盖唯一历史结果。

---

## 3. 参考资料深读结果

### 3.1 已完成的研究规模

Reference 目录中的正式材料已经完成全量读取和视觉检查：

- 14 份正式材料
- 4 份 PPTX
- 10 份 PDF
- 共 1,536 页
- 已逐页提取标题、正文、文本密度、图片、表格、图表、字体和字号
- 已生成全页缩略图总览和代表页高清渲染
- 原始参考资料未被修改

分析工作目录：

`C:\Users\2899\.codex\visualizations\2026\08\28\01a048d5-ba66-7eb0-b376-c210bc52f22c\reference_analysis`

核心索引：

`C:\Users\2899\.codex\visualizations\2026\08\28\01a048d5-ba66-7eb0-b376-c210bc52f22c\reference_analysis\output\reference_index.json`

### 3.2 正式材料清单

1. `20251029鄂州城市更新1.pptx`
2. `城更+策划文件整理\城市更文本\20230318宜昌市城市更新 .pdf`
3. `城更+策划文件整理\城市更文本\20251121鄂州城市更新.pdf`
4. `城更+策划文件整理\城市更文本\儒学社区汇报文本.pdf`
5. `城更+策划文件整理\城市更文本\祥店片区完整社区项目方案.pdf`
6. `城更+策划文件整理\城市更文本\鄂州市全民健身中心.pdf`
7. `城更+策划文件整理\城市更文本\黄冈东门社区.pdf`
8. `城更+策划文件整理\策划类文本\11- 东街城市更新概念规划文本 .pdf`
9. `城更+策划文件整理\策划类文本\14-浙江省衢州市衢江区城南片区城市设计 [同济]（143页）.pdf`
10. `城更+策划文件整理\策划类文本\厦门三康社区.pdf`
11. `城更+策划文件整理\策划类文本\鄂州市全民健身中心建设项目谋划汇报4.1.pptx`
12. `鄂州城市更新-明塘+洋澜湖地块（汇总）-汇报版1125.pptx`
13. `鄂州城市更新-明塘.pdf`
14. `鄂州市2025年度策划咨询服务项目12.7.pptx`

### 3.3 参考资料的正确角色

这些材料提供的是：

- 内容完整性的及格线
- 专业图种和分析方法
- 基本客户叙事
- 空间、业态、投资与实施之间的证据链

它们不是最终视觉上限。需要主动避免其中普遍存在的问题：

- 页面密度过高
- 标题只是栏目名
- 表格和小图过多
- 字体和视觉风格混杂
- 技术路线侵占主叙事
- 多页机械重复
- 封面与正文完成度断层
- 视觉缺少产品发布级焦点与节奏

### 3.4 最值得继承的长处

- `东街城市更新概念规划文本`：建筑产品册感较强，大图、章节幕、Before/After 和场景叙事完整。
- `鄂州市2025年度策划咨询服务项目12.7`：商业汇报叙事最强，善于提出命题、价值启示和最终追问。
- `20251029鄂州城市更新1`：专业规划深度较强，问题、定位、业态与空间系统完整。
- `鄂州城市更新-明塘`：前策推演较完整，能从客群、定位进入主题产品和消费场景。
- `全民健身中心建设项目谋划汇报4.1`：适合学习“数据—缺口—定位—投资—结论”的决策简报。
- `衢江区城南片区城市设计`：地图、导则与专业证据系统完整，但高密度老式结构不能照搬。

### 3.5 语料证据结论

全量主题信号：

- 产业与运营：1,622
- 空间与设计：1,335
- 现状与问题：666
- 投资与实施：510
- 策略与路径：363
- 定位与愿景：330
- 价值与机会：241
- 背景与政策：131

这说明正式策划成果不能停在政策与现状分析，必须完成“价值—产品—场景—落地”的闭环。

高频动作词包括“提升、打造、形成、营造、构建、激活、塑造”；但动作词必须连接具体对象、空间动作和预期结果，不能形成空泛口号。

全量客户材料中，`Gate`、`Revision`、`Workflow`、`工作项`、`审批状态`、`完成度`、`审计日志`等内部治理词出现次数均为 0。客户层与内部层的隔离有真实语料依据。

---

## 4. 甲方主册的标准叙事

主线固定为：

> 背景与命题 → 现状判断 → 机会价值 → 定位愿景 → 策略体系 → 产品体系 → 空间场景 → 运营投资 → 实施路径 → 决策结论

### 4.1 开篇：三页内建立价值

前三页必须回答：

1. 这是一个什么项目？
2. 为什么现在必须做？
3. 最值得甲方记住的价值是什么？

禁止以十余页政策、区位和工作历程慢慢热身。

### 4.2 现状：输出判断而非资料

现状章节应收束为 3—5 个明确矛盾。每个问题必须由数据、地图、现场照片、访谈、规范或案例支撑。

### 4.3 机会：把问题转译为价值

示例：

- 民生短板 → 公共服务升级机会
- 闲置空间 → 可运营载体
- 历史文化 → 可感知、可参与的城市产品
- 交通节点 → 客流入口
- 滨水资源 → 公共活动与消费界面
- 体育设施 → 运动、社交、商业复合目的地

### 4.4 定位：形成可传播的项目身份

定位必须包含：

- 一句总定位
- 目标客群
- 核心价值
- 差异化来源
- 3—5 个关键词
- 项目在城市中的角色

定位不能只是“生态、活力、文化、融合”等通用词集合。

### 4.5 产品体系：把策略变成可体验产品

每个核心产品至少说明：

> 产品名称 + 目标客群 + 核心内容 + 使用场景 + 空间载体 + 运营方式 + 价值贡献

不能停留在“餐饮、零售、运动、文创”的业态清单。

### 4.6 空间场景：让人看见未来

每个空间动作必须回答：

- 解决哪个问题？
- 服务哪个客群？
- 承载什么产品和活动？
- 如何组织人流、停留、消费和公共服务？
- 改造前后有什么可感知变化？

### 4.7 运营、投资与实施：证明可落地

至少说明：

- 公益性与经营性内容如何组合
- 建设、招商、运营主体关系
- 投资构成和关键假设
- 近期启动项目
- 分期逻辑
- 关键资源、政策抓手和前置条件
- 下一步需要甲方作出的决策

---

## 5. 文案与信息表达标准

### 5.1 标题必须输出观点

弱标题：

- 项目背景
- 现状分析
- 案例研究
- 方案展示
- 工作内容
- 投资估算

产品级标题：

- 城市进入存量提质阶段，本项目成为老城价值重构的关键抓手
- 四大问题：风貌不显、空间局限、设施缺乏、业态低端
- 打开滨水界面，让公共空间成为活动与消费的共同舞台
- 以策划前置统筹空间、业态和投资，形成可实施的增长方案

至少 80% 的内容页应使用结论型标题。只读所有页面标题，也应能理解完整故事。

### 5.2 每页遵循四段逻辑

> 结论 → 证据 → 含义 → 对策

### 5.3 写作口吻

- 结论先行，少写过程。
- 用主动句和具体动词。
- 以客户价值、使用体验和实施结果为主语。
- 避免“我方完成、经讨论、已经确认、本轮状态”等内部口吻。
- 避免重复使用“打造、赋能、引领、地标、名片”等空泛词。

### 5.4 事实、判断、提案和测算必须区分

- 事实：标注来源、时间和单位。
- 判断：由事实推导。
- 提案：说明适用条件和实现路径。
- 测算：公开关键假设。
- 效果图：按项目阶段如实表达，不暗示已经建成；制作方式不进入客户层说明。

不得编造数据、政策、案例、合作品牌、项目进展或运营结果。

---

## 6. 苹果式产品美学总纲

### 6.1 设计目标

最终视觉应达到：

> 苹果式产品发布质感 × 高端建筑编辑设计 × 甲方决策提案

参考资料的专业内容可作为及格线，视觉必须明显高一个等级。不能只是“把参考资料排得更整齐”，而应像经过创意总监、建筑编辑和信息设计师共同完成的正式产品。

### 6.2 苹果式美学的七条转译原则

1. **一个页面，一个主角**：只能有一个第一视觉焦点。
2. **价值先于说明**：先展示客户能获得什么，再解释如何实现。
3. **尺度制造震撼**：关键空间、产品、数字和判断获得足够面积。
4. **留白建立尊贵感**：留白用于聚焦与节奏，不是信息不足。
5. **真实材质建立信任**：图像、模型、图表、光影和文字都要精准。
6. **微妙层次替代装饰**：依靠明暗、景深、材质、细边界和轻阴影，不依靠花哨形状。
7. **整册像一次发布**：页面前后有铺垫、揭示、高潮和收束。

### 6.3 不能误做成什么

- 不是简单黑底白字。
- 不是全册大字加留白。
- 不是毛玻璃、渐变和圆角卡片堆叠。
- 不是复制 Apple Logo、SF Pro、官网结构或产品图。
- 不是把建筑方案做成消费电子广告。
- 不是牺牲证据与专业深度换取“极简”。

---

## 7. 视觉系统规范

### 7.1 色彩

推荐比例：

> 75%—85% 中性色 + 10%—20% 项目主色 + 5% 强调色

建议基础中性色：

- 暖白或雾白：接近 `#F5F5F7`
- 主文字：接近 `#1D1D1F`
- 次级文字：接近 `#6E6E73`
- 深色章节：`#000000` 至 `#121212`
- 细分隔线：低对比冷灰或暖灰

以上是气质参考，不是强制复制 Apple 色值。项目主色应从场地、文化、材料或核心产品中提取，例如江湖蓝绿、砖瓦赭红、石材灰、植被绿或体育橙。

要求：

- 全册原则上只使用一个项目主色和一个强调色。
- 强调色只用于核心数字、推荐方案、项目范围和关键节点。
- 禁止默认蓝紫科技渐变。
- 禁止同页出现五六种无语义颜色。
- HTML 普通文字与背景建议达到 WCAG 4.5:1 对比度。

### 7.2 字体

最多使用两个字体家族：

- 中文主字体：HarmonyOS Sans SC、思源黑体、Noto Sans CJK SC 或经过验证的现代无衬线体。
- 英文和数字：Inter、Segoe UI 或与中文协调的无衬线体。
- 可选辅助字体：思源宋体，仅用于少量文化引语或章节情绪。

不要依赖仅在 macOS 存在的 SF Pro。若使用特殊字体，必须验证 Windows、HTML、PPTX 和 PDF 的嵌入与回退。

16:9 PPT 建议字号：

| 层级 | 建议字号 |
|---|---:|
| 封面主标题 | 54—80pt |
| 章节主张 | 48—72pt |
| 内容页观点标题 | 30—42pt |
| 二级标题 | 20—26pt |
| 正文 | 16—20pt |
| 图注与来源 | 10—12pt |
| 核心数字 | 72—140pt |

硬底线：标题不低于 24pt，正文不低于 14pt，图表标签不能依赖 7—8pt 小字才能读懂。标题原则上不超过两行，不留孤词。

### 7.3 网格与安全区

- 默认采用 12 列网格。
- 左右安全边距约占页面宽度 5%—7%。
- PPT 页面元素距离边缘原则上不少于 0.5 英寸。
- 使用 8pt 间距体系：8、16、24、32、48、64。
- 标题、正文、图片、图注、页码和来源必须落在一致的基准线上。
- 不允许每页重新发明边距。

### 7.4 留白

主张页可保留 40%—60% 的主动留白；证据页与技术页可更密，但必须维持明确层级。

坏留白包括：

- 内容挤在左上角，右下大片无意义空白。
- 一条窄文字带漂在页面中央。
- 图片自带大面积白边却未裁切。
- 页面看似极简，实际上缺少论据和视觉锚点。

### 7.5 深度、圆角和阴影

- 深度只用于区分功能层级，不用于装饰。
- 允许极轻阴影、材质渐变或细边界。
- HTML 大型产品模块可使用 18—28px 圆角；信息密集模块应更克制。
- PPT/PDF 不应变成浮动卡片墙。
- 卡片带贴边色条时使用直角或小圆角，避免色条与大圆角冲突。
- 禁止厚重投影、强发光、霓虹描边和廉价玻璃拟态。

---

## 8. 图像与空间视觉规范

### 8.1 图像优先级

默认优先：

1. 单张主视觉
2. 一主两辅
3. 60:40、65:35 或 70:30 非对称图文
4. 上下或左右对比
5. 多图矩阵

多图矩阵是最后选择。禁止把六七张图片缩成邮票。

### 8.2 全幅主视觉

适用于封面、章节幕、核心愿景、关键产品和最终价值页。图片可占页面 75%—100%。

文字必须放在天空、水面、地面、暗部或经过控制的安全区，不能压在复杂立面、人群和树影上。允许使用克制的局部渐变蒙版，不允许用厚重半透明黑框遮住图片。

### 8.3 一主两辅

一张大图承担空间和情绪，两张小图补充细节、尺度、材质或使用场景。禁止三张图片等大排列；等大意味着没有判断。

### 8.4 Before / After

尽量保持同一视点、相近焦段、相同边界和相同标注体系。可使用切换、滑动、上下镜像或清晰分割线。不能把两个完全不同角度的图片标成改造前后。

### 8.5 地图、总平面和轴测

关键地图、总平面和轴测图应占页面 60%—80%。旁边只保留一句结论、3—5 个标注、必要图例和简短解读。

地图建立四级层次：项目范围、核心影响区、关键交通/资源、普通城市背景。项目范围必须一眼可见，底图主动降噪。

### 8.6 裁切与清晰度

逐图检查：

- 建筑竖线和地平线
- 主体是否被错误裁切
- 入口、节点和人流方向
- 人物视线是否进入页面
- 文字是否遮挡主体
- 是否存在无意义天空、草地和空白边缘
- 色温、对比度、饱和度是否统一

全屏图原则上不低于 1920×1080。印刷型 PDF 大图应接近 150—300dpi。

### 8.7 图像来源中性原则

客户层不区分摄影、人工渲染、程序生成或其他制作方式。所有图像统一按清晰度、构图、裁切、项目专属性、信息有效性和全册风格一致性验收。

- 不要求在客户主册中标注制作工具或生成方式。
- 是否注明“概念示意”只取决于项目阶段与表达内容，不取决于制作工具。
- 场景图、地图、分析图和数据图表必须按实际信息角色使用，不能用错误角色标签替代专业内容。
- 图像不得暗示不存在的建成状态、法定结论、测算结果或项目进展。

### 8.8 数据分析图表硬合同

32—48 页甲方主册至少包含 6 张有效数据分析图表，且至少覆盖以下 4 类：

1. 现状规模、用地、设施或空间构成。
2. 客群、活动、时段或使用需求。
3. 交通可达性、步行时间、服务半径或流量结构。
4. 产品组合、运营收入、成本投入或价值贡献。
5. 分期、建设时序、投资节奏或实施优先级。

每张图表必须具备一个主要结论、直接标注、数据来源、日期、单位和必要口径。重复同一组数字、仅改变颜色或图形样式，不计为不同图表。数据不足时必须在内部 QA 中报告缺口并阻止正式发布，禁止编造或用场景图替代。

### 8.9 场地分析图件硬合同

建筑前期策划必须形成连续、可读、可追溯的场地分析图件序列。以下 6 项为所有项目的强制图件：

1. 城市区位与区域关系图。
2. 项目范围、已确认边界与研究范围图；只有法定资料支持时才标注“红线”。
3. 现状用地、建筑与功能分布图。
4. 对外交通、主要入口与可达性分析图。
5. 人行、车行、后勤与关键流线分析图。
6. 场地资源、核心问题与限制条件分析图。

景观生态、水系地形、历史文化、日照风环境、噪声视线、机会结构、功能落位和分期实施图根据项目条件补充。其中机会结构或功能落位图、分期落位图原则上应进入空间与实施章节。

正式项目进入场地分析前，Agent 必须主动向人索取并确认已采用总平图、已采用红线图、带 CRS 的闭合红线坐标或兼容 GeoJSON 的闭合几何。自然语言地点、普通地图截图、模型推断和未经确认的地图圈画都不能确定正式边界；缺少有效输入时只能制作待核研究范围，并阻止正式发布。

测试或演示可使用自定义研究范围，但图面必须完整显示 `研究范围（待核） · 非法定红线 · 非测绘成果`。未来地图圈地功能可以作为人类输入方式，但圈地结果在记录 CRS/闭合几何并完成项目确认前仍属于研究范围，不能被表述为法定红线。

正式场地图必须包含图名、边界性质、项目边界、图例、指北针、比例或尺度说明、数据来源和日期；不适用比例尺的概念结构图应明确标注 `NTS`。场地分析应在项目定位与产品体系之前完成，不得拖到附录充数。客户正文只需表达“项目边界：已由项目资料确认”或待核研究范围声明，不暴露内部 boundary ID、SHA、revision 或确认日志。

### 8.10 图件覆盖率与大图版式

- 除封面、三页开场、章节幕和最终决策大字页外，至少 50% 的内容页必须由图像、地图、图表或分析图担任第一视觉主角。
- 纯文字内容页不得连续超过 2 页；上述产品发布式大字页不计入纯文字连续页统计。
- 单图优先采用全页背景或半页大图。全页背景占页面 75%—100%，半页大图占页面 50%—65%。
- 地图、图表、总平面和分析图属于主视觉，不得缩成正文旁边的装饰配图。
- 同一图像原则上只承担一个核心页面；跨页复用必须有不同的分析目的和标注层级。

---

## 9. 大文字与苹果式产品发布节奏

### 9.1 大文字只用于关键时刻

适用内容：

- 项目总定位
- 核心矛盾
- 章节命题
- 决定性数字
- 价值转折
- 最终行动建议

不适用：政策原文、普通解释句、无意义英文、栏目名和没有语境的数字。

大文字页应包含：一个主判断、一个补充解释、一个视觉锚点和必要的章节/来源信息。不能让一句大字孤零零漂在空白中央。

### 9.2 页面节奏类型

全册应交替使用：

1. 电影化主张页：大图、大字、少信息。
2. 证据页：地图、数据、现场照片。
3. 推演页：问题到机会、定位到策略。
4. 产品场景页：人物、活动、空间体验。
5. 决策页：投资、分期、结论和行动。

推荐节奏：

> 视觉高潮 → 信息展开 → 证据加深 → 呼吸停顿 → 场景高潮 → 决策收束

硬要求：

- 同一种版式不得连续出现三页。
- 不能连续十页都是白底标题加四卡片。
- 深色页只用于开篇、章节转换或高潮。
- 技术密集章节之后安排总结或场景页。
- 封面建立的视觉母题必须在内页持续出现。
- 不允许封面精美、正文退化。

### 9.3 动效原则

HTML：

- 动效用于揭示层级、切换方案和强化滚动叙事。
- 推荐 300—700ms 的平滑 ease-out 或 spring 动效。
- 避免频繁弹跳、旋转、视差眩晕和无意义粒子背景。
- 核心信息不能依赖悬停才出现。
- 尊重 `prefers-reduced-motion`。

PPTX：

- 优先淡入、擦除和克制 Morph。
- 动效只服务讲述顺序。
- 不使用飞入、弹跳、旋转、随机路径和声音。

PDF：

- 靠静态构图完成全部表达，不能依赖动效补充信息。

---

## 10. 关键页面类型

### 10.1 封面

必须包含：具有项目识别度的主视觉、主标题、核心命题、必要单位与日期、可贯穿全册的视觉母题。

禁止九宫格、通用城市天际线、多个 Logo 抢中心、随机金线和渐变光效。

### 10.2 三页开场

建议结构：

1. 项目宣言：一句价值主张。
2. 时代/城市命题：为什么现在。
3. 项目答案：我们要创造什么。

应形成产品发布式的“提出问题—制造期待—揭示答案”。

### 10.3 章节幕

承担逻辑与情绪转换，可使用全幅场景、极简大字、局部地图、材料细节或关键数字。不能只是放大 `PART 01`。

### 10.4 产品页

一页只重点讲一个产品，至少包含产品名称、一句话价值、目标客群、关键体验、空间载体、主场景图和精简构成。

### 10.5 数据与证据页

- 一个主要结论。
- 一个主图表或主地图。
- 全册有效数据分析图表不少于 6 张，且覆盖不少于 4 类业务主题。
- 核心系列使用项目主色，其余主动降灰。
- 优先直接标注，减少图例跳读。
- 删除 Excel 默认边框、阴影、3D 和无意义网格。

### 10.6 场地分析章节

场地分析必须作为项目定位与产品体系之前的独立证据序列，至少用 6 个可独立阅读的主视觉页面完成城市区位、项目边界、现状用地/建筑、交通可达性、内部流线、资源与限制分析。不得用文字清单、通用城市照片或场景效果图替代。

### 10.7 总结与结尾

每章结束回答“发现了什么、意味着什么、下一章解决什么”。整册结尾必须形成项目价值、实施行动和甲方决策，不以单独“感谢聆听”代替结论。

---

## 11. HTML、PPTX、PDF 的媒介标准

### 11.1 同源原则

三种格式消费同一个客户叙事模型，核心结论、章节顺序、数据口径和视觉资产一致；允许根据媒介重排，不要求逐页像素复制。

### 11.2 HTML

- 首屏直接提出项目价值。
- 支持桌面主流尺寸，无横向溢出。
- 关键内容无需阅读说明书即可发现。
- 交互用于地图、方案对比、产品查看和场景切换。
- 图片、字体和数据完整加载。
- 无控制台错误、断链和空白模块。
- 支持键盘操作、基本无障碍和 reduced motion。
- 应具备打印或导出 PDF 的基本能力。

### 11.3 PPTX

- 默认 16:9。
- 核心元素可编辑。
- 正文优先 16pt 以上。
- 适合现场讲述，一页一个判断。
- 在真实 PowerPoint 中检查字体回退、错位、溢出和动画。
- 不把 PPTX 做成缩小的长篇 PDF。

### 11.4 PDF

- 字体嵌入，跨设备一致。
- 图片清晰，地图和文字放大后可读。
- 有页码、目录和必要书签。
- 兼顾屏幕阅读和打印。
- 不是低清 PPT 截图拼接。
- 可承载比 PPTX 更详细的说明，但不回退成技术台账。

### 11.5 篇幅

篇幅由受众和项目复杂度决定，不再把 28—32 页视为硬规格。

默认建议：

- 管理层简报：18—24 页
- 标准甲方产品册：32—48 页
- 完整策划主册：48—72 页
- 技术深度进入独立附录

不能为满足页数删掉证据链，也不能用重复页面制造“厚度”。

---

## 12. 客户层与治理层边界

### 12.1 甲方主册禁止出现

- Gate 编号与状态
- Workflow ID、工作项 ID
- Revision、冻结版本、成果版本 Rxx
- confirmed、approved、adopted 等内部枚举
- 状态矩阵、审批台账、责任追踪和内部行动项
- 自动化测试、运行日志和技术审计
- 本地路径、JSON、manifest 和调试截图
- “需甲方确认”“本轮决策事项”等会议主持语言，除非是正式决策页且用户明确要求

### 12.2 可以保留的位置

- DSH 系统界面
- Artifact manifest
- 内部 QA 包
- 独立审计报告
- 内部附件
- 不影响客户阅读且确有必要的不可见校验元数据

### 12.3 正确工程做法

不能为了视觉简化删除底层版本、证据和审计能力。应建立清晰的客户投影层，让同一事实源分别生成客户主册与内部治理附件。

---

## 13. 技术改造边界

重点文件：

- `src/report/types.ts`
- `src/report/source.ts`
- `src/report/build-document.ts`
- `src/report/theme.ts`
- `src/report/render-html.ts`
- `src/report/render-pptx.ts`
- `src/report/render-pdf.ts`
- `src/report/package-service.ts`
- `src/report/validate-artifacts.ts`
- `scripts/build-golden-project.ts`
- `tests/full-flow-golden.spec.ts`

推荐结构：

1. 保持治理数据源和发布校验。
2. 增加明确的 `client report projection`。
3. 客户模型只接收结论、证据、价值、产品、场景、实施和视觉资产。
4. Gate、Revision 和状态进入内部附件，不进入客户节点。
5. 三种渲染器消费同一个客户叙事模型。
6. 视觉资产与章节语义、产品和场景绑定，不按资产序号在末尾堆放。
7. 把苹果式设计令牌、页面类型、节奏和媒介差异显式建模，避免渲染器随意套模板。
8. 保留可追溯的 source、caption、图件语义类型、场地分析类型和测算假设，但不在客户层强调图像制作工具。
9. 在客户报告合同中显式建模：数据图表最低数量、场地分析必备类型、内容页视觉覆盖率和纯文字连续页上限。
10. 把场地边界作为独立治理输入：人工登记/确认的来源或闭合几何进入 frozen input，客户 profile 不得覆盖；正式发布前验证来源、文件 SHA、geometry lineage 与制品 identity。

### 13.1 已批准的场地输入 MVP 范围（2026-08-30）

用户已批准首版采用以下输入，不在本轮同时开发通用文件平台或地图圈地：

- 总平图、红线图：只接受 DSH 原生附件链可验真的 `PNG`、`JPEG`、`WebP`；拒绝 GIF。
- 几何输入：带独立 CRS 参数的二维闭合坐标数组，或单个 `Polygon`、单外环、无洞的 GeoJSON geometry。
- 第一阶段不接受 PDF、DWG、DXF、IFC、SHP、ZIP、FeatureCollection、MultiPolygon、带洞 Polygon 或在线地图圈画。
- PDF、DWG/DXF 原文件保全、原始文件 SHA、通用二进制附件存储、地图底图、地图圈地、坐标转换和复杂 GeoJSON 属于第二阶段独立能力。

DSH `0.1.1-rc.2` 的命令附件会先经过系统的图片格式、字节、像素和批次准入并持久化。DSH attachment ref 本身不提供 SHA；插件必须调用 `attachments.readImage()` 读取系统已验真的存储图像字节，并由插件对这些字节计算 SHA-256。因此第一阶段治理记录中的 SHA 必须称为“插件对 DSH 已验真并读取出的存储图像字节计算的 SHA-256”，不得称为原始 PDF、DWG 或原始上传文件 SHA。正式确认表示项目负责人确认该存储图像是当前项目采用的总平/红线表达；系统不使用 AI 猜测图像是否具有法定效力。

### 13.2 状态、来源与真实性合同

沿用 `SiteBoundaryRecord` 的 `pending_confirmation → confirmed_formal_boundary` 两步状态，不把整个项目粗暴分类为“正式项目/测试项目”。真实项目在资料不足时同样可以处于研究阶段；能否正式发布只由当前 Revision 的确认边界决定。

记录必须显式保存：

- `source`：`approved_site_plan | approved_redline | closed_coordinates | geojson`。
- `origin`：`user_image | user_coordinates | user_geojson | synthetic`。
- 图片输入：attachment ID、媒体类型、显示名、宽高、字节数、存储图像 SHA、内部证据资产 ID 与文件名。
- 几何输入：CRS、规范化闭合坐标、geometry SHA、确定性派生图资产 ID、派生图 SHA。
- 提交人、提交时间、提交 Revision、确认人、确认时间、确认 Revision。

`synthetic` 只能形成研究候选，永远不得进入 `confirmed_formal_boundary`。生产上下文中的图片、坐标和 GeoJSON 命令必须在 handler/service 内部分别固定 `origin = user_image | user_coordinates | user_geojson`；`origin` 不得出现在命令 grammar、raw input、JSON payload 或服务 public input type 中。受控测试必须由 raw input 无法设置、且不进入生产 bundle 的 `synthetic_fixture` capability 包装同一真实命令执行链，服务据此强制写入 `origin = synthetic`；`synthetic` 禁止通过复制、重放、修改 `source` 或重新登记升级为 `user_*`。TypeScript、运行时 Zod、v0.7 JSON Schema、repository、confirm、confirmed selector 和 export preflight 必须共同拒绝 `synthetic + confirmed_formal_boundary`。

Agent、模型、ProposalEnvelope、Agent tool 和自动工作流不得替人提交或确认。提交与确认入口只允许 DSH 人工交互命令通道，actor 的角色与调用通道来自 Host/session context，不能来自 raw input、profile 或模型生成字段；两个动作是两次独立调用，但第一阶段不强制由两个不同自然人完成。当前 DSH Host 若不能提供可独立核验的自然人身份，本轮只能按“单用户受信任部署”验收，不得宣称具备多用户角色鉴权；实现不得继续把任意调用无条件伪装为 `decision_owner`。后续 Revision 只能选择 `confirmedRevision <= currentRevision` 的记录，跨项目记录、future-only 记录和哈希不一致记录全部 fail-closed。

### 13.3 图片附件输入链

保留现有 `/preplan-boundary-asset` 名称并兼容旧的 `<source> <assetId>` 命令语法；新增附件方式时命令声明 `images: true`。两条路径必须先明确分流：附件新路径是“只有 `<source>` 且恰好一张图片”，legacy 路径是“`<source> <assetId>` 且零附件”；同时出现 asset ID 与附件、缺 source 或其他歧义组合全部拒绝：

1. 用户在 DSH 输入 `/preplan-boundary-asset approved_site_plan` 或 `/preplan-boundary-asset approved_redline`，并随命令附带恰好一张图片。
2. 命令执行器先用 DSH 原生附件服务验证格式、字节、像素和批次限制；handler 只接收已持久化的 `ImageBlock`。
3. 附件分支先断言 `invocation.attachments.length === 1`，且唯一 block 为 `type === image`，再使用该 block 的 `attachment` 调用 `attachments.readImage()`；拒绝 GIF、多图、零图和媒体类型不一致，不能把 `ImageBlock` 直接当 attachment ref。显示名允许缺失或为空，但绝不能参与路径解析或真实性判断。
4. 使用项目 ID 与存储图像 SHA 生成稳定的内部 evidence asset ID，把验真后的图像字节写入项目 evidence 目录；显示名只能作为元数据，绝不能解释为本地路径。显示名缺失或为空时，文件名必须由 attachment ID 与经验证 MIME 确定性生成，不得把空显示名直接写入要求非空 `fileName` 的 legacy 合同。
5. 创建 `kind = evidence` 的候选资产与 `pending_confirmation` 边界记录；此时不得写 `adoptedRevision`，不得进入正式报告。
6. `/preplan-boundary-confirm <boundaryId>` 再次核对项目、Revision、附件引用、存储文件 SHA 和 actor；成功后采用 evidence asset 并确认正式边界。

所有校验必须在业务记录写入前完成。稳定 asset ID 使同一项目、同一验真图像的重试幂等；失败时允许留下未采用的候选文件，但不得留下已确认边界或可被正式报告消费的 adopted asset。

legacy `<source> <assetId>` 只兼容命令语法，不继承旧资产的信任等级。可确认的 asset ID 必须指向具备不可变人类附件血缘的 `kind = evidence` 资产，并能回溯 attachment ID、验真存储图像 SHA、提交 actor、提交 Revision 和 `origin = user_image`；存在 provider/model/promptSummary、synthetic/test fixture 血缘，或无法证明人类附件来源的 evidence/deterministic 资产，只能用于研究，禁止确认。缺少新血缘的历史资产必须重新上传，或经过独立、受控并保留人工声明的迁移流程；`sourceKind` 只是客户投影标签，不具有任何信任或确认权限。

项目负责人确认前必须看到验真图像，并显式确认“该图是本项目采用的总平图或红线图，且图中明确表达项目边界”。仅区位底图、普通在线地图截图、地点截图或无明确项目边界的图片只能作为研究资料，不得形成正式边界。系统不自动判断图像的法定效力或语义真伪，但必须保存该人工声明、对应图像 SHA 和确认 Revision；缺少该声明时确认必须失败。

### 13.4 坐标与 GeoJSON 输入链

`/preplan-boundary-coordinates` 保持一个入口，输入为 `<CRS> <闭合坐标数组或 GeoJSON Polygon>`。实现必须：

- CRS 至少符合 `EPSG:<正整数>`；`EPSG:4326` 与 `EPSG:4490` 额外校验经纬度范围。其他 EPSG 代码只保存标识，不声称系统已验证其单位或投影参数。
- 最多 5000 个有效顶点；拒绝 NaN、Infinity、少于三个不同点、零面积、开环、自相交、FeatureCollection、MultiPolygon、多环和洞。
- 规范化连续重复点与 `-0`，统一环方向，并把环起点旋转到稳定位置；等价环的 geometry SHA 必须相同。
- 明确按 `X=东向/经度、Y=北向/纬度` 解释坐标。首版不进行 CRS 转换和真实测距，不输出未经验证的面积、长度或法定坐标成果。

合法几何在提交阶段生成确定性边界图候选，便于人在确认前检查：固定 `1600×1000` viewBox、固定边距和小数格式，只呈现项目边界、图例、指北针、来源日期、CRS 与 `NTS`，不加载在线底图，也不虚构道路、建筑、地块或周边资源。规范化几何计算 `geometrySha256`；SVG 文件另算文件 SHA，并以 `boundaryGeometrySha256 = geometrySha256` 建立血缘，二者不得混同。

同一项目、同一 `source + origin + normalized geometry SHA` 的几何重试必须复用稳定 boundary ID 与候选资产，不得重复创建多个 pending 记录；图片路径同理使用项目、source、origin 与存储图像 SHA 作为幂等键。

SVG 是第一阶段权威派生源。HTML/PDF/PPTX 必须使用同一边界身份；PowerPoint 真实 Office 渲染是验收门槛。若简单 SVG 在 Office 中出现空框或兼容性问题，该迭代必须新增固定版本、固定尺寸的确定性 PNG 派生链并重新全页复核，不能把未验证的浏览器截图当正式资产。

### 13.5 报告绑定与导出门禁

确认边界属于治理输入，不能依赖客户 profile 手填正确 asset ID。报告源必须在消费 profile 视觉绑定前，从唯一治理边界构造并注入唯一权威的 `site-boundary` 资产绑定：

- 文件来源绑定已确认的 evidence asset 与存储图像 SHA。
- 坐标/GeoJSON 来源绑定确定性派生图、派生图 SHA 与 geometry SHA。
- profile 缺失 site-boundary binding 时由报告源注入；profile 无权创建、删除、改名或替换该 binding。
- profile 出现重复 site-boundary、不同 asset ID、不同 SHA、不同 geometry lineage、`sourceKind = ai-concept` 或其他冲突时，必须在 staging 前失败，不能静默忽略。
- 最终 ClientReport 中必须且只能存在一个治理注入的 site-boundary 资产；所有引用项目边界的页面统一绑定该资产。

`/preplan-export` 必须在 profile、分页、渲染和 staging 目录创建之前完成以下检查：确认状态、actor、Revision、项目归属、origin 非 synthetic、来源资产存在且已采用、文件 SHA/geometry SHA/派生图 lineage 一致。任何一项失败都返回统一的人类可理解提示，不产生 `.staging-*`、manifest 或新报告包。HTML、PPTX、PDF 使用同一 `siteBoundaryIntegrityDigest`；客户正文只显示“项目边界：已由项目资料确认”及必要来源信息，不暴露 attachment ID、内部 asset ID、SHA、Revision 或确认日志。

### 13.6 界面与交互

第一阶段不新建地图界面，复用 DSH 标准图片附件输入。项目状态卡增加“场地边界”摘要：`未提供 / 待确认 / 已确认`、来源类型和下一步动作；synthetic 必须单独显示为“模拟研究范围（不可正式确认）”，不得伪装成普通待确认。Agent 遇到具体场地分析时先询问人选择图片、闭合坐标或 GeoJSON；没有输入时只生成研究范围说明，不得静默进入正式发布。

项目名称、地址、行政区、经纬度中心点、自然语言地点描述、`PS01.location`、地理编码结果、普通地图截图及 Agent 推断，均不得创建或确认 `SiteBoundaryRecord`，也不得计算正式 geometry SHA；它们只能作为研究区位信息。状态卡与研究预览必须复用同一状态推导函数，显式区分 `not_provided | pending_confirmation | confirmed_formal_boundary | synthetic_research`：未提供显示“尚未提供场地边界”，待确认显示“已收到边界资料，待项目负责人确认”，synthetic 显示“模拟研究范围，不可作为正式边界”；正式成果只能消费 confirmed，且客户正文不显示内部状态枚举。

命令不得把完整坐标和附件引用重复写进 `command/run` 文本日志；坐标命令应使用 `recordInput: false`，由治理记录承担权威载荷。错误提示必须区分：缺附件、附件数量错误、格式不支持、CRS 无效、几何未闭合、几何自相交、权限不足、Revision 冲突、证据文件丢失、哈希不一致和正式发布缺确认。

### 13.7 文件边界与合同同步

实施至少涉及以下责任单元，具体命名可在计划中微调，但不得把所有逻辑继续堆进 `commands/register.ts`：

- `src/governance/site-boundary-service.ts`：提交、确认、选择和完整性摘要。
- 新的边界证据存储单元：读取 DSH 图片附件、写项目 evidence 文件、计算存储 SHA、建立幂等 evidence asset。
- 新的几何规范化与边界 SVG 渲染单元：纯函数校验、规范化、geometry SHA、确定性 SVG。
- `src/commands/register.ts`：只负责命令语法、actor/context 获取和服务调用。
- `src/report/source.ts` 与 `src/report/client-projection.ts`：从治理边界建立权威客户绑定。
- `src/session/events.ts` 与客户状态卡：显示边界摘要，不暴露内部审计字段。
- `contracts/v0.7/governance/`：新增 site-boundary Schema，并把 `boundaryGeometrySha256`、附件来源元数据与运行时 Zod/TypeScript 合同同步到 manifest；禁止继续存在“运行时接受、JSON Schema 不认识”的漂移。

### 13.8 TDD 与实际验收

生产代码前必须先出现失败测试，至少覆盖：

1. 附件新路径仅接受恰好一张 PNG/JPEG/WebP；该路径的零图、多图、GIF、伪 MIME 均零写入。legacy `<source> <assetId>` 路径不携带图片，按其既有资产与新增血缘校验执行；asset ID 与附件并存必须拒绝。
2. DSH attachment ref 能读取验真字节，稳定生成 evidence asset、存储 SHA 和 pending 边界；相同输入重试幂等。
3. legacy `<source> <assetId>` 命令语法保持兼容；跨项目、concept、candidate、缺失资产、deterministic/AI 血缘、synthetic/test fixture 血缘、缺少人类附件血缘或缺少人工确认声明继续拒绝。
4. 坐标与 GeoJSON 的闭合、有限数、顶点上限、零面积、自相交、洞、多面、CRS 和稳定规范化哈希。
5. 确定性 SVG 内容、文件 SHA、geometry SHA、`boundaryGeometrySha256` 及跨 HTML/PPTX/PDF identity。
6. Agent、ProposalEnvelope、tool 和 automation 无权提交/确认；角色和人工交互通道不可由 raw input 伪造；synthetic 无法确认、复制或重放升级，确认前资产不能正式采用，future Revision 不可用。
7. 治理边界覆盖 profile 伪绑定；缺确认、文件丢失、SHA 漂移和血缘错配均在 staging 前失败。
8. 重启后 pending/confirmed 状态、附件来源、派生图和导出门禁保持一致。

工程验证必须包含定向 RED→GREEN、全量测试、typecheck、built-package、`git diff --check` 和独立复审。真实 DSH 验收分两层：

- 当前可用自定义夹具：在不进入生产 bundle、raw input 无法设置的 `synthetic_fixture` capability 下，通过真实命令上传测试 PNG、提交自定义坐标/GeoJSON、看到 synthetic research 状态和派生图；确认尝试必须失败，`/preplan-export` 必须在 staging 前 fail-closed。该测试不得冒充真实红线。
- 正式项目 E2E：必须由人提供真实图像或几何并亲自确认后，才可验证正式导出、三格式下载、全页视觉检查和重启恢复。在取得真实资料前，整体状态必须保持 PARTIAL。

以上 13.1—13.8 是本轮场地输入子系统的实施规格；用户书面确认后才能进入实施计划和生产代码。

---

## 14. 下一位 Agent 的执行顺序

### 14.1 场地输入 MVP Implementation Plan（用户已于 2026-08-30 书面确认）

> **For agentic workers:** REQUIRED SUB-SKILL: 使用 superpowers:subagent-driven-development 逐任务实施；每项先规格复审，再实现，再代码质量复审。所有步骤用本节复选框追踪。

**Goal：** 在不引入地图 UI 和通用文件平台的前提下，完成“DSH 图片附件或闭合坐标/单环 GeoJSON → pending → 人工确认 → 权威场地边界资产 → 三格式报告”的闭环，并保证 synthetic 测试资料永远不能冒充正式项目边界。

**Architecture：** 边界输入拆成四个单一责任单元：附件/派生图资产存储、纯函数几何规范化与 SVG、治理状态机、报告/界面投影。命令层只解析语法并注入 Host 人工交互上下文；治理记录是唯一权威，profile 不能创建或覆盖 site-boundary binding；正式导出在创建 package root 和 staging 前完成 fail-closed 预检。

**Tech Stack：** TypeScript 5.9、Zod 4、DSH 0.1.1-rc.2 Command/Attachment/Storage API、Vitest 3、Node crypto/fs、PptxGenJS 4；第一阶段不新增 GIS、地图或光栅化依赖。

**Spec：** 本文件第 13.1—13.8 节。

#### Global Constraints

- 首版图片只接受 PNG、JPEG、WebP；GIF、PDF、DWG/DXF、SHP、ZIP 和任意二进制文件继续拒绝。
- 几何只接受带 CRS 的闭合坐标数组，或 type=Polygon、单外环、无洞的 GeoJSON geometry；最多 5000 个有效顶点。
- 不做 CRS 转换、真实测距、法定面积、在线底图、地图圈地、地址地理编码或道路建筑识别。
- origin 不进入命令 grammar、raw input 或公开业务 payload；生产命令固定 user_*，测试 capability 固定 synthetic。
- synthetic_fixture 的实现入口只存在于 tests/scripts 的夹具适配器，不写入生产 cordis.patch.yml，也不进入正式 tgz。
- 当前 DSH 无独立自然人身份服务，按单用户受信任部署验收；不得宣称多用户角色鉴权。
- package.json 版本保持 0.7.0；不得提交、暂存、推送、合并、升版或发布 Release。
- 保留当前脏 worktree 全部既有改动；每个任务只修改其列出的责任文件。
- 没有用户真实总平图、红线图或真实坐标时，正式项目 E2E 与总体状态保持 PARTIAL。

#### 文件责任图

| 单元 | 文件 | 唯一责任 |
|---|---|---|
| 持久化合同 | src/governance/types.ts、domain.ts、repository.ts | origin、附件血缘、派生图血缘、synthetic 不变量 |
| 状态推导 | 新建 src/governance/site-boundary-status.ts | 四态摘要与当前 Revision 选择 |
| 几何 | 新建 src/governance/site-boundary-geometry.ts | 输入解析、校验、规范化、geometry SHA |
| 边界图 | 新建 src/governance/site-boundary-svg.ts | 固定 1600×1000 的确定性 NTS SVG |
| 边界资产 | 新建 src/governance/site-boundary-asset-store.ts | DSH 图片验真字节、evidence/SVG 文件、文件 SHA、幂等 |
| 状态机 | src/governance/site-boundary-service.ts | 提交、确认、采用、selector、正式完整性预检 |
| 命令/Host | src/commands/register.ts、src/index.ts | 新旧语法分流、attachments、recordInput、依赖注入 |
| 报告 | src/report/types.ts、source.ts、client-types.ts、client-projection.ts、package-service.ts | 唯一治理 binding、四态预览、staging 前阻断 |
| 状态卡 | src/session/events.ts、src/client/PreplanningDashboard.tsx | 未提供/待确认/已确认/synthetic 展示 |
| v0.7 合同 | contracts/v0.7/governance/*.json、manifest.json | JSON Schema 与运行时类型同步 |

---

### Task 1：锁定领域类型、运行时不变量与四态推导

**Files：**

- Modify: src/governance/types.ts
- Modify: src/governance/domain.ts
- Modify: src/governance/repository.ts
- Create: src/governance/site-boundary-status.ts
- Create: tests/site-boundary-fixture.ts
- Create: tests/site-boundary-domain.spec.ts
- Modify: tests/governance-repository.spec.ts

**Interfaces：**

- Produces: SiteBoundaryOrigin、SiteBoundarySubmissionChannel、SiteBoundaryAttachmentEvidence、SiteBoundaryGeometryRecord、SiteBoundaryStateSummary。
- Produces: deriveSiteBoundaryState(records, revision)。
- Produces: tests/site-boundary-fixture.ts 中的 siteBoundaryFixture(overrides)，作为后续所有测试唯一合法边界夹具。
- Consumed by: Tasks 3—6。

- [ ] **Step 1：先写运行时合同 RED**

~~~ts
it('rejects synthetic formal boundaries at the storage boundary', async () => {
  await expect(governance.putSiteBoundary({
    ...syntheticPending,
    status: 'confirmed_formal_boundary',
    confirmedBy: owner,
    confirmedAt: now,
    confirmedRevision: 4,
    confirmationChannel: 'dsh_human_command',
    confirmationStatement: '项目负责人确认采用当前边界表达',
  })).rejects.toThrow(/synthetic.*formal/iu)
})
~~~

- [ ] **Step 2：运行 RED**

Run: pnpm exec vitest run tests/site-boundary-domain.spec.ts tests/governance-repository.spec.ts --maxWorkers=1

Expected: FAIL，原因是 origin、submissionChannel、附件/派生图血缘和 deriveSiteBoundaryState 尚不存在。

- [ ] **Step 3：实现明确类型**

~~~ts
export type SiteBoundaryOrigin =
  | 'user_image'
  | 'user_coordinates'
  | 'user_geojson'
  | 'synthetic'

export type SiteBoundarySubmissionChannel =
  | 'dsh_human_command'
  | 'synthetic_fixture'

export interface SiteBoundaryAttachmentEvidence {
  readonly origin: 'user_image'
  readonly attachmentId: string
  readonly mediaType: 'image/png' | 'image/jpeg' | 'image/webp'
  readonly displayName?: string
  readonly bytes: number
  readonly width: number
  readonly height: number
  readonly storageSha256: string
  readonly submittedBy: ActorRef
  readonly submittedRevision: number
}

export interface SiteBoundaryGeometryRecord {
  readonly crs: string
  readonly coordinates: readonly (readonly [number, number])[]
  readonly sha256: string
  readonly derivedAssetId: string
  readonly derivedFileName: string
  readonly derivedSha256: string
}
~~~

VisualAssetRecord 增加可选 boundaryEvidence；SiteBoundaryRecord 必须增加 origin、submissionChannel，并让图片来源携带冻结后的 attachment evidence，让几何来源携带完整派生图 identity。confirmed_formal_boundary 必须同时满足 origin 非 synthetic、confirmationChannel=dsh_human_command、decision_owner、确认时间/Revision/固定人工声明齐全。

- [ ] **Step 4：建立唯一测试夹具**

~~~ts
const owner = {
  actorId: 'owner-1',
  name: '项目负责人',
  role: 'decision_owner' as const,
}

export function siteBoundaryFixture(
  overrides: Partial<SiteBoundaryRecord> = {},
): SiteBoundaryRecord {
  return {
    boundaryId: 'boundary-1',
    projectId: 'project-1',
    submittedRevision: 4,
    status: 'pending_confirmation',
    source: 'approved_redline',
    origin: 'user_image',
    submissionChannel: 'dsh_human_command',
    sourceAsset: {
      assetId: 'boundary-evidence-1',
      fileName: 'project-1/evidence/boundary-evidence-1.png',
      sha256: 'a'.repeat(64),
      attachment: {
        origin: 'user_image',
        attachmentId: 'attachment-1',
        mediaType: 'image/png',
        displayName: 'redline.png',
        bytes: 68,
        width: 1,
        height: 1,
        storageSha256: 'a'.repeat(64),
        submittedBy: owner,
        submittedRevision: 4,
      },
    },
    submittedBy: owner,
    submittedAt: '2026-08-30T10:00:00.000Z',
    ...overrides,
  }
}
~~~

- [ ] **Step 5：实现统一四态函数**

~~~ts
export type SiteBoundaryStateSummary =
  | { readonly kind: 'not_provided'; readonly label: '尚未提供场地边界'; readonly nextAction: string }
  | { readonly kind: 'pending_confirmation'; readonly boundaryId: string; readonly source: SiteBoundarySource; readonly label: string; readonly nextAction: string }
  | { readonly kind: 'confirmed_formal_boundary'; readonly boundaryId: string; readonly source: SiteBoundarySource; readonly label: string; readonly nextAction: string }
  | { readonly kind: 'synthetic_research'; readonly boundaryId: string; readonly source: SiteBoundarySource; readonly label: '模拟研究范围（不可正式确认）'; readonly nextAction: string }

export function deriveSiteBoundaryState(
  records: readonly SiteBoundaryRecord[],
  revision: number,
): SiteBoundaryStateSummary
~~~

- [ ] **Step 6：运行 GREEN 与回归**

Run: pnpm exec vitest run tests/site-boundary-domain.spec.ts tests/governance-repository.spec.ts tests/report-source.spec.ts --maxWorkers=1

Expected: PASS；旧记录不能因缺 origin 被误选为正式边界。

- [ ] **Checkpoint：** 只记录测试结果，不提交 Git。

### Task 2：实现闭合坐标/单环 GeoJSON 规范化与确定性 SVG

**Files：**

- Create: src/governance/site-boundary-geometry.ts
- Create: src/governance/site-boundary-svg.ts
- Create: tests/site-boundary-geometry.spec.ts
- Create: tests/site-boundary-svg.spec.ts

**Interfaces：**

- Produces: normalizeSiteBoundaryGeometry(crs, payload)。
- Produces: renderSiteBoundarySvg(input)。
- Consumed by: Tasks 3、4。

- [ ] **Step 1：写几何规范化 RED**

~~~ts
it('gives cyclic and reversed equivalent rings one geometry SHA', () => {
  const a = normalizeSiteBoundaryGeometry('EPSG:4490', [[114, 30], [114.01, 30], [114.01, 30.01], [114, 30]])
  const b = normalizeSiteBoundaryGeometry('EPSG:4490', [[114.01, 30.01], [114.01, 30], [114, 30], [114.01, 30.01]])
  expect(b.coordinates).toEqual(a.coordinates)
  expect(b.sha256).toBe(a.sha256)
})

it('rejects a self-intersecting polygon', () => {
  expect(() => normalizeSiteBoundaryGeometry(
    'EPSG:4490',
    [[114, 30], [114.02, 30.02], [114, 30.02], [114.02, 30], [114, 30]],
  )).toThrow('SITE_BOUNDARY_GEOMETRY_SELF_INTERSECTION')
})
~~~

覆盖 open ring、NaN/Infinity、少于 3 个不同点、零面积、连续重复点、-0、5001 顶点、4326/4490 范围、未知 EPSG 标识、FeatureCollection、MultiPolygon、多环与洞。

- [ ] **Step 2：运行几何 RED**

Run: pnpm exec vitest run tests/site-boundary-geometry.spec.ts --maxWorkers=1

Expected: FAIL with module not found。

- [ ] **Step 3：实现纯函数**

~~~ts
export interface NormalizedSiteBoundaryGeometry {
  readonly source: 'closed_coordinates' | 'geojson'
  readonly crs: string
  readonly coordinates: readonly (readonly [number, number])[]
  readonly sha256: string
}

export function normalizeSiteBoundaryGeometry(
  crs: string,
  payload: unknown,
): NormalizedSiteBoundaryGeometry

export interface RenderedBoundarySvg {
  readonly svg: string
  readonly sha256: string
  readonly width: 1600
  readonly height: 1000
}

export function renderSiteBoundarySvg(input: {
  readonly projectName: string
  readonly sourceDate: string
  readonly geometry: NormalizedSiteBoundaryGeometry
}): RenderedBoundarySvg
~~~

实现顺序固定为：解析 CRS → 解析数组或 Polygon → 检查输入闭环 → 去连续重复点与 -0 → 顶点/范围/有限数 → 自相交与面积 → 统一逆时针 → 旋转至字典序最小起点 → 闭环 → 对 {crs,coordinates} 计算 SHA-256。

- [ ] **Step 4：写 SVG RED**

~~~ts
it('renders one deterministic 1600x1000 NTS boundary sheet', () => {
  const first = renderSiteBoundarySvg({
    projectName: '自定义测试项目',
    sourceDate: '2026-08-30',
    geometry,
  })
  const second = renderSiteBoundarySvg({
    projectName: '自定义测试项目',
    sourceDate: '2026-08-30',
    geometry,
  })
  expect(first).toEqual(second)
  expect(first.svg).toContain('viewBox="0 0 1600 1000"')
  expect(first.svg).toContain('NTS')
  expect(first.svg).toContain('EPSG:4490')
  expect(first.svg).toContain('项目边界')
  expect(first.svg).not.toMatch(/道路|建筑|地块|高德|百度|Mapbox/iu)
})
~~~

- [ ] **Step 5：实现 SVG 并运行 GREEN**

Run: pnpm exec vitest run tests/site-boundary-geometry.spec.ts tests/site-boundary-svg.spec.ts --maxWorkers=1

Expected: PASS；SVG 固定宽高、边距、三位小数、图例、指北针、CRS、日期、NTS，不含在线资源。

- [ ] **Checkpoint：** 独立复审几何起点/方向、自相交相邻边豁免与 SVG 可重复 SHA，不提交 Git。

### Task 3：实现 DSH 图片证据与派生图的幂等资产存储

**Files：**

- Create: src/governance/site-boundary-asset-store.ts
- Create: tests/site-boundary-asset-store.spec.ts
- Modify: src/index.ts

**Interfaces：**

- Consumes: DSH AttachmentStore.readImage、Task 1 类型、Task 2 SVG。
- Produces: SiteBoundaryAssetStore.ingestImage、saveGeometrySvg、verifyVisualAsset、resolveAsset。
- Consumed by: Tasks 4、5。

- [ ] **Step 1：写图片附件 RED**

~~~ts
it('reads one verified DSH image and stores a stable evidence asset', async () => {
  const first = await store.ingestImage({
    projectId: 'project-1',
    source: 'approved_redline',
    block: imageBlock,
    actor: owner,
    submittedRevision: 7,
    signal: AbortSignal.timeout(1000),
  })
  const second = await store.ingestImage({
    projectId: 'project-1',
    source: 'approved_redline',
    block: imageBlock,
    actor: owner,
    submittedRevision: 7,
    signal: AbortSignal.timeout(1000),
  })
  expect(second).toEqual(first)
  expect(first.kind).toBe('evidence')
  expect(first.status).toBe('candidate')
  expect(first.boundaryEvidence?.storageSha256).toBe(first.sha256)
})
~~~

同一文件增加 GIF、伪 MIME、ref/readImage 元数据不一致、空字节、文件 SHA 漂移和路径逃逸负例。

- [ ] **Step 2：运行资产 RED**

Run: pnpm exec vitest run tests/site-boundary-asset-store.spec.ts --maxWorkers=1

Expected: FAIL with module not found。

- [ ] **Step 3：实现存储接口**

~~~ts
export interface IngestBoundaryImageInput {
  readonly projectId: string
  readonly source: 'approved_site_plan' | 'approved_redline'
  readonly block: ImageBlock
  readonly actor: ActorRef
  readonly submittedRevision: number
  readonly signal: AbortSignal
}

export interface SaveBoundarySvgInput {
  readonly projectId: string
  readonly source: 'closed_coordinates' | 'geojson'
  readonly geometrySha256: string
  readonly svg: string
}

export class SiteBoundaryAssetStore {
  constructor(
    root: string,
    attachments: Pick<AttachmentStore, 'readImage'>,
    now: () => string,
  )

  ingestImage(input: IngestBoundaryImageInput): Promise<VisualAssetRecord>
  saveGeometrySvg(input: SaveBoundarySvgInput): Promise<VisualAssetRecord>
  verifyVisualAsset(asset: VisualAssetRecord): Promise<void>
  resolveAsset(fileName: string): string
}
~~~

图片 asset ID 使用 projectId、source、storage SHA 的 SHA-256 前缀；几何 asset ID 使用 projectId、source、geometry SHA。文件只写入 project/evidence 或 project/deterministic；若目标已存在则读取并核对 SHA，内容相同复用，内容不同 fail-closed。显示名只进元数据；文件名只由稳定 asset ID 和验证 MIME 生成。

- [ ] **Step 4：运行 GREEN**

Run: pnpm exec vitest run tests/site-boundary-asset-store.spec.ts tests/host-apply.spec.ts --maxWorkers=1

Expected: PASS；Host 只注入 ctx.attachments.readImage，不增加泛型文件 API。

- [ ] **Checkpoint：** 检查 tests/scripts 之外没有 synthetic capability 实例，不提交 Git。

### Task 4：重构治理状态机与三个公开命令

**Files：**

- Modify: src/governance/site-boundary-service.ts
- Modify: src/commands/register.ts
- Modify: src/index.ts
- Create: tests/site-boundary-command-flow.spec.ts
- Modify: tests/site-boundary-service.spec.ts
- Modify: tests/commands.spec.ts
- Modify: tests/host-apply.spec.ts
- Create: tests/support/synthetic-boundary-context.ts

**Interfaces：**

- Consumes: Tasks 1—3。
- Produces: registerImageAttachment、registerLegacyAsset、registerGeometry、confirm、selectConfirmedSiteBoundary、assertFormalBoundaryIntegrity。
- Consumed by: Tasks 5—7。

- [ ] **Step 1：写命令分流 RED**

~~~ts
expect(assetCommand?.input).toEqual({
  hint: '<approved_site_plan|approved_redline> [assetId]',
  images: true,
})
expect(coordinateCommand?.recordInput).toBe(false)

await expect(assetCommand?.handler({
  ...invocation,
  rawInput: 'approved_redline',
  attachments: [imageBlock],
} as never)).resolves.toMatchObject({
  kind: 'success',
  text: expect.stringContaining('待确认'),
})

await expect(assetCommand?.handler({
  ...invocation,
  rawInput: 'approved_redline legacy-asset',
  attachments: [imageBlock],
} as never)).resolves.toMatchObject({ kind: 'error' })
~~~

- [ ] **Step 2：写状态机 RED**

~~~ts
await expect(syntheticService.confirm(
  'project-1',
  pending.boundaryId,
  7,
  syntheticContext,
)).rejects.toThrow('SITE_BOUNDARY_SYNTHETIC_NOT_CONFIRMABLE')

await expect(service.confirm(
  'project-1',
  pending.boundaryId,
  7,
  humanContext,
)).resolves.toMatchObject({
  status: 'confirmed_formal_boundary',
  confirmationChannel: 'dsh_human_command',
  confirmationStatement: '项目负责人确认采用当前边界表达',
})
~~~

- [ ] **Step 3：运行联合 RED**

Run: pnpm exec vitest run tests/site-boundary-service.spec.ts tests/commands.spec.ts tests/site-boundary-command-flow.spec.ts tests/host-apply.spec.ts --maxWorkers=1

Expected: FAIL，证明附件入口、GeoJSON、synthetic、采用闭环和错误分类未实现。

- [ ] **Step 4：实现固定执行上下文**

~~~ts
export interface SiteBoundaryExecutionContext {
  readonly actor: ActorRef
  readonly channel: 'dsh_human_command' | 'synthetic_fixture'
}

function originFor(
  channel: SiteBoundaryExecutionContext['channel'],
  userOrigin: Exclude<SiteBoundaryOrigin, 'synthetic'>,
): SiteBoundaryOrigin {
  return channel === 'synthetic_fixture' ? 'synthetic' : userOrigin
}
~~~

生产 registerPreplanningCommands 只能构造 dsh_human_command；tests/support/synthetic-boundary-context.ts 构造 synthetic_fixture，且该文件不在 package.json files 中。

- [ ] **Step 5：实现提交/确认**

~~~ts
registerImageAttachment(
  projectId: string,
  input: {
    readonly source: 'approved_site_plan' | 'approved_redline'
    readonly block: ImageBlock
    readonly submittedRevision: number
    readonly signal: AbortSignal
  },
  context: SiteBoundaryExecutionContext,
): Promise<SiteBoundaryRecord>

registerLegacyAsset(
  projectId: string,
  input: {
    readonly source: 'approved_site_plan' | 'approved_redline'
    readonly assetId: string
    readonly submittedRevision: number
  },
  context: SiteBoundaryExecutionContext,
): Promise<SiteBoundaryRecord>

registerGeometry(
  projectId: string,
  input: {
    readonly crs: string
    readonly payload: unknown
    readonly submittedRevision: number
    readonly projectName: string
  },
  context: SiteBoundaryExecutionContext,
): Promise<SiteBoundaryRecord>

confirm(
  projectId: string,
  boundaryId: string,
  confirmedRevision: number,
  context: SiteBoundaryExecutionContext,
): Promise<SiteBoundaryRecord>

assertFormalBoundaryIntegrity(
  projectId: string,
  revision: number,
): Promise<{
  readonly record: SiteBoundaryRecord
  readonly asset: VisualAssetRecord
  readonly integrityDigest: string
}>
~~~

新附件先验真并写 candidate evidence，再写 pending；legacy 只接受 adopted evidence、完整 boundaryEvidence、无 provider/model/promptSummary、origin=user_image。几何提交写 candidate deterministic SVG。确认时重新核对项目、Revision、origin、actor/channel、附件/文件/geometry/派生图 SHA；成功后采用资产并写 confirmed。所有失败在边界业务记录写入前发生。

- [ ] **Step 6：实现命令错误分类并运行 GREEN**

Run: pnpm exec vitest run tests/site-boundary-service.spec.ts tests/commands.spec.ts tests/site-boundary-command-flow.spec.ts tests/host-apply.spec.ts --maxWorkers=1

Expected: PASS；缺附件、数量错误、GIF、CRS、开环、自相交、权限、Revision、丢文件、SHA 漂移分别有稳定错误码和中文提示。

- [ ] **Checkpoint：** 复审 command/run 不含完整坐标和 attachment ref，Agent tools 仍只有 preplanning_get_context、preplanning_apply_commands，不提交 Git。

### Task 5：建立唯一治理 binding 与 staging 前正式导出门禁

**Files：**

- Modify: src/report/types.ts
- Modify: src/report/source.ts
- Modify: src/report/client-types.ts
- Modify: src/report/client-projection.ts
- Modify: src/report/package-service.ts
- Modify: src/report/validate-artifacts.ts
- Modify: tests/report-source.spec.ts
- Modify: tests/report-package.spec.ts
- Modify: tests/formal-boundary-artifacts.spec.ts
- Create: tests/site-boundary-profile-binding.spec.ts

**Interfaces：**

- Consumes: Task 1 四态、Task 4 正式 selector/preflight。
- Produces: FrozenSiteBoundary 四态、injectGovernedSiteBoundaryBinding、三格式同一 integrity digest。
- Consumed by: Task 7。

- [ ] **Step 1：写 profile 绕过 RED**

~~~ts
it('injects one governed binding when profile omits it', () => {
  const bundle = createClientReportBundle(formalInput, profileWithoutBoundary)
  expect(bundle.report.assets.filter(asset => asset.analysisKind === 'site-boundary')).toHaveLength(1)
  const confirmed = formalInput.siteBoundary
  if (confirmed.status !== 'confirmed') throw new Error('fixture must contain a confirmed boundary')
  expect(bundle.report.assets.find(asset => asset.analysisKind === 'site-boundary')?.assetId)
    .toBe(confirmed.assetId)
})

it.each([
  profileWithDuplicateBoundary,
  profileWithDifferentBoundaryAsset,
  profileWithConceptBoundary,
  profileWithWrongGeometrySha,
])('fails closed on a conflicting profile boundary', profile => {
  expect(() => createClientReportBundle(formalInput, profile)).toThrow('SITE_BOUNDARY_PROFILE_CONFLICT')
})
~~~

- [ ] **Step 2：写 staging 前门禁 RED**

~~~ts
await expect(service.publish('project-1', 7)).rejects.toThrow('SITE_BOUNDARY_FILE_SHA_MISMATCH')
await expect(access(packageRoot)).rejects.toThrow()
expect((await readdir(parentRoot)).filter(name => name.startsWith('.staging-'))).toEqual([])
~~~

- [ ] **Step 3：运行报告 RED**

Run: pnpm exec vitest run tests/site-boundary-profile-binding.spec.ts tests/report-source.spec.ts tests/report-package.spec.ts tests/formal-boundary-artifacts.spec.ts --maxWorkers=1

Expected: FAIL，证明 profile 仍控制 binding，且文件完整性没有在 mkdir/staging 前重验。

- [ ] **Step 4：实现 Frozen 四态与治理注入**

~~~ts
export type FrozenSiteBoundary =
  | { readonly status: 'not_provided' }
  | { readonly status: 'pending_confirmation'; readonly boundaryId: string; readonly source: SiteBoundarySource }
  | { readonly status: 'synthetic_research'; readonly boundaryId: string; readonly source: SiteBoundarySource; readonly declarations: readonly string[] }
  | { readonly status: 'confirmed'; readonly boundaryId: string; readonly assetId: string; readonly confirmedRevision: number; readonly source: SiteBoundarySource; readonly sourceSha256?: string; readonly geometrySha256?: string; readonly assetSha256: string; readonly integrityDigest: string }
~~~

报告源先读取治理边界并构造权威 binding；profile 缺失时注入，完全相同可接受，重复/不同 ID/SHA/lineage/ai-concept 全部报错。最终报告只能有一个 site-boundary 资产。

- [ ] **Step 5：实现 preflight 顺序**

publish 顺序固定为：safe project ID → governance selector → SiteBoundaryService.assertFormalBoundaryIntegrity → source 四态一致性 → profile 冲突 → client bundle/policy/page plan → package root mkdir → staging → render/validate/rename。

- [ ] **Step 6：运行 GREEN**

Run: pnpm exec vitest run tests/site-boundary-profile-binding.spec.ts tests/report-source.spec.ts tests/report-package.spec.ts tests/formal-boundary-artifacts.spec.ts tests/report-html.spec.ts tests/report-pptx.spec.ts tests/report-pdf.spec.ts --maxWorkers=1

Expected: PASS；HTML/PPTX/PDF 的 siteBoundaryIntegrityDigest、asset SHA、geometry SHA 和边界身份一致。

- [ ] **Checkpoint：** 确认客户正文不显示 attachment ID、asset ID、SHA、Revision 或确认日志，不提交 Git。

### Task 6：同步状态卡、研究预览与 v0.7 JSON Schema

**Files：**

- Modify: src/session/events.ts
- Modify: src/client/PreplanningDashboard.tsx
- Modify: contracts/v0.7/manifest.json
- Create: contracts/v0.7/governance/site-boundary.schema.json
- Modify: contracts/v0.7/governance/visual-asset-manifest.schema.json
- Create: tests/site-boundary-status.spec.ts
- Modify: tests/governance-contracts.spec.ts
- Modify: tests/contracts.spec.ts
- Modify: tests/host-apply.spec.ts

**Interfaces：**

- Consumes: deriveSiteBoundaryState。
- Produces: PreplanningStatusEventData.boundary 与版本化 JSON Schema。

- [ ] **Step 1：写状态卡 RED**

~~~ts
expect(buildPreplanningStatus(context, dependencies).boundary).toEqual({
  kind: 'synthetic_research',
  label: '模拟研究范围（不可正式确认）',
  source: 'geojson',
  nextAction: '请提供真实总平图、红线图或带 CRS 的闭合几何',
})
~~~

- [ ] **Step 2：写合同 RED**

~~~ts
const registry = await GovernanceContractRegistry.open(
  new URL('../contracts/v0.7/', import.meta.url),
)
const syntheticConfirmed = siteBoundaryFixture({
  origin: 'synthetic',
  submissionChannel: 'synthetic_fixture',
  status: 'confirmed_formal_boundary',
  confirmedBy: {
    actorId: 'owner-1',
    name: '项目负责人',
    role: 'decision_owner',
  },
  confirmedAt: '2026-08-30T10:05:00.000Z',
  confirmedRevision: 4,
  confirmationChannel: 'dsh_human_command',
  confirmationStatement: '项目负责人确认采用当前边界表达',
})
const geometryAssetWithoutLineage = {
  assetId: 'geometry-map-1',
  taskId: 'site-boundary-geometry',
  projectId: 'project-1',
  kind: 'deterministic',
  required: true,
  status: 'candidate',
  mimeType: 'image/svg+xml',
  fileName: 'project-1/deterministic/geometry-map-1.svg',
  sha256: 'b'.repeat(64),
  width: 1600,
  height: 1000,
  createdAt: '2026-08-30T10:00:00.000Z',
}
expect(registry.schemaIds()).toContain('site-boundary')
expect(registry.validate('site-boundary', syntheticConfirmed).valid).toBe(false)
expect(registry.validate('visual-asset-manifest', geometryAssetWithoutLineage).valid).toBe(false)
~~~

- [ ] **Step 3：运行 RED**

Run: pnpm exec vitest run tests/site-boundary-status.spec.ts tests/governance-contracts.spec.ts tests/contracts.spec.ts tests/host-apply.spec.ts --maxWorkers=1

Expected: FAIL，状态事件无 boundary，v0.7 manifest 无 site-boundary。

- [ ] **Step 4：实现 UI 与 Schema**

状态卡只显示人类可读三态并把 synthetic 单列为“模拟研究范围（不可正式确认）”；研究预览复用同一 deriveSiteBoundaryState。JSON Schema 使用 additionalProperties=false，要求 origin、channel、附件或几何/派生图血缘，并用 if/then 禁止 synthetic confirmed。

- [ ] **Step 5：运行 GREEN**

Run: pnpm exec vitest run tests/site-boundary-status.spec.ts tests/governance-contracts.spec.ts tests/contracts.spec.ts tests/host-apply.spec.ts --maxWorkers=1

Expected: PASS；运行时 Zod、TypeScript、JSON Schema 与 built types 属性名一致。

- [ ] **Checkpoint：** 对 manifest 八项旧 schema 做回归，新增后数量精确为九，不提交 Git。

### Task 7：集成验证、未升版候选包与真实 DSH synthetic 验收

**Files：**

- Modify: tests/full-flow-golden.spec.ts
- Modify: scripts/build-golden-project.ts
- Create: tests/site-boundary-restart-flow.spec.ts
- Create: tests/fixtures/synthetic-boundary-host.ts
- Create: scripts/build-synthetic-boundary-fixture-package.ts
- Update: 本 HANDOFF 的“当前验证状态”与候选包哈希；不得新建第二份总体文档。

**Interfaces：**

- Consumes: Tasks 1—6。
- Produces: 自动化证据、正式候选 tgz、仅验收用 synthetic fixture tgz、真实 DSH 运行证据。

- [ ] **Step 1：写重启与全链 RED**

测试必须在真实 Storage JSON Domain 上完成：图片附件/坐标/GeoJSON → synthetic_research → 派生 SVG → 关闭并重开仓库 → 状态不变 → confirm 拒绝 → export 在 package root/staging 前拒绝。

- [ ] **Step 2：运行定向 RED/GREEN**

Run: pnpm exec vitest run tests/site-boundary-*.spec.ts tests/commands.spec.ts tests/host-apply.spec.ts tests/formal-boundary-artifacts.spec.ts --maxWorkers=1

Expected: 先 RED，完成 Tasks 1—6 后全部 GREEN。

- [ ] **Step 3：运行完整工程门禁**

~~~powershell
pnpm test
pnpm typecheck
pnpm test:built
git diff --check
~~~

Expected: 全量通过；git diff --check 退出码 0；版本仍为 0.7.0。

- [ ] **Step 4：独立双审**

一名 reviewer 逐条核对 HANDOFF 13.1—13.8 的规格覆盖；另一名 reviewer 核对安全/数据真实性、profile 绕过、synthetic 生产隔离和测试强度。Critical/Important 必须为 0。

- [ ] **Step 5：构建两个不同用途的包**

正式候选 tgz 使用正常 src/index.ts，保持 0.7.0；synthetic fixture tgz 使用 tests/fixtures/synthetic-boundary-host.ts，仅用于真实 DSH 验收。对两个 tgz 分别记录绝对路径、字节数和 SHA-256；解包证明正式 tgz 不含 synthetic fixture entry、tests 或 scripts。

- [ ] **Step 6：备份并进行真实 DSH 验收**

先对 C:\Users\2899\.dsh\profiles\web\node_modules\@architectureworld\dsh-preplanning-agent 做时间戳备份，再安装 synthetic fixture tgz。使用真实 DSH UI：

1. 上传一张自定义 PNG，执行 /preplan-boundary-asset approved_redline。
2. 验证状态为“模拟研究范围（不可正式确认）”并存在 evidence 文件/SHA。
3. 执行一组自定义 EPSG:4490 闭合坐标和一组单环 GeoJSON，查看 1600×1000 派生图。
4. 执行 confirm，必须被 synthetic 门禁拒绝。
5. 执行 /preplan-export，必须在 staging 前拒绝；候选/已采用/孤儿任务计数符合预期。
6. 重启 DSH，再次确认状态、文件与阻断一致。

- [ ] **Step 7：恢复正式候选并复核**

安装正式候选 tgz，重启 DSH，验证 HTTP 200、插件版本 0.7.0、已安装 lib/index.js 与本地正式 build SHA 一致。不得在没有真实用户边界资料时执行正式确认或宣称完整 E2E。

- [ ] **Checkpoint：** 本轮总体结论只能是“工程与 synthetic 真实 DSH 验收通过；正式项目 E2E 等待用户真实资料”，即 PARTIAL；不提交、不推送、不升版、不发布。

#### Plan Self-Review（2026-08-30）

- 规格覆盖：13.1 → Global Constraints；13.2 → Tasks 1/4；13.3 → Tasks 3/4；13.4 → Tasks 2/3；13.5 → Task 5；13.6 → Task 6；13.7 → Tasks 1—6；13.8 → Task 7。
- 占位扫描：无 TBD、TODO、implement later、fill in details 或“参照上一任务”等不可执行语句；文末“无 TODO/TBD”是验收规则本身。
- 类型一致：Task 1 定义 origin/channel/record/status，Task 2 定义 geometry/SVG，Task 3 定义资产输入与 store，Task 4 定义状态机接口，Tasks 5—7 只消费已定义接口。
- 权限一致：用户禁止 Git 提交/推送/升版/发布，故本计划用 Checkpoint 代替 writing-plans 默认的 commit 步骤。

---

### 阶段 A：接棒与核验

1. 阅读本 HANDOFF。
2. 检查 worktree、分支、提交和未提交改动。
3. 重新探测 DSH，而不是复用历史 PID。
4. 网络恢复后核验远端分支和 Release。
5. 确认具体改版项目的受众、媒介、篇幅和数据范围。
6. 对真实项目主动索取并确认总平图、红线图、带 CRS 的闭合红线坐标或兼容 GeoJSON；资料未确认时只能进入带固定声明的研究模式。

### 阶段 B：规格

规格必须包含：

- 客户层/内部层边界
- 内容叙事模型
- 苹果式视觉 DNA 和设计令牌
- 页面类型与节奏
- 图片与章节绑定规则
- 数据分析图表数量、主题覆盖和数据来源合同
- 场地分析必备图件类型与完整性合同
- 正式边界人工输入、确认、revision 冻结与研究范围 fail-closed 合同
- 内容页视觉覆盖率、纯文字连续页和单图面积合同
- HTML/PPTX/PDF 同源及媒介差异
- 图像来源中性与图件语义真实性规则
- 自动化测试、视觉 QA 和真实 E2E

规格通过用户确认后才实施。

### 阶段 C：TDD 实现

先写失败测试，至少覆盖：

- 客户正文禁止 Gate、Workflow、Revision 和内部状态词。
- 治理数据仍存在于内部附件/manifest。
- 三格式叙事结构和数据口径一致。
- 图像与产品、章节和图件语义正确绑定。
- 数据分析图表不少于 6 张且覆盖至少 4 类主题。
- 城市区位、项目边界、现状用地/建筑、交通可达性、内部流线、资源与限制 6 类场地分析缺一不可。
- 正式边界必须来自当前项目已采用文件或带 CRS 的闭合几何，并完成人工确认；缺确认、跨项目、非有限/开口/零面积坐标和 future-only 记录全部 fail-closed。
- 测试研究范围在 HTML、PPTX、PDF 中均显示 `研究范围（待核） · 非法定红线 · 非测绘成果`，且不能被正式发布路径误认。
- 内容页视觉覆盖率不低于 50%，纯文字内容页不得连续超过 2 页。
- 设计令牌、字号底线和安全边距有效。
- 无占位符、缺图、空页、溢出和字体回退。
- 页面布局不过度重复。
- 客户层不显示图像制作工具或生成方式。
- 旧发布校验和包身份不退化。

### 阶段 D：新 Golden 与真实验收

1. 新建 Golden，不覆盖旧基线。
2. 生成 HTML、PPTX、PDF。
3. 将 PPTX 和 PDF 全页渲染成图片。
4. 制作全册 contact sheet/montage。
5. 逐页检查封面、开场、章节幕、证据页、产品页、场景页、实施页和结尾。
6. 在 DSH 中跑完整流程：模式选择 → 任务执行 → 生图 → 采纳 → 发布 → 下载。
7. 保存工程与 QA 证据，但向用户首先展示甲方正式成果。
8. 真实 E2E 和视觉验收通过后再更新版本、分支和 Release。

---

## 15. 双重验收量表

成果必须同时通过“内容产品性”和“苹果式美学”两套门禁，不能用一项高分补偿另一项失败。

### 15.1 内容产品性：100 分

| 维度 | 分值 |
|---|---:|
| 甲方成品定位 | 15 |
| 叙事结构 | 20 |
| 判断与证据 | 20 |
| 产品与场景 | 20 |
| 运营、投资与实施 | 15 |
| 交付完整性 | 10 |

通过线：85 分；90 分以上可直接正式汇报。

### 15.2 苹果式美学：100 分

| 维度 | 分值 |
|---|---:|
| 视觉概念与项目专属性 | 15 |
| 图像选择、裁切与空间表现 | 20 |
| 网格、构图与留白 | 20 |
| 字体、尺度与信息层级 | 15 |
| 色彩、材质与视觉识别 | 10 |
| 图表、地图与专业表达 | 10 |
| 全册节奏与连续性 | 5 |
| 输出细节与视觉 QA | 5 |

通过线：88 分；目标 90 分以上。75—84 分只相当于整洁模板或现有参考资料的及格水平，不能作为本次最终目标。

### 15.3 一票否决项

出现任意一项直接不合格：

- 没有正式 HTML、PPTX 或 PDF。
- 用日志、JSON、状态页或测试证明冒充主成果。
- 没有清晰定位、核心产品或实施闭环。
- 关键数据、案例或项目进展明显虚构。
- 主册出现大量内部治理字段。
- 封面精美、正文退化为表格和卡片。
- 连续三页以上机械使用相同模板。
- 页面没有明确第一视觉主角。
- 图片低清、拉伸、错误裁切或风格严重混乱。
- 地图、总平面或图表缩小到不可读。
- 有效数据分析图表少于 6 张，或覆盖业务主题少于 4 类。
- 城市区位、项目边界、现状用地/建筑、交通可达性、内部流线、资源与限制任一强制场地分析图缺失。
- 正式项目的边界来自自然语言、模型推断、普通地图截图或未经人工确认的圈画；或把待核研究范围表述为法定红线。
- 内容页视觉图件覆盖率低于 50%，或纯文字内容页连续超过 2 页。
- 用场景图、装饰图或错误的角色标签替代地图、数据图表和场地分析图。
- 大量正文低于字号底线。
- 文字溢出、遮挡、低对比、断链、缺图或占位符。
- 图像暗示不存在的建成状态、法定结论、测算结果或项目进展。
- 成果必须依赖制作者口头解释才能理解。

---

## 16. 自动与人工视觉 QA

### 16.1 自动检查

- 文件可打开，资源完整。
- 无 TODO、TBD、XXX、lorem、placeholder。
- 无文字溢出和对象越界。
- 字号不低于设计合同底线。
- 正文不侵入页脚和来源区域。
- 图片文件存在且分辨率满足使用尺寸。
- 主册内部治理禁词扫描为零或只有明确豁免。
- 数据来源、时间、单位和口径完整。
- 数据分析图表数量不少于 6，主题类型不少于 4。
- 6 类强制场地分析图件全部存在且类型不重复冒充。
- 内容页视觉图件覆盖率不低于 50%，纯文字内容页连续不超过 2 页。
- HTML 无控制台错误、断链和横向溢出。

### 16.2 人工渲染检查

必须把全部页面渲染成图片，完成：

1. 全册缩略图节奏检查。
2. 每页 5 秒主次判断。
3. 文字、边距、对齐、裁切、对比度检查。
4. 地图、图表和效果图可读性检查。
5. 封面视觉母题连续性检查。
6. 至少一次“发现问题—修改源文件—重新渲染—复查”的闭环。

第一版渲染不应默认正确。不能用“看起来还行”替代逐页 QA，也不能直接修补生成后的 PPTX 而不修正源模型或渲染器。

---

## 17. Definition of Done

### 17.1 插件

- DSH 可启动，插件真实加载。
- 人工确认/全自动模式符合定义。
- 生图任务可执行、失败可恢复、结果可采纳。
- 报告发布与下载链路真实可用。

### 17.2 甲方成果

- HTML、PPTX、PDF 可打开、内容完整、数据同源。
- 第一观感是苹果式产品发布质感的建筑策划产品册。
- 内容产品性 ≥85，苹果式美学 ≥88。
- 主册无内部 Gate、Revision、Workflow ID 和状态台账。
- 每页有明确结论和视觉主角。
- 数据图表不少于 6 张并覆盖至少 4 类业务主题。
- 城市区位、项目边界、现状用地/建筑、交通可达性、内部流线、资源与限制 6 类场地分析完整。
- 正式项目边界已由人提供并确认，来源为已采用总平图、红线图、带 CRS 的闭合坐标或兼容 GeoJSON；研究模式完整显示固定待核声明且没有冒充法定红线。
- 内容页视觉图件覆盖率不低于 50%，纯文字内容页连续不超过 2 页。
- 场景图、图表、地图与相关章节形成连续证据链。
- PPTX 适合现场讲述，PDF 适合会后传阅，HTML 适合浏览展示。
- 无溢出、变形、空白页、字体缺失、低清图、断链和模板机械重复。

### 17.3 工程

- 类型检查、构建和全量自动化测试通过。
- 客户层与治理层隔离有测试保护。
- Artifact manifest、版本和来源一致性有效。
- 旧 Golden 可追溯，新产物有独立目录、时间和版本。
- worktree 干净，提交、分支、Release 和产物哈希可核验。

---

## 18. 给执行 Agent 的完整指令

以下内容可作为后续规格、实现或生成任务的上层系统约束：

```text
你正在改造 DSH 前期策划插件的甲方成果层。你的最终任务不是生成会议纪要、研究过程、技术台账、状态页面或调试证明，而是生成一份甲方可以直接正式汇报和会后传播的策划型建筑产品册。

内容标准：
主报告采用“背景与命题—现状判断—机会价值—定位愿景—策略体系—产品体系—空间场景—运营投资—实施路径—决策结论”的叙事。每页一个结论，至少80%的内容页使用观点型标题。所有事实注明来源、时间和单位；判断由事实推导；测算公开关键假设；不得编造政策、案例、数据、合作品牌或项目结果。

产品标准：
每个核心产品必须说明名称、客群、内容、场景、空间载体、运营方式和价值贡献。空间方案必须对应问题、客群、产品、活动和实施动作。主报告必须形成从价值到产品、场景和落地的完整闭环。

场地位置与边界输入标准：
具体场地位置、项目边界和法定红线不能靠自然语言或模型推断。正式项目开始场地分析前，必须主动向人索取并确认至少一种：当前项目已采用的总平图、已采用的红线图、带 CRS 的闭合红线坐标或兼容 GeoJSON 的闭合几何。资料缺失时只能使用研究模式，并在图面和 HTML/PPTX/PDF 中完整显示“研究范围（待核） · 非法定红线 · 非测绘成果”；普通地图截图、图上画线和未经确认的地图圈地不能升级为正式红线。未来地图交互可收集人圈选范围，但必须保存 CRS、闭合几何与来源并经人确认后，才能作为项目确认边界使用。

视觉标准：
视觉目标是“苹果式产品发布质感 × 高端建筑编辑设计 × 甲方决策提案”。这不是复制Apple官网，而是贯彻单一焦点、价值优先、巨幅视觉、强烈层级、克制配色、真实材质、连续叙事和精密完成。

每页只能有一个第一视觉主角。优先使用全幅主视觉、一主两辅、60:40/65:35/70:30非对称图文。禁止邮票式图片矩阵和默认四等分圆角卡片。关键地图、总平面、轴测和效果图必须获得足够面积。大文字只用于定位、命题、核心数字和决策结论。

32—48页甲方主册至少包含6张有效数据分析图表，并覆盖现状结构、客群需求、交通可达性、运营投资、实施分期等至少4类主题。每张图表必须有主要结论、直接标注、数据来源、日期、单位和必要口径。数据不足必须阻止正式发布，禁止编造或用场景图替代。

建筑前期策划必须包含城市区位、项目边界、现状用地/建筑、交通可达性、内部流线、资源与限制6类强制场地分析图；景观生态、机会结构、功能落位和分期实施图按项目条件补充。正式场地图必须包含图名、边界性质、项目边界、图例、指北针、比例或NTS、来源和日期，并在项目定位与产品体系之前形成连续分析序列。

除封面、三页开场、章节幕和最终决策大字页外，至少50%的内容页由图像、地图、图表或分析图担任第一视觉主角，纯文字内容页不得连续超过2页。单图优先采用75%—100%全页背景或50%—65%半页大图。

使用12列网格、统一安全边距和8pt间距体系。标题、正文、图片、图注和来源严格对齐。全册最多使用两个字体家族。PPT标题不低于24pt，正文原则上不低于14pt并优先16pt以上。色彩以75%-85%中性色、10%-20%项目主色和约5%强调色构成。禁止默认蓝紫科技渐变、厚重阴影、霓虹、廉价玻璃拟态和随机装饰。

同一版式不得连续使用三页。封面建立的视觉母题必须贯穿正文。技术密集页面之后安排总结或场景页。不能只做漂亮封面，正文必须保持同等完成度。

图像标准：
检查清晰度、裁切、透视、地平线、主体、视线方向、色温、项目适配度和风格一致性。全屏图原则上不低于1920×1080。客户层不区分图像制作方式，不显示制作工具或生成方式；是否注明概念示意只取决于项目阶段和表达内容。场景图、地图、分析图和数据图表必须使用准确的语义角色。

客户层边界：
主报告不得出现Gate、Revision、Workflow ID、工作项、完成度、审批状态、测试日志、JSON、manifest、本地路径和内部调试截图。底层治理事实必须保留在内部附件和QA包中，通过明确的client report projection与客户主册隔离。

媒介标准：
HTML、PPTX、PDF消费同一个客户叙事模型，但按媒介优化。HTML用于沉浸浏览与方案对比，PPTX用于现场讲述，PDF用于正式传阅和归档。三者核心结论、数据口径、章节和资产必须一致。

验收：
最终成果同时通过内容产品性和苹果式美学双门禁。内容低于85分、美学低于88分或出现任一一票否决项均不得宣称完成。正式项目还必须通过人工边界输入与确认门禁；测试研究范围不能冒充法定红线。必须渲染全部页面并检查全册缩略图，至少完成一次发现问题、修改源文件、重新渲染和复查的循环。工程测试、端口在线和日志不能替代甲方成果的真实视觉与业务验收。
```

---

## 19. 常用核验命令

目标 worktree：

```powershell
git status --short --branch
git worktree list --porcelain
git remote -v
git log -5 --oneline --decorate
pnpm typecheck
pnpm build
pnpm test
pnpm test:built
```

DSH 基础探测：

```powershell
Get-NetTCPConnection -LocalPort 3080 -State Listen
Invoke-WebRequest -Uri 'http://127.0.0.1:3080/' -UseBasicParsing
```

端口、HTTP 200 和测试通过都不是甲方成果质量的充分证明；最终必须检查渲染后的全部页面并完成 DSH 真实业务流程。

---

## 20. 严格禁止事项

- 把历史测试数字当作当前通过结果。
- 把 DSH 端口在线当作插件全流程验收。
- 把日志、JSON、manifest、调试页或状态面板当作甲方成果。
- 为了“苹果极简”删除必要证据、技术深度和实施逻辑。
- 直接复制 Apple 品牌、产品图、网页结构或受保护视觉资产。
- 把苹果风误做成全册黑白、大字、毛玻璃和圆角卡片。
- 为了视觉简化删除底层治理和审计事实。
- 修改、覆盖或重新保存 Reference 原始参考资料。
- 覆盖唯一一份 v0.7.0 Golden。
- 泄露 Gemini、Antigravity、DSH 或代理服务的密钥、令牌和认证内容。
- 删除或暂存无关的用户改动。
- 网络不可用时声称远端 GitHub 已实时核验。
- 未经具体实施规格确认就直接重构报告生成器。

---

## 21. 接棒开场建议

下一位 Agent 可以直接向用户说明：

> 我已阅读唯一汇总 HANDOFF。当前目标已经明确：保留 DSH 的治理和自动化能力，将客户成果重构为具有苹果产品发布质感的建筑策划产品册。参考资料的 14 份、1,536 页已经完成深读，内容只作为专业及格线，最终视觉必须明显更高。接下来我会先核验当前 worktree、DSH 与 Golden 状态，再针对具体项目确认受众、篇幅和媒介，提交客户投影层、同源三格式和苹果式视觉系统的实施规格；规格确认后再按 TDD 实现，并以全页渲染、真实 DSH E2E 和双重评分门禁验收。

本 HANDOFF 是本阶段唯一汇总文件。后续若产生实施规格、测试报告或 QA 证据，它们属于执行附件，不得取代本文件，也不得把零散附件重新当作总体成果标准。
