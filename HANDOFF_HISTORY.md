# Historical Handoff Index

> Status: `superseded-history`  
> Version authority: [`docs/version-matrix.json`](docs/version-matrix.json)  
> Current handoff: [`HANDOFF.md`](HANDOFF.md)

The former long-form handoff is intentionally not retained as a current-tree authority because it contains dated branch names, candidate versions, local paths and statements such as “current” or “unique authority” that were valid only at their original timestamps.

Full historical content remains recoverable from Git history:

```text
last full historical handoff commit: 13b1e51b9378ce1436327afef5aa139f8b6b7be1
original blob: ad22becec2a333935c1b4b2c145f5e2deaa0e9fb
original path: HANDOFF.md
```

Historical labels referenced by that material:

| Label | Classification |
|---|---|
| `v0.7.0` | Published historical package/tag baseline |
| `v0.8` | Unreleased historical client-report candidate line |
| `FINAL_v2.0` | Archive-directory label only; not SemVer |
| old branch/worktree names | Dated historical evidence only |

None of these records overrides the current alignment baseline `v2.0.0`, executable package `0.7.0`, published tag `v0.7.0`, Presentation standard line `v1`, Presentation target `standardVersion = 1.0.0`, Contract Lock status `pending`, or implementation status `blocked-by-contract-lock`.
