# 双项目开发基线修订 — 2026-09-07

## 当前决策（优先于此前 Review 中冲突的建议）

1. AI/非现场图像来源只记在内部素材与生成记录中，不要求汇报画面、脚注、导出结果显示 AI 标签或水印；不批量删除用户已有图注。内部仍须区分证据与概念图。
2. Pre-design 拥有内容组织、叙事、视觉选择、排版 Skill 与 Agent 编排；Presentation 拥有编辑界面、工具契约、布局数据、渲染、机械检查、保存和修订记录。DSH 拥有会话、模型和 Agent 执行，不另建 harness。
3. 汇报修改取消 Proposal 二次审批。用户提交本次修改要求即为范围内直接修改授权；候选、版本校验、操作记录与撤销能力不能随审批一起删除。生成图片仍需要明确的补图许可。
4. 只继续已有分支：Pre `feat/pre-v2.0.0`；Studio `feat/report-studio-v0.2.0-layout`。不得新建分支，不合并 main，不发布 Release。

## 本批实际改动

- Pre 新增 `src/prompts/report-design-skill.ts`，从 Studio 迁入结构化排版规则，主系统提示词实际导入该模块；不只是新增一份说明。
- Studio `getDesignRules()` 保留既有 API/资源名称，但内容收窄为工具约束，策略唯一归属指向 Pre，不再保留 `concerns` 和 `designLoop` 两套策略。
- 新宿主设计请求默认采用有效范围内直接修改；旧客户端明确传 `allowApply:false` 时拒绝升级为写入，要求刷新或改用只读入口。
- 新设计 UI 移除自动应用选项与逐项“确认采用”按钮。历史待处理记录不自动采纳，用户重新发起请求后按新规则执行。
- 存储和兼容工具仍可能使用 `proposalId` / `proposals` 作为内部操作记录；本批取消的是新流程中的人工审批，不是对旧存储进行破坏性改名或迁移。
- 专业状态 `ProposalEnvelope`、Gate、事实依据校验保持原合同。它们不应被用于恢复汇报页面的审批步骤；本次未擅自改动专业审批权限。

## 交界面

| 事项 | Pre | Studio |
|---|---|---|
| 专业事实、证据、假设、待确认项 | 权威维护 | 引用、提出修订问题 |
| 大纲、草案、叙事、排版决策 | Skill/Agent 负责判断 | 提供可编辑对象与工具 |
| 布局坐标、样式、真实预览 | 调用工具并观察 | 执行、检查、持久化 |
| 素材 | 专业来源、生成服务、内部 provenance | 登记、引用、挂页、渲染 |
| 人工编辑后上游变化 | 提供来源变化 | 保留本地编辑并处理冲突 |
| Session、模型、授权执行 | 复用 DSH | 复用 DSH |

交换内容沿用标准目录 `0.1.0`；当前页补图沿用 `pre-design.page-visual.v1`。不通过整项目同步补一张图，不直接写对方内部数据库。首次输出可以生成草案；后续更新不得接管用户已经编辑的整份汇报。

## 后续开发队列（尚未完成，不得宣称已修复）

| 优先级 | 任务 | 负责人 | 必须通过的验收 |
|---|---|---|---|
| P0 | 保存后编辑的同步保护 | Studio，Pre 配合 | 修改并保存标题/拆合页后再接收上游变化，不能静默覆盖；先做基线偏离阻断，再做按内容块合并 |
| P0 | 批注逐条完成 | Studio | 三条批注仅处理一条时显示 1 完成/2 未完成；提交后改写的批注不能被旧任务关闭 |
| P0 | 应用冲突与失败恢复 | Studio | LLM 返回期间用户保存，旧 CAS 被拒绝且任务进入可恢复状态；无卡死、无二次重复应用 |
| P0 | 现有 Windows CI 与完整构建 | Studio | 定位测试隔离路径 ancestry 错误，保留安全检查；Linux/Windows 完整流水线均产出实际结果 |
| P1 | 补图两阶段续接 | 两边 | Pre 已采用而 Studio 未挂页时只续接挂页；保持 requestId/brief/assetId，不重复付费或重复挂图 |
| P1 | 拆合页的受控范围继承 | Studio，Pre 消费 | 仅从本轮已授权且未保护来源页派生的新页继承权限；不能扩大到无关页面 |
| P1 | 事实与编辑归纳的区分 | Pre，Studio 展示 | 数值/单位/关键限定发生变化时有受控专业修订，不把来源 ID 匹配当成事实正确 |
| P1 | README、安装与版本入口统一 | 两边 | 根包、插件包、Node 下限、当前分支和真实安装包一致；旧 Schema 版本不因产品版本变动随意改写 |
| P1 | 双插件真实 DSH 联合验收 | 两边 | 同一对安装包完成读页、真实图像、修改、保存、重开、冲突及恢复，能关联到实际版本 |
| P2 | 完整报告与成品输出 | Pre 策略 / Studio 工具 | 叙事、时长、附录由 Pre 决定；Studio 输出可验证的成品，不另做排版 Agent |

## 验证边界

本批在受限环境进行了定向 Node 测试；Pre 的 Vitest 测试体使用 Node test runner 重放，另外执行新增提示词模块的 TypeScript 定向类型检查。这不等于整个仓库的 `pnpm test` / `npm run verify:all` 通过。完整依赖安装、浏览器、DSH、双插件装包验收以当前提交的真实 CI/宿主记录为准。历史提交的绿色结果不得充当新提交的通过证据。

当前产品包版本仍为 Pre `2.0.0`、Studio `0.2.0-alpha.3`，Node 基线 `>=24.11.0`，标准合同 `0.1.0`；本批是现有开发分支内的修订，不是新 Release。

## 本次执行补充：报告 Skill 1.0.1

在已提交的 Skill 1.0.0 上补齐了 Studio 实际工具协议：annotationResults 的精确字段、commands=[]/no_changes、scopeInherited/newPageIds、local_saved_conflict、conflict/apply_failed 的新版重试，以及 link_failed 复用同一 proposalId。主系统提示词仍实际导入该 Skill。产品包保持 2.0.0，Skill 内部版本为 1.0.1；不改专业 Gate/ProposalEnvelope。

Studio 对应实现与测试见其现有分支 docs/handoff/2026-09-07-direct-edit-implementation.md。上面的旧“后续队列”是本次修订前状态；源码完成程度以该交接中的“本批源码变化”和“仍待完成”区分，不把旧队列全部当作尚未开始，也不把自动化测试当作真实模型验收。
