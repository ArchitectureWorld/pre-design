# 前期策划 Agent｜DSH 插件开发交接包 FINAL v2.0

## 使用顺序

1. 阅读 `01_前期策划Agent_DSH插件开发_HANDOFF_FINAL_v2.0.md`；
2. 解压 `03_前期策划_v0.6_DSH完整技术合同包_CANONICAL_949.zip`；
3. 在合同包根目录运行 `python tests/test_contracts.py`，必须得到 `949 passed / 0 failed`；
4. 打开 `04_前期策划_章节工具级母架构_LATEST.html`，确认 01—08 专业语义；
5. 将 `02_下一会话启动指令_前期策划_DSH插件开发_v2.0.txt` 粘贴给下一开发会话；
6. 直接执行 Handoff 中的 D0 → D1，不重新讨论产品路线。

## 唯一 canonical 合同包

只使用：

```text
03_前期策划_v0.6_DSH完整技术合同包_CANONICAL_949.zip
```

不要使用仍记录 `797/797` 的旧合同包。

## 当前起点

```text
专业方法与技术合同已封版
→ 尚未实现可安装 DSH 插件
→ 下一步先实测 DSH 版本与扩展点
→ 完成原生 Bundle / Client 冒烟
→ 使用 DSH 已配置的真实模型跑通最小项目闭环
```
